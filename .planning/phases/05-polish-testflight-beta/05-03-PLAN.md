---
phase: 05-polish-testflight-beta
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - apps/mobile/src/lib/perf.ts
  - apps/mobile/app/_layout.tsx
  - apps/mobile/src/features/dashboard/DashboardScreen.tsx
  - apps/mobile/src/features/dashboard/DonutChart.tsx
  - apps/mobile/src/features/transactions/TransactionList.tsx
autonomous: false
requirements: [QUAL-05, QUAL-06]

must_haves:
  truths:
    - "Cold start to first interactive frame is measured on every launch and the number is observable on-device"
    - "Skia dashboard chart first-frame time is measured and observable on-device"
    - "Cold start completes in under 2 seconds on iPhone SE 2020"
    - "Skia chart renders its first frame in under 100ms when the dashboard mounts"
    - "Perf instrumentation does not hard-fail when Sentry EU keys are absent (graceful on-device fallback)"
    - "No transaction detail is written to perf logs in production builds"
  artifacts:
    - path: "apps/mobile/src/lib/perf.ts"
      provides: "Sentry-optional cold-start + first-frame measurement utility"
      contains: "markColdStart"
    - path: "apps/mobile/app/_layout.tsx"
      provides: "Cold-start start mark + ready mark around the render gate"
      contains: "perf"
    - path: "apps/mobile/src/features/dashboard/DonutChart.tsx"
      provides: "Skia first-frame mark on first paint"
      contains: "perf"
  key_links:
    - from: "apps/mobile/app/_layout.tsx"
      to: "src/lib/perf.ts"
      via: "markColdStart at module load + markAppReady when gate opens"
      pattern: "markColdStart|markAppReady"
    - from: "apps/mobile/src/features/dashboard/DonutChart.tsx"
      to: "src/lib/perf.ts"
      via: "markFirstFrame on first Skia paint"
      pattern: "markFirstFrame"
---

<objective>
Deliver the measured performance slice end-to-end: instrument cold-start and Skia
first-frame, observe the numbers on-device, and tune the hot paths to hit
< 2s cold start (QUAL-05) and < 100ms Skia first frame (QUAL-06) on iPhone SE 2020.

Purpose: QUAL-05/QUAL-06 are hard ROADMAP success criteria (#5). Without
instrumentation they are unverifiable. Sentry EU DSN is user-blocked (P0 #7), so
measurement must work standalone and only enrich into Sentry if keys exist.
Output: A Sentry-optional perf utility, cold-start + first-frame marks, and a
documented tuning pass with measured before/after numbers in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-testflight-beta/05-CONTEXT.md
@.planning/phases/05-polish-testflight-beta/05-PATTERNS.md
@./CLAUDE.md
@.planning/phases/05-polish-testflight-beta/05-01-SUMMARY.md

<interfaces>
<!-- Depends on 05-01: app/_layout.tsx now has the biometric render gate
     (if (!fontsLoaded || !i18nReady || !dbReady || !biometricPassed) return null).
     Read 05-01-SUMMARY.md for the exact final gate shape — the app-ready mark
     must be placed where the gate opens, AFTER biometric pass when enabled. -->

Cold-start critical path = app/_layout.tsx: 3 font families + initI18n() +
runMigrations() gate first render. 05-01 added the biometric gate condition.

@sentry/react-native ~7.2.0 installed but NOT initialized anywhere (P0 #7 —
EU DSN absent). perf.ts must detect Sentry absence and fall back to an
on-device measurement (timestamp delta + __DEV__ console + in-app debug surface).

Skia chart: src/features/dashboard/DonutChart.tsx (Skia <Canvas>); first
paintable frame is the QUAL-06 measurement point.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Sentry-optional perf utility + cold-start & first-frame marks</name>
  <files>apps/mobile/src/lib/perf.ts, apps/mobile/app/_layout.tsx, apps/mobile/src/features/dashboard/DonutChart.tsx</files>
  <read_first>
    - .planning/phases/05-polish-testflight-beta/05-01-SUMMARY.md (exact final render-gate shape in _layout.tsx after 05-01 — where the gate opens is the app-ready mark point)
    - apps/mobile/app/_layout.tsx (full file post-05-01 — module-load point for the start mark, the gate line for the ready mark)
    - apps/mobile/src/features/dashboard/DonutChart.tsx (Skia <Canvas> mount/first-paint point for the first-frame mark)
    - apps/mobile/src/lib/secure.ts (project utility module conventions — typed exports, no default export, error-name-only logging)
    - .planning/phases/05-polish-testflight-beta/05-PATTERNS.md §"Shared Patterns / Error handling" (log name only, never data — applies to perf logs)
    - .planning/STATE.md §"P0 Outstanding" #7 (Sentry/PostHog EU keys blocked — graceful-absence requirement)
  </read_first>
  <action>
    Create `src/lib/perf.ts` exporting `markColdStart()`, `markAppReady()`, `markFirstFrame()`, and a `getPerfReport()` returning `{ coldStartMs, firstFrameMs }`. Use a module-scope start timestamp captured at import time (`markColdStart` records it). `markAppReady` computes `coldStartMs = now - start`; `markFirstFrame` computes `firstFrameMs` from the cold-start origin. Sentry is OPTIONAL: detect whether `@sentry/react-native` has an active client (e.g. a try/catch around a guarded `Sentry.getCurrentHub?.().getClient?.()` style check) — if a client exists, attach the measurement (span/measurement); if not, fall back silently to on-device only (store in module state + `__DEV__` `console.log` of the millisecond number ONLY — never transaction/financial data; CLAUDE.md). No throw if Sentry absent (P0 #7 graceful-absence — perf must never hard-fail the app). In `app/_layout.tsx`: call `markColdStart()` at module load (top-level, before component); call `markAppReady()` exactly when the render gate opens (the first render where `fontsLoaded && i18nReady && dbReady && biometricPassed` — place it so biometric auth time is excluded from the cold-start product metric if the gate is enabled, OR documented clearly in SUMMARY which definition is used; default: measure to first render of the Stack). In `DonutChart.tsx`: call `markFirstFrame()` on the first committed Skia paint (e.g. via the Skia canvas onLayout/first effect after the picture is ready) — guard so it fires once.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit && npx expo lint</automated>
  </verify>
  <acceptance_criteria>
    - MACHINE: `cd apps/mobile && npx tsc --noEmit` exits 0.
    - MACHINE: `cd apps/mobile && npx expo lint` exits 0.
    - SOURCE: `grep -n "markColdStart\|markAppReady\|markFirstFrame\|getPerfReport" apps/mobile/src/lib/perf.ts` returns all four exports.
    - SOURCE: `grep -n "markColdStart" apps/mobile/app/_layout.tsx` (module-load) and `grep -n "markAppReady" apps/mobile/app/_layout.tsx` (at gate open) both return matches.
    - SOURCE: `grep -n "markFirstFrame" apps/mobile/src/features/dashboard/DonutChart.tsx` returns a match.
    - SOURCE: perf.ts does NOT throw when Sentry is absent — `grep -n "throw" apps/mobile/src/lib/perf.ts` shows no unconditional throw on the Sentry-absent path; a Sentry-presence guard (try/catch or optional-chaining client check) is present.
    - SOURCE: `grep -n "console.log" apps/mobile/src/lib/perf.ts` — any log is `__DEV__`-gated and logs only the numeric ms, never transaction/financial fields.
    - UAT (device): On iPhone SE 2020, observe the logged coldStartMs and firstFrameMs over 3+ cold starts (Sentry absent path). Record numbers in SUMMARY.
  </acceptance_criteria>
  <done>perf.ts measures cold-start + Skia first-frame, works with Sentry absent (no hard-fail), wired into _layout.tsx + DonutChart; tsc + lint exit 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tuning pass — hit < 2s cold start and < 100ms Skia first frame</name>
  <files>apps/mobile/app/_layout.tsx, apps/mobile/src/features/dashboard/DashboardScreen.tsx, apps/mobile/src/features/dashboard/DonutChart.tsx, apps/mobile/src/features/transactions/TransactionList.tsx</files>
  <read_first>
    - apps/mobile/app/_layout.tsx (cold-start critical path: font load + initI18n + runMigrations + biometric gate — identify deferrable work)
    - apps/mobile/src/features/dashboard/DashboardScreen.tsx (dashboard mount path feeding the Skia chart)
    - apps/mobile/src/features/dashboard/DonutChart.tsx (Skia <Canvas> — first-frame cost; memoization / picture-precompute opportunities)
    - apps/mobile/src/features/transactions/TransactionList.tsx (FlashList config — estimatedItemSize, recycling, render cost; relevant to perceived cold-start when list is initial route)
    - .planning/phases/05-polish-testflight-beta/05-PATTERNS.md §"Shared Patterns" (StyleSheet/tokens/parameterized SQL — do not regress these while tuning)
    - context7: @shopify/flash-list and @shopify/react-native-skia perf guidance (resolve-library-id → query-docs before changing config)
  </read_first>
  <action>
    Using the Task 1 measurements as the baseline, apply targeted tuning ONLY where the marks show budget overruns. Cold start (< 2s, QUAL-05): defer any non-gating work out of the first-render critical path in `_layout.tsx` (the gate must keep blocking on fonts/i18n/migrations/biometric — do not weaken correctness; only move genuinely non-critical init to after first render); avoid synchronous heavy work in module scope. Skia first frame (< 100ms, QUAL-06): in `DonutChart.tsx` memoize the Skia path/picture so the first paint does not recompute geometry per render (`useMemo` on the arc/path derived from props), avoid layout thrash, ensure the canvas mounts with data already available rather than after an async round-trip. In `DashboardScreen.tsx` ensure the chart's data is ready synchronously from the local DB (op-sqlite executeSync) so the chart is not gated on an async fetch. In `TransactionList.tsx` confirm/repair FlashList tuning (`estimatedItemSize`, stable `keyExtractor`, no inline component definitions per row, no anonymous style objects) per context7 FlashList guidance. Do NOT introduce hardcoded hex, inline style objects, or non-token values while tuning (CLAUDE.md design rules — banned values must not appear). Re-measure with Task 1 marks and record before/after coldStartMs + firstFrameMs (iPhone SE 2020) in the SUMMARY; if hardware unavailable, record the on-device numbers from whatever device is used and flag QUAL-05/06 evidence as device-only pending the SE 2020 run (P0 #1).
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit && npx expo lint</automated>
  </verify>
  <acceptance_criteria>
    - MACHINE: `cd apps/mobile && npx tsc --noEmit` exits 0.
    - MACHINE: `cd apps/mobile && npx expo lint` exits 0.
    - SOURCE: `grep -rn "#667EEA\|#8B7AB8\|#E8E0FF\|#10B981\|#1A73E8\|#2563EB" apps/mobile/src/features/dashboard apps/mobile/src/features/transactions apps/mobile/app/_layout.tsx` returns NO matches (no banned hex introduced).
    - SOURCE: `grep -n "useMemo" apps/mobile/src/features/dashboard/DonutChart.tsx` shows the Skia path/picture is memoized.
    - SOURCE: `grep -n "estimatedItemSize\|keyExtractor" apps/mobile/src/features/transactions/TransactionList.tsx` shows FlashList tuned with a stable key + estimated size.
    - UAT (device, iPhone SE 2020 preferred): cold start < 2000 ms (median of ≥3 launches) — QUAL-05. Skia dashboard chart first frame < 100 ms — QUAL-06. Before/after numbers recorded in SUMMARY. If SE 2020 unavailable, numbers from the test device recorded and flagged device-only pending the SE 2020 confirmation.
  </acceptance_criteria>
  <done>Critical path tuned without weakening the render gate or design rules; measured cold-start and Skia first-frame numbers recorded in SUMMARY; tsc + lint exit 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Device measurement — cold start < 2s, Skia first frame < 100ms</name>
  <what-built>
    Sentry-optional perf instrumentation + a tuning pass targeting QUAL-05
    (< 2s cold start) and QUAL-06 (< 100ms Skia first frame) on iPhone SE 2020.
  </what-built>
  <how-to-verify>
    On a physical iPhone (iPhone SE 2020 if available) via a release-like build:
    1. Cold-launch the app 3+ times from a killed state; read the logged
       coldStartMs (Sentry-absent on-device path). Median should be < 2000 ms.
    2. Open the dashboard; read the logged firstFrameMs for the Skia donut chart.
       Should be < 100 ms.
    3. Confirm no perf log prints any transaction/financial data — only ms numbers.
    4. Record the before/after numbers; if SE 2020 is unavailable, note the device
       used and that QUAL-05/06 evidence is device-only pending the SE 2020 run.
  </how-to-verify>
  <resume-signal>Type "approved" with the measured numbers, or describe regressions (UAT items if device unavailable — P0 #1)</resume-signal>
  <files>apps/mobile (no source files modified — measurement only)</files>
  <action>Perform the device measurement steps in <how-to-verify> on iPhone SE 2020 (or the available device, flagged device-only). Record median coldStartMs over >=3 launches and the Skia firstFrameMs. This task gates on the measured numbers meeting QUAL-05/QUAL-06.</action>
  <verify>
    <human-check>Human reports the measured coldStartMs (<2000 median) + firstFrameMs (<100) or regressions. No CLI gate (on-device measurement — jest harness absent).</human-check>
  </verify>
  <done>Measured cold start <2s and Skia first frame <100ms recorded in SUMMARY (or device-only numbers flagged pending the SE 2020 run, P0 #1).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → perf logs / Sentry | Performance instrumentation could capture or transmit sensitive data |
| app → @sentry/react-native (optional) | Sentry client may be absent (P0 #7) — code must not assume presence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Information Disclosure | perf.ts logging | mitigate | Only millisecond integers logged; `__DEV__`-gated; never transaction/jar/financial fields (CLAUDE.md grep-gate enforced). |
| T-05-13 | Denial of Service | Sentry absent crash | mitigate | Sentry presence guarded (try/catch / optional client check); no unconditional throw on absent path — app never hard-fails on missing EU DSN (P0 #7 graceful-absence). |
| T-05-14 | Tampering | tuning weakening render gate | mitigate | Render gate must keep blocking on fonts/i18n/migrations/biometric; only non-critical init may be deferred; correctness regressions called out as forbidden in the action. |
| T-05-SC | Tampering | npm installs | mitigate | No new packages — @sentry/react-native already a dep; no FlashList/Skia version change without context7 verification. No legitimacy checkpoint needed. |
</threat_model>

<verification>
- `cd apps/mobile && npx tsc --noEmit` exits 0
- `cd apps/mobile && npx expo lint` exits 0
- Source grep-gates (perf exports, mark wiring, no banned hex, FlashList tuning) pass
- Device measurement (Task 3) recorded; flagged device-only pending SE 2020 if unavailable (P0 #1 / P0 #7 graceful absence)
- No jest / npm test dependency (harness absent)
</verification>

<success_criteria>
- Cold start measured and < 2s on iPhone SE 2020 (ROADMAP SC#5, QUAL-05)
- Skia chart first frame measured and < 100ms (QUAL-06)
- Perf instrumentation works with Sentry EU keys absent (P0 #7 graceful absence)
- No financial data in perf logs (CLAUDE.md)
- No design-rule regression introduced during tuning
- tsc + lint green
</success_criteria>

<output>
Create `.planning/phases/05-polish-testflight-beta/05-03-SUMMARY.md` when done (include measured before/after coldStartMs + firstFrameMs)
</output>
