---
phase: 02-dashboard-transactions-categories
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/data/transactionsRepo.ts
  - apps/mobile/src/data/categoriesRepo.ts
  - apps/mobile/src/data/dashboardRepo.ts
  - apps/mobile/src/design/icons/tabs/index.tsx
  - apps/mobile/src/design/icons/tabs/DashboardIcon.tsx
  - apps/mobile/src/design/icons/tabs/TransactionsIcon.tsx
  - apps/mobile/src/design/icons/tabs/CategoriesIcon.tsx
  - apps/mobile/src/design/icons/chevrons/ChevronLeft.tsx
  - apps/mobile/src/design/icons/chevrons/ChevronRight.tsx
  - apps/mobile/src/features/dashboard/types.ts
  - apps/mobile/src/features/dashboard/useMonthData.ts
  - apps/mobile/src/features/dashboard/MonthSwiper.tsx
  - apps/mobile/src/features/dashboard/MonthlyTotalHero.tsx
  - apps/mobile/src/features/dashboard/DonutChart.tsx
  - apps/mobile/src/features/dashboard/CategoryRow.tsx
  - apps/mobile/src/features/dashboard/EmptyState.tsx
  - apps/mobile/src/features/dashboard/donutArcs.ts
  - apps/mobile/src/features/dashboard/donutArcs.test.ts
  - apps/mobile/src/features/dashboard/monthMath.ts
  - apps/mobile/src/features/dashboard/monthMath.test.ts
  - apps/mobile/src/i18n/locales/en/dashboard.json
  - apps/mobile/src/i18n/locales/uk/dashboard.json
  - apps/mobile/app/(tabs)/_layout.tsx
  - apps/mobile/app/(tabs)/index.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
must_haves:
  truths:
    - "Tab bar shows three tabs (Overview / Transactions / Categories) with custom SVG icons — no emoji, no lucide"
    - "Dashboard screen replaces Phase 1 minimal dashboard and renders MonthSwiper, MonthlyTotalHero, DonutChart, and TopCategoryRows in that vertical order"
    - "User can swipe horizontally between months from earliest-tx month to current+1; future month shows the empty-state placeholder per D-02"
    - "Skia donut renders top-5 categories sorted descending by absolute amount with an Other slice for the rest (D-06)"
    - "Tapping a donut slice morphs the center label to category name + amount + percentage (D-04) with 200ms opacity+translateY"
    - "On month change donut slices interpolate via withTiming 300ms Easing.out(Easing.cubic) (D-05)"
    - "Empty state (current-month) renders the editorial italic phrase 'This month is a blank page.' + CTA 'Add transaction' (D-23, D-24)"
  artifacts:
    - path: "apps/mobile/app/(tabs)/index.tsx"
      provides: "Replaced dashboard screen — MonthSwiper + MonthlyTotalHero + DonutChart + CategoryRows + EmptyState"
      contains: "MonthSwiper"
    - path: "apps/mobile/app/(tabs)/_layout.tsx"
      provides: "Three-tab bar with custom SVG icons"
      contains: "TransactionsIcon"
    - path: "apps/mobile/src/features/dashboard/DonutChart.tsx"
      provides: "Skia donut, tappable slices, center-label morph, withTiming animation"
      min_lines: 80
    - path: "apps/mobile/src/features/dashboard/MonthSwiper.tsx"
      provides: "Horizontal pan gesture month navigator"
      min_lines: 50
    - path: "apps/mobile/src/data/dashboardRepo.ts"
      provides: "getMonthlyExpenseTotal, getCategoryBreakdown, getMonthsWithTransactions"
      exports: ["getMonthlyExpenseTotal", "getCategoryBreakdown", "getMonthsWithTransactions"]
    - path: "apps/mobile/src/i18n/locales/en/dashboard.json"
      provides: "dashboard.* string keys (empty_month, empty_future, error_load, cta_add_transaction)"
      contains: "empty_month"
  key_links:
    - from: "apps/mobile/app/(tabs)/index.tsx"
      to: "apps/mobile/src/data/dashboardRepo.ts"
      via: "useFocusEffect → executeSync queries"
      pattern: "getCategoryBreakdown|getMonthlyExpenseTotal"
    - from: "apps/mobile/src/features/dashboard/DonutChart.tsx"
      to: "apps/mobile/src/features/dashboard/donutArcs.ts"
      via: "buildDonutArcs(slices, radius, strokeWidth)"
      pattern: "buildDonutArcs"
    - from: "apps/mobile/app/(tabs)/_layout.tsx"
      to: "apps/mobile/src/design/icons/tabs/*"
      via: "tabBarIcon render prop"
      pattern: "DashboardIcon|TransactionsIcon|CategoriesIcon"
---

<scope_rationale>
File count = 23, above the 15-file threshold. Plan accepted without split per the following breakdown:

- **7 mechanical files**: 5 tab/chevron SVG icon components (DashboardIcon, TransactionsIcon, CategoriesIcon, ChevronLeft, ChevronRight) + 2 i18n JSON files (en/dashboard.json, uk/dashboard.json). Per-file edit is a trivial pattern.
- **8 pure-logic files**: 3 repo files (transactionsRepo.ts extension with listByMonth, new dashboardRepo.ts, new categoriesRepo.ts read scaffold) + 2 type/helper files (features/dashboard/types.ts, monthMath.ts) + 3 unit-test/scaffold files (donutArcs.ts + donutArcs.test.ts + monthMath.test.ts).
- **8 UI component files**: MonthSwiper, MonthlyTotalHero, DonutChart, CategoryRow, EmptyState, useMonthData hook, app/(tabs)/_layout.tsx, app/(tabs)/index.tsx — plus the icons barrel `src/design/icons/tabs/index.tsx`.

23 files breaks into 7 mechanical + 8 pure-logic + 8 UI components. The mechanical SVG icons follow one pattern repeated five times; the repos are additive (no contradicting transforms); the UI components are independent of each other. Each cluster fits within one execution sub-context; no quality degradation expected. Plan accepted at 23 files as a Wave 1 foundation budget.
</scope_rationale>

<objective>
Establish the Phase 2 foundation: replace Phase 1 minimal dashboard with the editorial dashboard (MonthSwiper → MonthlyTotalHero → DonutChart → CategoryRows → EmptyState), extend the tab bar to three tabs with custom SVG icons, and create the shared category + dashboard repositories that downstream plans (02-02, 02-03, 02-04) will consume.

Purpose: D-01 mandates the editorial scrollable layout with no fixed shell; this plan delivers it and the tab structure on which Plans 02 / 03 / 04 sit. The Skia donut, MonthSwiper, dashboardRepo, and categoriesRepo are shared assets — building them in Wave 1 unblocks parallel work in Wave 2.

Output: Working three-tab app with a navigable, animated dashboard for the current month plus past months with data, plus the dashboard's empty/future states.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-transactions-categories/02-CONTEXT.md
@.planning/phases/02-dashboard-transactions-categories/02-UI-SPEC.md
@.planning/phases/01-onboarding-data-ingest/01-LEARNINGS.md
@.planning/phases/01-onboarding-data-ingest/01-SKELETON.md
@CLAUDE.md
@apps/mobile/src/design/tokens.ts
@apps/mobile/src/design/typography.ts
@apps/mobile/src/data/transactionsRepo.ts
@apps/mobile/src/lib/db/index.ts
@apps/mobile/app/(tabs)/_layout.tsx
@apps/mobile/app/(tabs)/index.tsx

<interfaces>
<!-- Contracts this plan establishes — downstream plans (02-02..04) consume these. -->

apps/mobile/src/features/dashboard/types.ts (new):
```typescript
export type CategorySlice = {
  readonly categoryId: number;
  readonly slug: string;
  readonly nameEn: string;
  readonly color: string;          // hex from D-22 swatch set
  readonly amountCents: number;    // absolute, positive
  readonly percentage: number;     // 0..1 of monthly total
};

export type MonthKey = { readonly year: number; readonly month: number }; // month: 1..12

export type CategoryBreakdown = {
  readonly top: readonly CategorySlice[];   // up to 5 slices
  readonly other: CategorySlice | null;     // aggregated remainder, color = COLORS.textMuted
  readonly totalExpenseCents: number;       // sum of expense (positive cents)
};
```

apps/mobile/src/data/dashboardRepo.ts (new):
- `getMonthlyExpenseTotal(year: number, month: number): number` — sum of negative-cents transactions in month, returned as positive cents.
- `getCategoryBreakdown(year: number, month: number): CategoryBreakdown` — descending-by-absolute-amount, top 5 + Other.
- `getMonthsWithTransactions(): readonly MonthKey[]` — distinct year/month tuples sorted ascending; used to bound MonthSwiper.

apps/mobile/src/data/categoriesRepo.ts (new — scaffold; full CRUD lands in 02-04):
- `listCategories(): readonly Category[]` — id, slug, name_en, name_uk, icon_slug, color, usage_count.
- `getCategoryById(id: number): Category | null`.

apps/mobile/src/features/dashboard/donutArcs.ts (new):
- `buildDonutArcs(slices: readonly CategorySlice[], other: CategorySlice | null, radius: number, strokeWidth: number, gapDeg: number): readonly { path: string; color: string; categoryId: number | 'other' }[]` — pure function, returns SVG-style arc path strings consumable by Skia `Path.MakeFromSVGString` for each slice.

apps/mobile/src/features/dashboard/monthMath.ts (new):
- `clampMonth(target: MonthKey, earliest: MonthKey, latestPlusOne: MonthKey): MonthKey` — past-lock + future-lock per D-02.
- `addMonths(key: MonthKey, delta: number): MonthKey` — month arithmetic (handles Dec→Jan rollover).
- `isFutureMonth(key: MonthKey, today: Date): boolean` — true if key > current month.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure logic — donutArcs, monthMath, dashboardRepo queries</name>
  <files>apps/mobile/src/features/dashboard/types.ts, apps/mobile/src/features/dashboard/donutArcs.ts, apps/mobile/src/features/dashboard/donutArcs.test.ts, apps/mobile/src/features/dashboard/monthMath.ts, apps/mobile/src/features/dashboard/monthMath.test.ts, apps/mobile/src/data/dashboardRepo.ts, apps/mobile/src/data/categoriesRepo.ts, apps/mobile/src/data/transactionsRepo.ts</files>
  <read_first>
    - apps/mobile/src/data/transactionsRepo.ts (existing repo pattern — countTransactions, sumLastNDays, insertManyTransactions)
    - apps/mobile/src/lib/db/index.ts (DB singleton, executeSync API, splitStatements pattern)
    - .planning/phases/01-onboarding-data-ingest/01-LEARNINGS.md §"op-sqlite v15 executeSync", §"slug→id pattern", §"node:test + tsx" (Pattern 11)
    - .planning/phases/01-onboarding-data-ingest/01-SKELETON.md (negative cents = expense, positive = income)
    - .planning/phases/02-dashboard-transactions-categories/02-CONTEXT.md §"D-06" (slice ordering), §"D-22" (8 swatch colors)
    - .planning/phases/02-dashboard-transactions-categories/02-UI-SPEC.md §"DonutChart" (stroke 10pt, gap 2pt, top 5 + Other)
  </read_first>
  <behavior>
    - donutArcs: 3 slices [50%, 30%, 20%] with radius=100, stroke=10, gap=2 → 3 arc paths whose start/end angles cover 360° minus gaps; verify total swept angle = 360 − 3×gap_angle.
    - donutArcs: empty slices → returns empty array (no NaN paths).
    - donutArcs: 6 slices [40,20,15,10,8,7] → returns top 5 + 1 Other (sum=7); Other slice color = COLORS.textMuted.
    - donutArcs: single slice 100% → one full ring with gap cut.
    - monthMath.addMonths({year:2026,month:12}, 1) → {year:2027,month:1}.
    - monthMath.addMonths({year:2026,month:1}, -1) → {year:2025,month:12}.
    - monthMath.clampMonth — target before earliest → earliest; target after latestPlusOne → latestPlusOne.
    - monthMath.isFutureMonth({2027,1}, Date('2026-05-14')) → true; same month → false.
    - dashboardRepo.getMonthlyExpenseTotal — sums absolute value of negative cents only, ignores income; returns 0 for empty month.
    - dashboardRepo.getCategoryBreakdown — sorts descending absolute amount, slices top 5 explicit + remainder aggregated to one Other slice; percentages sum to 1.0 ± 0.0001; totalExpenseCents matches getMonthlyExpenseTotal.
    - dashboardRepo.getMonthsWithTransactions — distinct (year,month) tuples ascending by year then month.
    - transactionsRepo extension: listByMonth(year, month) returns rows in date descending order for that calendar month.
  </behavior>
  <action>
    Create types.ts with CategorySlice / MonthKey / CategoryBreakdown exports (see <interfaces> above).

    Create donutArcs.ts implementing pure arc math per D-06 and UI-SPEC DonutChart spec (stroke 10pt, gap 2pt → ~1° per side in angle space; tune for visible 2pt gap at radius=100). Use SVG arc path commands ("M cx cy m -r 0 a r r 0 startEndFlag endX endY"). Color assignment per slice: use input slice.color (which the caller supplies from D-22 swatch palette via categoriesRepo). The Other slice color is `COLORS.textMuted` from tokens.ts.

    Create monthMath.ts with addMonths, clampMonth, isFutureMonth, formatMonthLabel(key, locale) → uses Intl.DateTimeFormat({month:'long', year:'numeric'}).

    Create dashboardRepo.ts using executeSync (per 01-LEARNINGS Pattern 1):
    - getMonthlyExpenseTotal: `SELECT COALESCE(-SUM(amount_cents), 0) AS total FROM transactions WHERE amount_cents < 0 AND occurred_at >= ? AND occurred_at < ?` with month-start/next-month-start ISO date bounds.
    - getCategoryBreakdown: `SELECT category_id, -SUM(amount_cents) AS abs_total FROM transactions WHERE amount_cents < 0 AND occurred_at BETWEEN ? AND ? GROUP BY category_id ORDER BY abs_total DESC`. Join categories table to enrich with slug, name_en, color. Aggregate index ≥5 into the Other slice. Compute percentages off totalExpenseCents.
    - getMonthsWithTransactions: `SELECT DISTINCT strftime('%Y', occurred_at) AS y, strftime('%m', occurred_at) AS m FROM transactions ORDER BY y, m`.

    Create categoriesRepo.ts scaffold: listCategories (SELECT * FROM categories ORDER BY usage_count DESC), getCategoryById (parameterized). Full mutation surface (create/rename/delete/merge) lands in 02-04 — this plan creates the read scaffold only so dashboardRepo can use it.

    Extend transactionsRepo.ts with listByMonth(year, month): parameterized SELECT with `occurred_at` bounds, ORDER BY occurred_at DESC, id DESC.

    Write tests in donutArcs.test.ts and monthMath.test.ts using node:test + tsx pattern from 01-LEARNINGS Pattern 11. Run via `npx tsx --test`. dashboardRepo / categoriesRepo are not unit-tested here (they need DB) — they are verified by device checkpoint in the final phase task.

    Per D-22: colors are stored in the categories table as hex strings — verify the Phase 1 seed already used D-22 swatches. If it didn't, update seed colors to D-22 palette and add a single-statement migration in db/migrations to backfill (but do not modify existing IDs).
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit && npx tsx --test src/features/dashboard/donutArcs.test.ts src/features/dashboard/monthMath.test.ts</automated>
  </verify>
  <done>Tests for donutArcs and monthMath pass; tsc exits 0; dashboardRepo exports getMonthlyExpenseTotal, getCategoryBreakdown, getMonthsWithTransactions; categoriesRepo exports listCategories, getCategoryById; transactionsRepo exports listByMonth (in addition to existing).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Custom SVG icon set (tabs + chevrons) and three-tab bar wiring</name>
  <files>apps/mobile/src/design/icons/tabs/index.tsx, apps/mobile/src/design/icons/tabs/DashboardIcon.tsx, apps/mobile/src/design/icons/tabs/TransactionsIcon.tsx, apps/mobile/src/design/icons/tabs/CategoriesIcon.tsx, apps/mobile/src/design/icons/chevrons/ChevronLeft.tsx, apps/mobile/src/design/icons/chevrons/ChevronRight.tsx, apps/mobile/app/(tabs)/_layout.tsx</files>
  <read_first>
    - apps/mobile/app/(tabs)/_layout.tsx (current Phase 1 tab structure)
    - apps/mobile/src/design/tokens.ts (COLORS.accent, COLORS.textMuted, COLORS.surface)
    - apps/mobile/src/design/typography.ts (TYPE.uiLabel — tab label preset per UI-SPEC)
    - .planning/phases/02-dashboard-transactions-categories/02-UI-SPEC.md §"Tab Bar" (3 tabs, icons 24×24pt, label TYPE.uiLabel, active = COLORS.accent, inactive = COLORS.textMuted, border-top 1pt textMuted @ 20%)
    - .planning/phases/02-dashboard-transactions-categories/02-CONTEXT.md §"D-21" (NO lucide, NO emoji, SVG only)
    - CLAUDE.md §"Design rules" (banned values list)
  </read_first>
  <behavior>
    - Each tab icon component renders an SVG with viewBox "0 0 24 24" and accepts `color: string` + `size?: number` props.
    - Active state uses `COLORS.accent`, inactive uses `COLORS.textMuted` (UI-SPEC Tab Bar).
    - No raster, no emoji, no lucide import — `grep -rE "from ['\"]lucide-react-native['\"]|🏠|💸|🏷" apps/mobile/app apps/mobile/src/design/icons` returns 0 matches.
    - Tab labels: "Overview", "Transactions", "Categories" — exact strings from UI-SPEC §Tab Bar.
    - Tab bar background = `COLORS.surface`, border-top 1pt `COLORS.textMuted` @ 20% opacity.
    - Tabs declared in order: index (Dashboard), transactions, categories. Routing screens `transactions/index.tsx` and `categories/index.tsx` will be created in 02-03 and 02-04 — this task creates placeholder route files only sufficient to allow the tab bar to compile (a single `<View><Text>...</Text></View>` "Coming next" placeholder is acceptable; downstream plans replace them).
  </behavior>
  <action>
    Create 3 hand-drawn-style tab icons (DashboardIcon, TransactionsIcon, CategoriesIcon) per UI-SPEC §"Tab Bar". Hand-drawn aesthetic (D-21) means slightly imperfect strokes — use `strokeWidth=1.6`, `strokeLinecap="round"`, `strokeLinejoin="round"`, no fill, and SVG `<Path>` shapes with one or two extra slightly-off control-point coordinates to suggest hand drawing. Suggested shapes: DashboardIcon = simple 2×2 rectangle grid with rounded corners; TransactionsIcon = three horizontal lines of slightly varying length (list pattern); CategoriesIcon = tag/label outline with a single dot for the hole. Each accepts `{ color: string; size?: number }` with `size` default 24.

    Create ChevronLeft + ChevronRight in `src/design/icons/chevrons/` (used by MonthSwiper in Task 3 + by tx detail navigation later). Same prop API. Stroke-width 1.6.

    Create `src/design/icons/tabs/index.tsx` barrel exporting all three tab icons.

    Replace `app/(tabs)/_layout.tsx`:
    - Use `Tabs` from `expo-router`.
    - Three `Tabs.Screen` entries: `name="index"` (Overview), `name="transactions"` (Transactions), `name="categories"` (Categories).
    - `tabBarIcon` render prop returns the corresponding custom SVG with `color={focused ? COLORS.accent : COLORS.textMuted}`.
    - `tabBarLabel` uses `Text` with `TYPE.uiLabel`, color matching tab state, `allowFontScaling={true}`.
    - `tabBarStyle`: backgroundColor `COLORS.surface`, borderTopWidth 1, borderTopColor `${COLORS.textMuted}33` (20% via 8-bit alpha hex).
    - Each tab gets `accessibilityRole="tab"`, `accessibilityLabel="[Name] tab"`, `accessibilityState={{selected: focused}}` (UI-SPEC Tab Bar accessibility).
    - Remove Phase 1 placeholder tabs entirely.

    Create thin placeholder route files at `app/(tabs)/transactions/index.tsx` and `app/(tabs)/categories/index.tsx` returning a centered Text "Coming next" with `TYPE.uiBody`, so the tab bar compiles. Downstream plans replace these.

    NO lucide. NO emoji. NO inline hex codes in icon files — colors received via prop (Banned-values gate). NO raster assets.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit && npx expo lint && grep -rEn "from ['\"]lucide-react-native['\"]|#667EEA|#8B7AB8|#10B981|#1A73E8|#2563EB" src/design/icons app/\(tabs\) ; if [ $? -eq 0 ]; then echo "BANNED VALUES FOUND" && exit 1; else exit 0; fi</automated>
  </verify>
  <done>Three-tab bar renders with custom SVG icons (no emoji, no lucide); tsc + expo lint exit 0; banned-values grep returns no matches under icons or tabs folders; placeholder `transactions/index.tsx` and `categories/index.tsx` exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Dashboard screen — MonthSwiper, MonthlyTotalHero, DonutChart, CategoryRow, EmptyState, replace (tabs)/index.tsx</name>
  <files>apps/mobile/src/features/dashboard/useMonthData.ts, apps/mobile/src/features/dashboard/MonthSwiper.tsx, apps/mobile/src/features/dashboard/MonthlyTotalHero.tsx, apps/mobile/src/features/dashboard/DonutChart.tsx, apps/mobile/src/features/dashboard/CategoryRow.tsx, apps/mobile/src/features/dashboard/EmptyState.tsx, apps/mobile/src/i18n/locales/en/dashboard.json, apps/mobile/src/i18n/locales/uk/dashboard.json, apps/mobile/app/(tabs)/index.tsx</files>
  <read_first>
    - .planning/phases/02-dashboard-transactions-categories/02-UI-SPEC.md §"DashboardScreen", §"MonthSwiper", §"MonthlyTotalHero", §"DonutChart", §"CategoryRow", §"EmptyState"
    - .planning/phases/02-dashboard-transactions-categories/02-CONTEXT.md §"D-01..D-06" (composition + donut), §"D-23..D-24" (empty states)
    - .planning/phases/01-onboarding-data-ingest/01-LEARNINGS.md §"useFocusEffect" pattern, §"Reanimated v4 worklets", §"i18n locale files"
    - apps/mobile/src/design/tokens.ts (COLORS, SPACING, RADIUS, GRADIENTS, SHADOWS)
    - apps/mobile/src/design/typography.ts (TYPE.displayXL, TYPE.displayL, TYPE.displayM, TYPE.uiLabel, TYPE.uiBody, TYPE.tabular, TYPE.editorialBody, TYPE.uiButton)
    - apps/mobile/src/lib/money.ts (formatMoney signature)
    - apps/mobile/src/features/dashboard/types.ts (created in Task 1)
    - apps/mobile/src/features/dashboard/donutArcs.ts (buildDonutArcs)
    - apps/mobile/src/features/dashboard/monthMath.ts (addMonths, clampMonth, isFutureMonth, formatMonthLabel)
    - apps/mobile/src/data/dashboardRepo.ts (created in Task 1)
  </read_first>
  <behavior>
    - Cold-open dashboard: synchronous DB read inside useFocusEffect, donut first frame rendered within 100ms on a populated DB (D-27, QUAL-06). Acceptance is a `console.time('donut-first-frame')` / `console.timeEnd` pair around the mount path, surfaced in dev-mode only, and a comment block citing D-27. Not a CI gate (per planning_context: informational only).
    - MonthSwiper renders the current month label centered with `TYPE.displayM`; left + right chevrons hidden at past-lock / future-lock bounds.
    - Pan gesture moves the swiper horizontally; on release, `withTiming(targetOffset, { easing: Easing.out(Easing.quad), duration: 250 })` snaps to nearest month per D-02 and UI-SPEC §Animation Contract.
    - MonthlyTotalHero renders `TYPE.displayXL` Oswald hero number with `fontVariant: ['tabular-nums']`; sub-label "Total spent in [Month]" in `TYPE.uiLabel` `COLORS.textMuted`.
    - DonutChart renders a Skia canvas 200×200pt; arcs drawn from `buildDonutArcs`; tapping a slice morphs the center label (200ms opacity 0→1 + translateY 4→0) to category name + amount + percentage; tap outside → restore monthly total label per D-04.
    - On month change, all arc paths animate to new values over 300ms `Easing.out(Easing.cubic)` (D-05) via shared values.
    - CategoryRow renders icon (placeholder neutral SVG until 02-04 ships full SVG icon set — accept a single fallback `BaseCategoryIcon` for now) + color dot + name + amount + percent bar. Long-press on row → no-op in Wave 1 (CategoryEditorBottomSheet lands in 02-04); single tap → `router.push('/transactions?categoryId={id}')` (deep link consumed by 02-03).
    - EmptyState supports variants `current-month`, `future-month`, `no-search-results`, `no-categories` per UI-SPEC table. `current-month` and `future-month` are used by this plan; the other variants are exported for use by 02-03 and 02-04.
    - When `getMonthsWithTransactions()` is empty OR the selected month has total=0, EmptyState `current-month` renders below MonthlyTotalHero (which renders €0,00).
    - When `isFutureMonth(selected, today)` is true, EmptyState `future-month` renders and the donut + category rows do NOT render.
    - i18n keys present in `en/dashboard.json` (and mirrored to `uk/dashboard.json` as TODO copies — Phase 4 will translate): `empty_month`, `empty_future`, `error_load`, `cta_add_transaction`, `total_spent_in`, `digest_above_avg`, `digest_below_avg`, `digest_on_track`, `digest_first_month` (digest_* keys consumed in 02-02).
    - VoiceOver: every interactive element has `accessibilityLabel` + `accessibilityRole` per UI-SPEC; donut announces `"Spending donut chart. [Top category] [amount], [percentage]%. Double-tap a slice to see details."`.
  </behavior>
  <action>
    1. Create `useMonthData(monthKey)`: returns `{ totalCents, breakdown, isLoading, error, refresh }`. Internally calls `getMonthlyExpenseTotal` and `getCategoryBreakdown` via `executeSync` inside a `useFocusEffect` callback per 01-LEARNINGS useFocusEffect pattern. Stores result in `useState`; refresh on focus.

    2. MonthSwiper.tsx: `PanGestureHandler` (gesture-handler v2 API) + `useAnimatedGestureHandler` worklet. `Animated.View` containing month label. Chevrons (`ChevronLeft`, `ChevronRight` from Task 2) shown conditionally — hidden when `isAtPastLock` / `isAtFutureLock`. Tap on a chevron → calls `setSelectedMonth(addMonths(current, ±1))` clamped to bounds. Bounds derived from `getMonthsWithTransactions()` (earliest) and `addMonths(today, +1)` (latest). `accessibilityRole="adjustable"`, `accessibilityActions=[{name:'increment'},{name:'decrement'}]`, handled in `onAccessibilityAction`. Use `withTiming(targetOffset, { easing: Easing.out(Easing.quad), duration: 250 })` for the snap (UI-SPEC Animation Contract row "Month swipe transition").

    3. MonthlyTotalHero.tsx: `View` + two `Text`s. Number = `formatMoney(totalCents, locale)` from `src/lib/money.ts`, `TYPE.displayXL`, color `COLORS.textPrimary`, `fontVariant: ['tabular-nums']`. Sub-label uses `t('dashboard.total_spent_in', { month: formatMonthLabel(selected, locale) })`. `accessibilityRole="text"`, `accessibilityLabel="Total spent in [Month]: [formatted amount]"`.

    4. DonutChart.tsx: `@shopify/react-native-skia` `Canvas`, width=height=200. Use `Path.MakeFromSVGString(arc.path)` per slice from `buildDonutArcs`. Each slice rendered as `<Path path={...} color={color} style="stroke" strokeWidth={10} strokeCap="round" />`. Hit-testing: wrap canvas in `Pressable` and compute angle from tap location relative to center; map to slice via cumulative angle. Center label: two views layered — "default" (monthly total + "Total" subtitle) and "active" (category name + amount + percentage). Toggle via shared value `selectedSliceId`; animate label opacity/translateY via `withTiming(200ms)`. On month-change animation per D-05: drive arc shared values with `withTiming(targetSweep, {duration: 300, easing: Easing.out(Easing.cubic)})`. Add `console.time('donut-first-frame')` / `console.timeEnd` around the mount path inside `__DEV__` (D-27 informational gate). `accessibilityRole="image"`, `accessibilityLabel` per UI-SPEC.

    5. CategoryRow.tsx: `Pressable` with row layout per UI-SPEC §CategoryRow. Color dot 8pt + icon 20pt (use a single generic `BaseCategoryIcon` for now — full hand-drawn set ships in 02-04 — pass `color={slice.color}` to render in category swatch). Name `TYPE.uiBody` `COLORS.textPrimary`. Amount `TYPE.tabular` `fontVariant: ['tabular-nums']`. Percent bar 2pt height, width = `(amountCents / maxAmountInList) × 100%`, color = `slice.color + '66'` (40% via 8-bit alpha hex). On press → `router.push({ pathname: '/(tabs)/transactions', params: { categoryId: String(slice.categoryId) }})`. On long-press → no-op (TODO: invoke CategoryEditorBottomSheet from 02-04 — leave a `// TODO(02-04): open category editor` comment with the explicit decision ID). `accessibilityRole="button"`, `accessibilityLabel="[Category name]: [amount], [percentage]%"`. Min height 44pt.

    6. EmptyState.tsx: accepts `variant: 'current-month' | 'future-month' | 'no-search-results' | 'no-categories'`. Renders 120×120pt SVG illustration + EB Garamond italic phrase + CTA button. Illustration component naming: `<EmptyIllustrationFlower />` for current-month, `<EmptyIllustrationCup />` for no-search-results, `<EmptyIllustrationBranches />` for no-categories, `<EmptyIllustrationWindow />` for future-month — create these as inline minimal SVG sketches in `src/design/icons/empty/`; hand-drawn style, brand palette (`COLORS.textPrimary` + `COLORS.accent` accents), no fill of bright colors. Copy + CTA labels + i18n keys exactly per UI-SPEC §EmptyState Variant table. CTA action via `onPress` prop set by caller.

    7. `app/(tabs)/index.tsx` — REPLACE Phase 1 minimal dashboard. New screen composition (D-01 vertical order, `SPACING.lg` gaps): `<SafeAreaView><ScrollView>`
       1. `<MonthSwiper selected={...} onChange={...} earliest={...} latestPlusOne={...} />`
       2. `<MonthlyTotalHero totalCents={data.totalCents} monthKey={selected} />`
       3. If `isFutureMonth(selected, today)`: `<EmptyState variant="future-month" onCtaPress={() => router.push('/transactions/new')} />` (manual entry route from Phase 1)
       4. Else if `data.totalCents === 0`: `<EmptyState variant="current-month" onCtaPress={...} />`
       5. Else: `<DonutChart breakdown={data.breakdown} />` then map `data.breakdown.top` to `<CategoryRow />`s and append `<CategoryRow />` for Other if present.
       Section gap = `SPACING.lg` between blocks per UI-SPEC §DashboardScreen. Top padding = `SPACING.xl` + safe-area inset. Horizontal padding = `SPACING.md`. Use `StyleSheet.create()`; no inline style objects except for dynamic colors (CLAUDE.md component rules). Error path: if `useMonthData` returns `error`, render an inline `Pressable` below the hero showing `t('dashboard.error_load')` in `TYPE.uiLabel` `COLORS.error` — on press, calls `refresh()`.

    8. i18n: create `src/i18n/locales/en/dashboard.json` with all dashboard.* keys listed in the behavior block. Mirror to `uk/dashboard.json` with the same key set but English values (Phase 4 fills UK translations). Register the namespace in the existing i18n init if not already auto-loaded.

    Reminder: NEVER use inline hex codes for colors except for the existing 8-bit alpha hex suffix pattern (`${COLORS.textMuted}33`). All other colors come from `tokens.ts`. NEVER use `lucide-react-native`. NEVER use emoji. All typography via `TYPE.*` presets — `grep -rEn "fontFamily:|fontSize:" apps/mobile/src/features/dashboard apps/mobile/app/\(tabs\)/index.tsx | grep -v '^[^:]*:.*//.*token'` returns 0 (typography preset enforcement).
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit && npx expo lint && grep -rEn "lucide-react-native|🏠|💸|🏷|#667EEA|#8B7AB8|#10B981|#1A73E8|#2563EB" src/features/dashboard src/i18n/locales/en/dashboard.json app/\(tabs\)/index.tsx ; if [ $? -eq 0 ]; then echo "BANNED VALUE FOUND" && exit 1; else exit 0; fi</automated>
  </verify>
  <done>Dashboard screen replaces Phase 1; tsc + expo lint exit 0; banned-values grep returns no matches in dashboard feature; MonthSwiper / MonthlyTotalHero / DonutChart / CategoryRow / EmptyState all exported; en/dashboard.json contains all behavior-listed keys; uk/dashboard.json mirrors keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → SQL (categoryId, month, year) | Numeric values passed to executeSync queries |
| UI interaction → Dashboard render | Tap, pan, long-press gestures; no untrusted text input in this plan (search input arrives in 02-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | dashboardRepo getMonthlyExpenseTotal / getCategoryBreakdown / transactionsRepo listByMonth | mitigate | All three use parameterized executeSync with positional `?` placeholders; year/month coerced to integer via `Number()` and bounded to 1900–3000 / 1–12 before query (defense in depth even though SQLite is process-local) |
| T-02-01-02 | Denial of Service | DonutChart / dashboard mount | mitigate | Synchronous `executeSync` query bounded by month — never SELECT * across all rows; query plan exercised at most once per focus event; useFocusEffect cleanup cancels in-flight setState |
| T-02-01-03 | Information disclosure | console.log of transaction details | mitigate | Only `console.time('donut-first-frame')` allowed (no PII); explicit grep gate in verify command catches accidental `console.log(transaction)` calls; CLAUDE.md security rule referenced inline as code comment |
| T-02-01-04 | Repudiation | Category long-press → no-op | accept | Long-press TODO in CategoryRow points at 02-04; no state change occurs until that plan ships, so no audit log needed |
| T-02-01-05 | Spoofing | i18n key namespace collision | accept | dashboard.* namespace owned by this plan; no overlap with onboarding.* from Phase 1 (grep-verified) |
</threat_model>

<verification>
- `cd apps/mobile && npx tsc --noEmit` exits 0
- `cd apps/mobile && npx expo lint` exits 0
- `cd apps/mobile && npx tsx --test src/features/dashboard/donutArcs.test.ts src/features/dashboard/monthMath.test.ts` exits 0
- Banned-value grep in icons, tabs, dashboard feature, en locale returns no matches
- Three tabs visible in tab bar with custom SVG icons (no emoji, no lucide)
- Dashboard renders MonthSwiper + hero + donut + category rows in the D-01 vertical order on first launch with seeded data
- Swiping the month label horizontally navigates between months; chevrons hidden at past/future lock
- Tapping a donut slice morphs the center label per D-04; tap outside resets
- Future month renders the `future-month` empty state instead of the donut
- Current month with zero transactions renders the `current-month` empty state
</verification>

<success_criteria>
- DASH-01 (monthly dashboard with hero total + donut + breakdown rows) — implemented
- DASH-02 (month navigation with past/future lock) — implemented
- DASH-03 (donut interactions + animations) — implemented
- All five components (MonthSwiper, MonthlyTotalHero, DonutChart, CategoryRow, EmptyState) exported from `src/features/dashboard/`
- Tab bar replaced with three custom-SVG tabs
- dashboardRepo + categoriesRepo (read-only scaffold) + transactionsRepo.listByMonth available for downstream plans
- en + uk dashboard.json i18n files in place with all dashboard.* keys (uk values mirror en as TODO)
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-transactions-categories/02-01-SUMMARY.md` documenting:
- Files created / replaced (list)
- Patterns established for downstream plans (Skia donut pattern, MonthSwiper pan worklet pattern, useMonthData hook pattern, BottomSheet primitive — not yet built, deferred to 02-04, document the deferral)
- Any deviations from D-01..D-06 with rationale
- Verification log (tsc, lint, test exit codes)
</output>
