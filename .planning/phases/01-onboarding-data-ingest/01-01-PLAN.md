---
phase: 01-onboarding-data-ingest
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/app.json
  - apps/mobile/app/_layout.tsx
  - apps/mobile/app/index.tsx
  - apps/mobile/app/onboarding/_layout.tsx
  - apps/mobile/app/onboarding/welcome.tsx
  - apps/mobile/app/onboarding/data-source.tsx
  - apps/mobile/src/lib/db/index.ts
  - apps/mobile/src/lib/db/migrations.ts
  - apps/mobile/src/lib/db/schema.sql.ts
  - apps/mobile/src/lib/secure.ts
  - apps/mobile/src/lib/http.ts
  - apps/mobile/src/lib/money.ts
  - apps/mobile/src/lib/time.ts
  - apps/mobile/src/lib/i18n/index.ts
  - apps/mobile/src/lib/i18n/en.json
  - apps/mobile/src/lib/i18n/uk.json
  - apps/mobile/src/stores/onboarding.ts
  - apps/mobile/src/api/queryClient.ts
  - apps/mobile/src/components/PressableButton.tsx
  - apps/mobile/tests/tsconfig.json
  - apps/mobile/tests/money.test.ts
  - apps/mobile/tests/db-migration.test.ts
  - apps/mobile/package.json
autonomous: true
requirements:
  - ONBD-01
  - ONBD-03
user_setup: []
must_haves:
  truths:
    - "User who has never opened the app is routed to /onboarding/welcome on first launch"
    - "User picks English or Ukrainian via two large Pressable tiles with Reanimated entrance animation"
    - "User's language choice persists across cold start (verified by relaunching and seeing the language reflected)"
    - "After language pick, app navigates to /onboarding/data-source (stub screen visible with title)"
    - "SQLite DB opens, migration 001 applies, PRAGMA user_version returns 1 (verified by db-migration.test.ts)"
    - "QueryClient + i18n + DB providers are wired in app/_layout.tsx and do not crash"
  artifacts:
    - path: "apps/mobile/app/index.tsx"
      provides: "Boot redirector: reads onboarding flag, replaces stack to welcome or tabs"
      contains: "useOnboardingStore"
      min_lines: 15
    - path: "apps/mobile/app/onboarding/_layout.tsx"
      provides: "Stack layout for onboarding screens with custom Reanimated transitions"
      exports: ["default"]
      min_lines: 15
    - path: "apps/mobile/app/onboarding/welcome.tsx"
      provides: "Welcome + EN/UK pick screen"
      exports: ["default"]
      min_lines: 60
    - path: "apps/mobile/app/onboarding/data-source.tsx"
      provides: "Stub data-source screen (filled in 01-02)"
      exports: ["default"]
      min_lines: 25
    - path: "apps/mobile/src/lib/db/index.ts"
      provides: "getDB() singleton + runMigrations(db) + getSchemaVersion(db)"
      exports: ["getDB", "runMigrations", "getSchemaVersion", "openTestDB"]
      min_lines: 45
    - path: "apps/mobile/src/lib/db/migrations.ts"
      provides: "Ordered migration list with version + SQL"
      exports: ["MIGRATIONS"]
      min_lines: 30
    - path: "apps/mobile/src/lib/db/schema.sql.ts"
      provides: "Exported SQL constants — schema + seed for migration 001"
      exports: ["SCHEMA_001", "SEED_DEFAULT_CATEGORIES"]
      min_lines: 80
    - path: "apps/mobile/src/lib/secure.ts"
      provides: "Typed wrapper around expo-secure-store with namespaced keys"
      exports: ["secureGet", "secureSet", "secureDelete", "SecureKey"]
      min_lines: 25
    - path: "apps/mobile/src/lib/http.ts"
      provides: "fetch wrapper with AbortController timeout + HttpError + X-Token redaction"
      exports: ["httpJson", "HttpError", "HttpOptions"]
      min_lines: 45
    - path: "apps/mobile/src/lib/money.ts"
      provides: "Money helpers — toCents/fromCents/parseAmount/formatMoney"
      exports: ["toCents", "fromCents", "formatMoney", "parseAmount", "Money"]
      min_lines: 40
    - path: "apps/mobile/src/lib/time.ts"
      provides: "Unix seconds helpers"
      exports: ["nowSeconds", "toSeconds", "fromSeconds", "startOfDaySeconds", "formatDateISODay"]
      min_lines: 30
    - path: "apps/mobile/src/lib/i18n/index.ts"
      provides: "i18next instance + init() + setLanguage; default lng from expo-localization"
      exports: ["i18n", "initI18n", "setLanguage"]
      min_lines: 25
    - path: "apps/mobile/src/stores/onboarding.ts"
      provides: "Zustand store: language, dataSource, completed, setters; persisted via secure-store"
      exports: ["useOnboardingStore"]
      min_lines: 35
    - path: "apps/mobile/src/api/queryClient.ts"
      provides: "QueryClient singleton with sensible defaults"
      exports: ["queryClient"]
      min_lines: 8
    - path: "apps/mobile/src/components/PressableButton.tsx"
      provides: "Reusable Pressable button with token-only styling + 44pt tap target"
      exports: ["PressableButton"]
      min_lines: 40
    - path: "apps/mobile/tests/db-migration.test.ts"
      provides: "Node-only test that opens an op-sqlite DB and asserts getSchemaVersion === 1 after runMigrations"
      min_lines: 25
  key_links:
    - from: "app/_layout.tsx"
      to: "src/lib/db/index.ts::getDB()"
      via: "useEffect at mount calls runMigrations(getDB())"
      pattern: "runMigrations\\("
    - from: "app/_layout.tsx"
      to: "src/lib/i18n/index.ts::initI18n()"
      via: "Top-level await pattern via useEffect + state gate before rendering Stack"
      pattern: "initI18n\\("
    - from: "app/index.tsx"
      to: "src/stores/onboarding.ts::useOnboardingStore"
      via: "Reads .completed; calls Redirect to welcome or tabs"
      pattern: "useOnboardingStore"
    - from: "app/onboarding/welcome.tsx"
      to: "src/stores/onboarding.ts::setLanguage"
      via: "On press, calls setLanguage('en'|'uk') + i18n.changeLanguage() + router.push('/onboarding/data-source')"
      pattern: "setLanguage\\("
---

<objective>
Bring up the Walking Skeleton for Phase 1: install the local SQLite layer, the i18n layer, the persisted onboarding store, the QueryClient, the boot redirector, and the first real onboarding screen (Welcome + language pick) with Reanimated entrance animation. This plan is the spine — every later Phase 1 plan plugs into it.

Purpose: Until this lands, no other Phase 1 plan can run; they all import @lib/db, @stores/onboarding, or @lib/i18n. Doing the spine first prevents cascading rework.
Output: A user opening the app sees the welcome screen, picks a language, and is forwarded to a data-source stub. DB exists with PRAGMA user_version = 1. tsc + lint + db-migration.test.ts pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-onboarding-data-ingest/01-SKELETON.md
@CLAUDE.md
@apps/mobile/app/_layout.tsx
@apps/mobile/app.json
@apps/mobile/package.json
@apps/mobile/tsconfig.json
@apps/mobile/src/design/tokens.ts
@apps/mobile/src/design/typography.ts

<interfaces>
<!-- Already in tree; the executor reuses these, no fetch needed. -->

From apps/mobile/src/design/tokens.ts:
- export const COLORS: { background, surface, white, textPrimary, textSecondary, textMuted, accent, accentSoft, accentDeep, sage, sageSoft, sageDeep, error, success, income, expense }
- export const GRADIENTS: { primary: readonly [string, string], warm, hero, sage, dark }
- export const SPACING: { xs:4, sm:8, md:16, lg:24, xl:32, xxl:48 }
- export const RADIUS: { sm:8, md:12, lg:16, xl:24, pill:999 }
- export const SHADOWS: { card, modal }

From apps/mobile/src/design/typography.ts:
- export const TYPE: { displayXL, displayL, displayM, editorialBody, editorialLead, uiBody, uiButton, uiLabel, uiMeta, tabular }

From @op-engineering/op-sqlite:
- open({ name: string }): DB
- DB.execute(sql: string, params?: unknown[]): { rows: {_array: any[]}, rowsAffected: number, insertId: number }
- DB.executeBatch(commands: [string, unknown[]?][]): void

From expo-localization (v17):
- getLocales(): Array<{ languageCode: string | null; ... }>

From i18next v26:
- createInstance(); use(initReactI18next); init({ resources, lng, fallbackLng, interpolation: { escapeValue: false } })

From zustand v5:
- create<T>()(persist(setter, { name, storage }))
- persist storage interface: getItem(name) returns string|null|Promise; setItem(name,value); removeItem(name)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add op-sqlite plugin to app.json; add tsx devDependency</name>
  <files>apps/mobile/app.json, apps/mobile/package.json</files>
  <read_first>
    - /home/iskan/projects/soldi/CLAUDE.md
    - /home/iskan/projects/soldi/apps/mobile/app.json
    - /home/iskan/projects/soldi/apps/mobile/package.json
    - /home/iskan/projects/soldi/.planning/phases/01-onboarding-data-ingest/01-SKELETON.md
  </read_first>
  <action>
    Register the @op-engineering/op-sqlite config plugin in apps/mobile/app.json by adding the string "@op-engineering/op-sqlite" to the expo.plugins array (place after "expo-secure-store"). Keep all other existing plugin entries unchanged. Do NOT add any plugin options; default config is correct for v15.

    No npm install needed for runtime deps — @op-engineering/op-sqlite@^15.2.12 and expo-document-picker@~14.0.8 are already in package.json (verified in Phase 0). Do NOT re-add or bump versions.

    Add tsx as a devDependency in apps/mobile/package.json with version range "^4.19.0" (rev 1: WARNING W5 — codifies the test runner so plan verify blocks can use `npx tsx tests/<file>` deterministically without `--yes tsx@latest`). Add the line directly to package.json devDependencies; let the user run `npm install` separately and record the choice in SUMMARY.

    Plugin order target: ["expo-router", ["expo-splash-screen", {...}], "expo-secure-store", "@op-engineering/op-sqlite", "expo-localization", ["expo-local-authentication", {...}], ["expo-notifications", {...}], "@sentry/react-native"].
  </action>
  <verify>
    <automated>cd apps/mobile && node -e "const j=require('./app.json');const p=j.expo.plugins.flat();if(!p.includes('@op-engineering/op-sqlite'))process.exit(1);console.log('ok')"</automated>
    <automated>cd apps/mobile && node -e "const j=require('./package.json');if(!(j.devDependencies&&j.devDependencies.tsx))process.exit(1);console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/app.json contains the string "@op-engineering/op-sqlite" inside expo.plugins (verified by the node one-liner above)
    - apps/mobile/package.json still lists "@op-engineering/op-sqlite": "^15.2.12" (unchanged)
    - apps/mobile/package.json devDependencies contains "tsx" with a "^4" range
    - cd apps/mobile && npx expo config --type prebuild produces no error mentioning op-sqlite plugin (smoke-only; do not commit prebuild artefacts)
  </acceptance_criteria>
  <done>op-sqlite plugin registered; tsx in devDependencies; expo config validates the plugin.</done>
</task>

<task type="auto">
  <name>Task 2a: DB layer (schema + migrations + open/runMigrations) + Money helpers + Money + DB-migration unit tests</name>
  <files>
    apps/mobile/src/lib/db/schema.sql.ts,
    apps/mobile/src/lib/db/migrations.ts,
    apps/mobile/src/lib/db/index.ts,
    apps/mobile/src/lib/money.ts,
    apps/mobile/tests/tsconfig.json,
    apps/mobile/tests/money.test.ts,
    apps/mobile/tests/db-migration.test.ts
  </files>
  <read_first>
    - /home/iskan/projects/soldi/CLAUDE.md
    - /home/iskan/projects/soldi/.planning/phases/01-onboarding-data-ingest/01-SKELETON.md
    - /home/iskan/projects/soldi/apps/mobile/tsconfig.json
    - /home/iskan/projects/soldi/apps/mobile/package.json
  </read_first>
  <action>
    Create apps/mobile/src/lib/db/schema.sql.ts exporting two string constants:
    - SCHEMA_001: the full SQL block for migration 001 from 01-SKELETON.md Database Schema section (schema_meta, categories, accounts, transactions, merchant_overrides, all indexes). Use single template literal per CREATE TABLE; chain statements with semicolons.
    - SEED_DEFAULT_CATEGORIES: an INSERT statement with parameterized VALUES for 18 default categories (groceries, transport, eating-out, coffee, rent, utilities, mobile, entertainment, health, clothing, gifts, transfers, salary, refunds, savings, kids, pets, misc). Use real English + Ukrainian names — coordinate with translations in i18n files (added in Task 2b). icon_name strings: "shopping-cart","bus","fork-knife","coffee","home","zap","smartphone","sparkles","heart-pulse","shirt","gift","arrow-right-left","banknote","rotate-ccw","piggy-bank","baby","paw-print","more-horizontal". is_custom = 0, created_at = strftime('%s','now') as integer cast.

    Create apps/mobile/src/lib/db/migrations.ts exporting MIGRATIONS as a ReadonlyArray of objects with shape { version: number; sql: string } and exactly one entry: { version: 1, sql: SCHEMA_001 + ';' + SEED_DEFAULT_CATEGORIES }.

    Create apps/mobile/src/lib/db/index.ts:
    - Import the named `open` function from '@op-engineering/op-sqlite'
    - Import MIGRATIONS from './migrations'
    - Module-level cached singleton: `let _db: ReturnType<typeof open> | null = null`
    - export function getDB(): ReturnType<typeof open> — opens with name 'soldi.db' on first call, returns cached singleton
    - export function getSchemaVersion(db: ReturnType<typeof open>): number — runs the PRAGMA user_version statement via db.execute; parses rows._array[0].user_version (or the first column value); returns int.
    - export function runMigrations(db: ReturnType<typeof open>): void — reads current version; iterates MIGRATIONS where m.version > current. For each pending migration: call db.execute('BEGIN'); call db.execute on m.sql; build the next PRAGMA statement as a plain string concatenation (form: "PRAGMA user_version = " + String(m.version)) and call db.execute on that string; call db.execute('COMMIT'). On caught error: call db.execute('ROLLBACK') then rethrow. PRAGMA value must be a literal integer in the SQL string (op-sqlite does not bind PRAGMA args).
    - The module MUST also support a test-mode override: export function openTestDB(name: string): ReturnType<typeof open> — calls the underlying open({ name }) directly (no singleton caching), so the test file can use a temp db name.

    Create apps/mobile/src/lib/money.ts:
    - export type Money: object with amountCents:number and currency:string
    - export function toCents(amount: number): number — Math.round(amount*100), throws RangeError if !Number.isFinite
    - export function fromCents(cents: number): number — cents/100, no rounding (display layer formats)
    - export function formatMoney(m: Money, locale: string = 'en-IE'): string — uses Intl.NumberFormat with style:'currency', currency:m.currency, minimumFractionDigits:2
    - export function parseAmount(input: string): number | null — strips currency symbols/whitespace/separators; supports both 1,234.56 and 1 234,56; returns null on invalid; rejects > 1e12

    Create apps/mobile/tests/tsconfig.json that extends ../tsconfig.json with compilerOptions overrides module=NodeNext, moduleResolution=NodeNext, types=["node"], noEmit=true, and include="**/*.ts".

    Create apps/mobile/tests/money.test.ts using node:test syntax (import { test } from 'node:test'; import assert from 'node:assert/strict'; import the four functions from '../src/lib/money'). Cover:
    - toCents(1.23) equals 123
    - toCents(0.1+0.2) equals 30 (handles float drift)
    - fromCents(123) equals 1.23
    - parseAmount('€1,234.56') equals 1234.56
    - parseAmount('1 234,56') equals 1234.56
    - parseAmount('abc') equals null
    - formatMoney with { amountCents: 12345, currency: 'EUR' } and locale 'en-IE' matches the pattern of euro symbol followed by 123.45

    Create apps/mobile/tests/db-migration.test.ts (rev 1: BLOCKER B1) using node:test:
    - This is a NODE test (no React Native runtime). op-sqlite is a native-only module; on Node it will not load. Two-path strategy in priority order:
      1. Primary path: try to dynamic-import '@op-engineering/op-sqlite'. If success AND its .open is callable → use a temp file name `t-soldi-${Date.now()}.db` under os.tmpdir(). Use the new openTestDB export from src/lib/db.
      2. Fallback path: if dynamic import throws (typical on Node because op-sqlite is iOS/Android JSI), DYNAMICALLY substitute with better-sqlite3 via a tiny shim defined inline in the test file. The shim adapts db.exec(sql) and db.execute(sql) symmetrically, and implements a PRAGMA user_version getter via db.pragma('user_version'). If better-sqlite3 is NOT installed, mark the test SKIPPED via t.skip with a clear reason AND console.warn '[db-migration.test] skipping — install better-sqlite3 to enable'. Skipped does NOT fail CI.
    - Imports: node:test, node:assert/strict, MIGRATIONS from '../src/lib/db/migrations'.
    - Implement test logic inline: open a new connection via either op-sqlite.openTestDB OR the better-sqlite3 fallback; call a local helper runMigrationsOn(db) that mimics the production logic (BEGIN → run sql → set PRAGMA user_version literal → COMMIT, looping over MIGRATIONS). Then read PRAGMA user_version and assert it equals 1. Then re-run runMigrationsOn and assert idempotency (version still 1; no errors).
    - Assertions:
      - After first runMigrationsOn: version equals 1
      - SELECT COUNT(*) FROM categories returns 18
      - Re-running runMigrationsOn does NOT throw and version stays 1
      - PRAGMA table_info('transactions') returns at least the columns id, amount_cents, currency, merchant_name, date, source, external_id
      - SELECT COUNT(*) FROM sqlite_master WHERE type='index' AND name='idx_transactions_date' returns 1
    - The test MUST NOT depend on RN or expo. It MUST run via `npx tsx tests/db-migration.test.ts` in the apps/mobile directory.

    All src/lib files use the @design / @lib / @stores / @api / @components / @data path aliases per tsconfig. No ../../../ relative imports across src directory boundaries.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
    <automated>cd apps/mobile && npx tsx tests/money.test.ts</automated>
    <automated>cd apps/mobile && npx tsx tests/db-migration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/src/lib/db/schema.sql.ts contains "export const SCHEMA_001" and "export const SEED_DEFAULT_CATEGORIES" and is at least 80 lines long
    - apps/mobile/src/lib/db/migrations.ts contains "export const MIGRATIONS" and "version: 1" and is at least 30 lines long
    - apps/mobile/src/lib/db/index.ts contains "export function getDB(" and "export function runMigrations(" and "export function getSchemaVersion(" and "export function openTestDB(" and is at least 45 lines long
    - apps/mobile/src/lib/money.ts contains "export function toCents" "export function fromCents" "export function parseAmount" "export function formatMoney" and is at least 40 lines long
    - apps/mobile/tests/db-migration.test.ts exists, references MIGRATIONS from '../src/lib/db/migrations', and asserts version equals 1 outcome (grep for `user_version` AND `assert`)
    - cd apps/mobile && npx tsc --noEmit exits 0
    - cd apps/mobile && npx tsx tests/money.test.ts exits 0
    - cd apps/mobile && npx tsx tests/db-migration.test.ts exits 0 (a `skipped` outcome is acceptable IF the test output begins with `[db-migration.test] skipping` AND SUMMARY records the skip reason)
  </acceptance_criteria>
  <done>DB schema + migrations + Money helpers in place; Money + DB-migration unit tests pass on Node via npx tsx.</done>
</task>

<task type="auto">
  <name>Task 2b: i18n (en+uk JSON + init module) + secure-store wrapper + http + time helpers</name>
  <files>
    apps/mobile/src/lib/secure.ts,
    apps/mobile/src/lib/http.ts,
    apps/mobile/src/lib/time.ts,
    apps/mobile/src/lib/i18n/index.ts,
    apps/mobile/src/lib/i18n/en.json,
    apps/mobile/src/lib/i18n/uk.json
  </files>
  <read_first>
    - /home/iskan/projects/soldi/CLAUDE.md
    - /home/iskan/projects/soldi/.planning/phases/01-onboarding-data-ingest/01-SKELETON.md
    - /home/iskan/projects/soldi/apps/mobile/tsconfig.json
    - /home/iskan/projects/soldi/apps/mobile/package.json
  </read_first>
  <action>
    Create apps/mobile/src/lib/secure.ts:
    - Import the namespace SecureStore from 'expo-secure-store'
    - export type SecureKey as the literal string union: 'monobank_token' | 'monobank_token_hash' | 'soldi-onboarding'. NOTE: persist-store key 'soldi-onboarding' is in the union because Zustand's persist middleware writes the whole serialized store under that name.
    - export async function secureSet(key: SecureKey, value: string): Promise<void> — calls SecureStore.setItemAsync with the option keychainAccessible set to SecureStore.WHEN_UNLOCKED_THIS_DEVICE_ONLY
    - export async function secureGet(key: SecureKey): Promise<string | null>
    - export async function secureDelete(key: SecureKey): Promise<void>
    - Never logs the value. JSDoc on each function: "Never AsyncStorage. Never log. expo-secure-store only."

    Create apps/mobile/src/lib/http.ts:
    - export type HttpOptions: { headers?: Record<string,string>; timeoutMs?: number; method?: 'GET'|'POST'; body?: string }
    - export class HttpError extends Error with public fields status:number and bodyText:string passed to the super message
    - export async function httpJson<T>(url: string, opts: HttpOptions = {}): Promise<T> — wraps globalThis.fetch with AbortController + setTimeout (default 10000ms); on non-2xx throws HttpError with status + bodyText; on json parse failure throws. Strip "X-Token" value from any thrown error message (replace value with the literal string "[redacted]"). No console.log of headers ever.

    Create apps/mobile/src/lib/time.ts:
    - export function nowSeconds(): number — Math.floor(Date.now()/1000)
    - export function toSeconds(d: Date): number
    - export function fromSeconds(s: number): Date
    - export function startOfDaySeconds(s: number, tzOffsetMinutes?: number): number — supports current device TZ via Date methods
    - export function formatDateISODay(s: number): string — returns 'YYYY-MM-DD' in device local TZ

    Create apps/mobile/src/lib/i18n/en.json with the following keys (flat, no nesting beyond two levels):
    - common: { back, next, continue, skip, cancel, done }
    - onboarding: welcome_title ("Money, slower."), welcome_subtitle ("A quieter way to see where your money goes."), language_en ("English"), language_uk ("Ukrainian / Українська"), pick_language ("Choose your language"), pick_source_title ("Where should we get your data?"), pick_source_subtitle ("Pick one. You can change this later."), source_synthetic_title ("Try with sample data"), source_synthetic_body ("90 days of realistic spending. Best for first look."), source_manual_title ("Add manually"), source_manual_body ("Type transactions in. Most private."), source_monobank_title ("monobank"), source_monobank_body ("Paste your personal token. EU and Ukraine."), source_csv_title ("Import CSV"), source_csv_body ("From your bank's web export.")
    - categories: groceries "Groceries", transport "Transport", eating_out "Eating out", coffee "Coffee", rent "Rent", utilities "Utilities", mobile "Mobile", entertainment "Entertainment", health "Health", clothing "Clothing", gifts "Gifts", transfers "Transfers", salary "Salary", refunds "Refunds", savings "Savings", kids "Kids", pets "Pets", misc "Other"

    Create apps/mobile/src/lib/i18n/uk.json with the same keys, Ukrainian copy. For onboarding: welcome_title "Гроші, повільніше.", welcome_subtitle "Тихіший спосіб бачити, куди йдуть твої гроші.", pick_language "Обери мову", pick_source_title "Звідки візьмемо твої дані?", pick_source_subtitle "Обери одне. Зможеш змінити пізніше.", language_en "English", language_uk "Українська", source_synthetic_title "Спробувати з прикладом", source_synthetic_body "90 днів реалістичних витрат. Найкращий перший погляд.", source_manual_title "Додавати вручну", source_manual_body "Вводь транзакції самостійно. Найбільш приватно.", source_monobank_title "monobank", source_monobank_body "Встав свій персональний токен. ЄС та Україна.", source_csv_title "Імпорт CSV", source_csv_body "З веб-експорту твого банку.". Categories: groceries "Продукти", transport "Транспорт", eating_out "Заклади", coffee "Кава", rent "Оренда", utilities "Комуналка", mobile "Мобільний", entertainment "Розваги", health "Здоров'я", clothing "Одяг", gifts "Подарунки", transfers "Перекази", salary "Зарплата", refunds "Повернення", savings "Заощадження", kids "Діти", pets "Тварини", misc "Інше".

    Create apps/mobile/src/lib/i18n/index.ts:
    - import i18next from 'i18next'
    - import { initReactI18next } from 'react-i18next'
    - import the Localization namespace from 'expo-localization'
    - import en from './en.json'
    - import uk from './uk.json'
    - export const i18n = i18next.createInstance()
    - export async function initI18n(forcedLng?: 'en' | 'uk'): Promise<void> — calls i18n.use(initReactI18next).init with resources for en+uk, lng = forcedLng OR (Localization.getLocales()[0]?.languageCode === 'uk' ? 'uk' : 'en'), fallbackLng 'en', compatibilityJSON 'v4', interpolation.escapeValue false, returnNull false. Returns the resolved promise.
    - export async function setLanguage(lng: 'en' | 'uk'): Promise<void> — wraps i18n.changeLanguage.

    All files use @lib path alias where they import siblings. No ../../../ imports.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
    <automated>cd apps/mobile && grep -RIl "AsyncStorage" src/lib/ 2>/dev/null | grep -v node_modules | (read line && { echo "VIOLATION: AsyncStorage referenced"; exit 1; } || exit 0)</automated>
    <automated>cd apps/mobile && node -e "JSON.parse(require('fs').readFileSync('src/lib/i18n/en.json','utf8'));JSON.parse(require('fs').readFileSync('src/lib/i18n/uk.json','utf8'));console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/src/lib/secure.ts contains "expo-secure-store" import, "soldi-onboarding" in the SecureKey union, no "AsyncStorage" string, and is at least 25 lines long
    - apps/mobile/src/lib/http.ts contains "class HttpError" and "AbortController" and "[redacted]" and is at least 45 lines long
    - apps/mobile/src/lib/time.ts contains "export function nowSeconds" and "export function startOfDaySeconds" and is at least 30 lines long
    - apps/mobile/src/lib/i18n/en.json validates as JSON and contains key path onboarding.welcome_title
    - apps/mobile/src/lib/i18n/uk.json validates as JSON and contains key path onboarding.welcome_title with value "Гроші, повільніше."
    - apps/mobile/src/lib/i18n/index.ts contains "createInstance" and "Localization.getLocales" and is at least 25 lines long
    - cd apps/mobile && npx tsc --noEmit exits 0
    - cd apps/mobile && grep -RIl "AsyncStorage" src/lib/ returns no files
  </acceptance_criteria>
  <done>i18n + secure-store + http + time helpers exist, all type-check, no AsyncStorage.</done>
</task>

<task type="auto">
  <name>Task 3: Wire store, QueryClient, root layout, boot redirector, onboarding stack + Welcome screen</name>
  <files>
    apps/mobile/src/stores/onboarding.ts,
    apps/mobile/src/api/queryClient.ts,
    apps/mobile/src/components/PressableButton.tsx,
    apps/mobile/app/_layout.tsx,
    apps/mobile/app/index.tsx,
    apps/mobile/app/onboarding/_layout.tsx,
    apps/mobile/app/onboarding/welcome.tsx,
    apps/mobile/app/onboarding/data-source.tsx
  </files>
  <read_first>
    - /home/iskan/projects/soldi/CLAUDE.md
    - /home/iskan/projects/soldi/.planning/phases/01-onboarding-data-ingest/01-SKELETON.md
    - /home/iskan/projects/soldi/apps/mobile/app/_layout.tsx
    - /home/iskan/projects/soldi/apps/mobile/src/design/tokens.ts
    - /home/iskan/projects/soldi/apps/mobile/src/design/typography.ts
    - /home/iskan/projects/soldi/apps/mobile/src/lib/secure.ts
    - /home/iskan/projects/soldi/apps/mobile/src/lib/i18n/index.ts
    - /home/iskan/projects/soldi/apps/mobile/src/lib/db/index.ts
  </read_first>
  <action>
    Decision: persist the Zustand onboarding store via expo-secure-store ONLY. No file-system dependency, no separate prefs file. The whole serialized state lives under SecureKey 'soldi-onboarding'. (rev 1: WARNING W4 — earlier draft considered expo-file-system; that path is dropped entirely.)

    Create apps/mobile/src/stores/onboarding.ts:
    - import create from 'zustand'
    - import the named exports persist, createJSONStorage, and the type StateStorage from 'zustand/middleware'
    - import the three secure helpers from '@lib/secure'
    - Define a local type alias DataSource as the literal union 'synthetic' | 'manual' | 'monobank' | 'csv'
    - Define an OnboardingState type with fields: language ('en'|'uk'|null), dataSource (DataSource|null), completed (boolean), and the four setter methods setLanguage, setDataSource, setCompleted, reset.
    - Define a const secureStorage of type StateStorage whose three methods delegate to secureGet, secureSet, secureDelete respectively (cast to any to satisfy the SecureKey union since persist uses the configured `name` field).
    - Export useOnboardingStore via create wrapped in persist; the persist config: name 'soldi-onboarding', storage createJSONStorage(()=>secureStorage). Initial state values null/null/false. The reset action resets all three.

    Create apps/mobile/src/api/queryClient.ts:
    - import { QueryClient } from '@tanstack/react-query'
    - export const queryClient = new QueryClient with defaultOptions.queries: retry 1, staleTime 60_000, refetchOnWindowFocus false

    Create apps/mobile/src/components/PressableButton.tsx — a reusable Pressable that:
    - Props: label:string, onPress:()=>void, variant?: 'primary'|'secondary', accessibilityLabel?:string, testID?:string, disabled?:boolean
    - Uses TYPE.uiButton, COLORS.accent (primary bg) / COLORS.surface (secondary bg), RADIUS.lg, SPACING.md padding, min height 44 (44pt tap target)
    - accessibilityRole 'button', accessibilityLabel falls back to label
    - Uses StyleSheet.create; no inline hex.

    Replace apps/mobile/app/_layout.tsx with a new version that:
    - Keeps the three useFonts hooks exactly as they are
    - Adds these top-level imports: QueryClientProvider from '@tanstack/react-query'; queryClient from '@api/queryClient'; initI18n and i18n from '@lib/i18n'; getDB and runMigrations from '@lib/db'; I18nextProvider from 'react-i18next'
    - Adds state: const i18nReady boolean defaulting to false; const dbReady boolean defaulting to false
    - In useEffect on mount: define an inner async IIFE that awaits initI18n(), then setI18nReady(true), then in a try block calls runMigrations(getDB()) and setDbReady(true); on catch, calls console.warn with the message string "migration failed" plus the caught error's .name (no SQL fragment, no values, no token bytes ever) and still calls setDbReady(true) to unblock the UI.
    - Replaces `if (!fontsLoaded) return null` with `if (!fontsLoaded || !i18nReady || !dbReady) return null;`
    - Wraps the existing Stack inside QueryClientProvider with the queryClient prop, which contains I18nextProvider with the i18n prop, which contains the Stack.
    - Adds Stack.Screen entries: one for name="index" with headerShown:false AND one for name="onboarding" with headerShown:false. Keep existing (tabs) and modal screens.
    - DELETE the obsolete `export const unstable_settings = { anchor: '(tabs)' }` from app/_layout.tsx (Index now controls landing).

    Create apps/mobile/app/index.tsx (boot redirector):
    - import { Redirect } from 'expo-router'
    - import { useOnboardingStore } from '@stores/onboarding'
    - export default function Index() — read `completed` from store; if true return Redirect to "/(tabs)", else Redirect to "/onboarding/welcome".

    Create apps/mobile/app/onboarding/_layout.tsx:
    - import { Stack } from 'expo-router'
    - export default function OnboardingLayout — returns Stack with screenOptions: headerShown false, contentStyle backgroundColor COLORS.background, animation 'slide_from_right'

    Create apps/mobile/app/onboarding/welcome.tsx:
    - Uses View, Text, Pressable, SafeAreaView from react-native. Plain COLORS.background (no LinearGradient — expo-linear-gradient not confirmed in package.json).
    - On mount: Animated.View entrance via react-native-reanimated v4: useSharedValue(0) → withTiming(1, {duration: 600}) drives opacity + translateY 24 → 0. Use useAnimatedStyle.
    - Layout: title with TYPE.displayL color COLORS.textPrimary using i18next key 'onboarding.welcome_title'. Subtitle with TYPE.editorialLead, color COLORS.textSecondary, key 'onboarding.welcome_subtitle'.
    - Two large Pressable tiles vertically stacked (gap SPACING.md), one labelled t('onboarding.language_en') + accessibilityLabel "English", accessibilityRole 'radio'; other t('onboarding.language_uk') + accessibilityLabel "Ukrainian / Українська", accessibilityRole 'radio'. Tile container: COLORS.surface, RADIUS.lg, SPACING.lg padding, SHADOWS.card, min height 64.
    - On press: call useOnboardingStore.getState().setLanguage(lng) + await setLanguage(lng) (from @lib/i18n; import as changeI18nLanguage to avoid name clash with the store action) + router.push('/onboarding/data-source'). Use useRouter from 'expo-router'.

    Create apps/mobile/app/onboarding/data-source.tsx (stub):
    - SafeAreaView + ScrollView wrapper using COLORS.background
    - Render a Text with style TYPE.displayM showing t('onboarding.pick_source_title')
    - Render a Text with style TYPE.editorialBody showing t('onboarding.pick_source_subtitle')
    - Below: a single visible Text reading "Coming next in plan 01-02" using TYPE.uiMeta + COLORS.textMuted. This stub is replaced in 01-02.

    Final checks: every Pressable has accessibilityRole + accessibilityLabel. All colors come from COLORS / GRADIENTS. No inline hex. No inline style with hex colors.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
    <automated>cd apps/mobile && npx expo lint</automated>
    <automated>cd apps/mobile && grep -RInE "#[0-9A-Fa-f]{3,6}" app/onboarding src/stores src/api src/components 2>/dev/null | grep -v node_modules | (read line && { echo "Inline hex found"; exit 1; } || exit 0)</automated>
    <automated>cd apps/mobile && grep -RIn "AsyncStorage" src app 2>/dev/null | grep -v node_modules | (read line && { echo "AsyncStorage forbidden"; exit 1; } || exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/app/index.tsx contains "Redirect" and "useOnboardingStore" and is at least 15 lines long
    - apps/mobile/app/onboarding/welcome.tsx contains both "accessibilityRole" and ("useSharedValue" OR "withTiming") and is at least 60 lines long
    - apps/mobile/app/onboarding/welcome.tsx contains exactly two t('onboarding.language_en') and t('onboarding.language_uk') call sites
    - apps/mobile/app/_layout.tsx contains "QueryClientProvider", "I18nextProvider", "runMigrations", "initI18n"
    - apps/mobile/app/_layout.tsx does NOT contain `unstable_settings`
    - apps/mobile/src/stores/onboarding.ts contains "persist(" and "secureGet" and is at least 35 lines long
    - apps/mobile/src/components/PressableButton.tsx contains "minHeight: 44" or "minHeight:44" and is at least 40 lines long
    - cd apps/mobile && npx tsc --noEmit exits 0
    - cd apps/mobile && npx expo lint exits 0
    - No inline hex colors in any file under app/onboarding/, src/stores/, src/api/, src/components/ (banned-hex gate)
    - No AsyncStorage references anywhere in apps/mobile/src or apps/mobile/app (banned-storage gate)
  </acceptance_criteria>
  <done>Boot redirector + welcome with language pick + data-source stub all type-check and lint clean. DB migration runs on cold start (smoke-verified via db-migration.test.ts in Task 2a). AsyncStorage not present anywhere. No inline hex colors in new files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user device storage → app | onboarding store + (future) monobank token live in expo-secure-store |
| device → i18n resource files | bundled at build time; no runtime fetch |
| app → SQLite | local file; no network; full trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Information Disclosure | onboarding store persisted state | mitigate | Persisted via expo-secure-store under SecureKey 'soldi-onboarding'. Type union enforces no other keys. |
| T-01-01-02 | Information Disclosure | console.warn during migration failure | mitigate | log message contains only "migration failed" + error.name; no SQL fragment, no values, no token bytes |
| T-01-01-03 | Tampering | SQLite DB file on rooted device | accept | Phase 1 scope; encryption-at-rest deferred per SKELETON; no banking creds yet stored in DB |
| T-01-01-04 | Spoofing | i18n key collision injecting markup | mitigate | Translation copy is static JSON; no user-supplied interpolation values yet (welcome screen renders only) |
| T-01-01-05 | Denial of Service | migration loop never terminates | mitigate | MIGRATIONS is a finite ReadonlyArray; loop iterates by index; BEGIN/COMMIT/ROLLBACK around each |
| T-01-01-06 | Elevation of Privilege | unauthorized SecureKey written | mitigate | TypeScript SecureKey literal union; secureSet typed param disallows arbitrary strings |
</threat_model>

<verification>
1. cd apps/mobile && npx tsc --noEmit exits 0
2. cd apps/mobile && npx expo lint exits 0
3. cd apps/mobile && npx tsx tests/money.test.ts exits 0
4. cd apps/mobile && npx tsx tests/db-migration.test.ts exits 0 (or printed-skip with reason)
5. grep -RIn "AsyncStorage" apps/mobile/src apps/mobile/app returns no hits
6. grep -RInE "#[0-9A-Fa-f]{3,6}" apps/mobile/app/onboarding apps/mobile/src/stores apps/mobile/src/api apps/mobile/src/components | grep -v node_modules returns no hits (banned-hex hygiene)
7. Manual: launching the app via `cd apps/mobile && npx expo start` and scanning QR shows welcome screen with EN/UK pick, animation runs, language pick navigates forward.
</verification>

<success_criteria>
- Walking Skeleton spine present: DB module, secure-store wrapper, money/time helpers, i18n init, persisted onboarding store, QueryClient, root layout providers, boot redirector, onboarding stack, Welcome screen with language pick, data-source stub.
- Requirement ONBD-01 satisfied (language pick UI exists, persists choice, retranslates app).
- Requirement ONBD-03 partially advanced: routing infra in place; final < 90s validation lives in 01-04 phase verification.
- All gates green: tsc, expo lint, money unit tests, db-migration unit test, no AsyncStorage, no inline hex in new files.
</success_criteria>

<output>
After completion, create `.planning/phases/01-onboarding-data-ingest/01-01-SUMMARY.md` per the SUMMARY template.
</output>
