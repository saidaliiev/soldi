---
phase: 01-onboarding-data-ingest
plan: 04
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
files_modified:
  - apps/mobile/src/api/monobank.ts
  - apps/mobile/src/lib/monobank/mapper.ts
  - apps/mobile/src/lib/monobank/mcc-table.ts
  - apps/mobile/src/lib/csv/parser.ts
  - apps/mobile/src/lib/csv/mappers.ts
  - apps/mobile/src/lib/i18n/en.json
  - apps/mobile/src/lib/i18n/uk.json
  - apps/mobile/src/data/merchantOverridesRepo.ts
  - apps/mobile/app/onboarding/monobank.tsx
  - apps/mobile/app/onboarding/csv.tsx
  - apps/mobile/app/onboarding/manual.tsx
  - apps/mobile/src/components/TextField.tsx
  - apps/mobile/tests/csv-parser.test.ts
  - apps/mobile/tests/monobank-mapper.test.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - ONBD-03
must_haves:
  truths:
    - "User pastes a monobank personal token; tokens are stored encrypted via expo-secure-store; UI never re-displays the raw token"
    - "On submit, the app calls api.monobank.ua /personal/client + /personal/statement; transactions appear in op-sqlite"
    - "User picks a CSV file via expo-document-picker; rows parse without crashing on malformed input; valid rows insert into op-sqlite"
    - "When CSV input contains zero importable rows (e.g. all categorySlug resolutions fail), the screen surfaces an error message and does NOT silently succeed"
    - "User adds a single transaction via a form (amount, merchant, date, category); the row appears in the dashboard"
    - "All three flows reach the dashboard within budget (timed end-to-end < 90 seconds for synthetic, monobank, csv, manual on a warm device)"
    - "monobank token is never logged; never appears in URL; only in the X-Token header"
    - "monobank Phase 1 syncs the primary account only (client.accounts[0]); multi-account picker is deferred per 01-SKELETON.md Out of Scope"
  artifacts:
    - path: "apps/mobile/src/api/monobank.ts"
      provides: "Typed monobank client (getClient, getStatement) using httpJson"
      exports: ["getMonobankClient", "getMonobankStatement", "MonobankClient", "MonobankStatementItem"]
      min_lines: 60
    - path: "apps/mobile/src/lib/monobank/mapper.ts"
      provides: "Pure function: MonobankStatementItem[] mapped to MappedRow[] ready for insertManyTransactions"
      exports: ["mapMonobankItems", "MappedRow"]
      min_lines: 45
    - path: "apps/mobile/src/lib/monobank/mcc-table.ts"
      provides: "MCC subset specific to monobank, re-exporting categoryForMcc from synthetic/mcc when overlapping"
      exports: ["resolveMcc"]
      min_lines: 20
    - path: "apps/mobile/src/lib/csv/parser.ts"
      provides: "Streaming-safe CSV → row[] parser with 5MB cap and quoted-field support"
      exports: ["parseCsv", "CsvParseError"]
      min_lines: 60
    - path: "apps/mobile/src/lib/csv/mappers.ts"
      provides: "Column-heuristic detection + row mapping to Transaction inserts; every output row carries a defined categorySlug"
      exports: ["detectColumns", "csvRowsToTransactions", "djb2", "MappedCsvRow", "ColumnMap"]
      min_lines: 80
    - path: "apps/mobile/src/data/merchantOverridesRepo.ts"
      provides: "Insert / list overrides (Phase 3 will consume; Phase 1 seeds defaults on first manual category pick)"
      exports: ["addMerchantOverride", "listMerchantOverrides", "findOverrideForMerchant"]
      min_lines: 35
    - path: "apps/mobile/app/onboarding/monobank.tsx"
      provides: "Token paste UI with sync flow, progress, error states"
      exports: ["default"]
      min_lines: 110
    - path: "apps/mobile/app/onboarding/csv.tsx"
      provides: "Document picker + preview row count + import button + zero-row defensive check"
      exports: ["default"]
      min_lines: 100
    - path: "apps/mobile/app/onboarding/manual.tsx"
      provides: "Single-entry form: amount, merchant, date, category, account; inserts one row"
      exports: ["default"]
      min_lines: 90
    - path: "apps/mobile/src/components/TextField.tsx"
      provides: "Token / amount / merchant-name input with secureTextEntry, multiline, a11y"
      exports: ["TextField"]
      min_lines: 45
  key_links:
    - from: "app/onboarding/monobank.tsx"
      to: "src/api/monobank.ts::getMonobankStatement"
      via: "TanStack Query mutation; token from secure-store; never in cache key"
      pattern: "getMonobankStatement"
    - from: "src/api/monobank.ts"
      to: "src/lib/http.ts::httpJson"
      via: "GET with X-Token header; errors strip the value"
      pattern: "X-Token"
    - from: "app/onboarding/csv.tsx"
      to: "src/lib/csv/parser.ts::parseCsv"
      via: "expo-document-picker → read file → parseCsv → csvRowsToTransactions → insertManyTransactions"
      pattern: "parseCsv\\("
    - from: "app/onboarding/csv.tsx"
      to: "src/lib/csv/mappers.ts::MappedCsvRow.categorySlug → src/data/categoriesRepo.ts::getCategoryIdBySlug"
      via: "Every MappedCsvRow carries a defined categorySlug (default 'misc'); csv.tsx maps slug to category_id via getCategoryIdBySlug"
      pattern: "getCategoryIdBySlug"
    - from: "app/onboarding/manual.tsx"
      to: "src/data/transactionsRepo.ts::insertManyTransactions"
      via: "Form submit creates single-element array → insertManyTransactions"
      pattern: "insertManyTransactions"
---

<objective>
Three remaining ingest paths in one plan: monobank, CSV, manual. Vertical slices each. All paths converge on the same insertManyTransactions repo from 01-03, so the dashboard "just works" once any flow completes.

Purpose: Closes DATA-01, DATA-02, DATA-03 + finishes ONBD-03 (the 90s timing assertion across all four flows). Phase 1's value is now complete.
Output: A user picking monobank/CSV/manual lands on a populated dashboard exactly like the synthetic flow does.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-onboarding-data-ingest/01-SKELETON.md
@.planning/phases/01-onboarding-data-ingest/01-01-PLAN.md
@.planning/phases/01-onboarding-data-ingest/01-02-PLAN.md
@.planning/phases/01-onboarding-data-ingest/01-03-PLAN.md
@CLAUDE.md
@apps/mobile/src/design/tokens.ts

<interfaces>
<!-- From 01-01 -->
From src/lib/http.ts:
- httpJson<T>(url, opts): Promise<T> — strips X-Token from error messages
- class HttpError extends Error { status: number; bodyText: string }

From src/lib/secure.ts:
- secureGet('monobank_token'): Promise<string|null>
- secureSet('monobank_token', value): Promise<void>
- secureDelete('monobank_token'): Promise<void>

From src/lib/money.ts:
- toCents, fromCents, parseAmount

<!-- From 01-03 -->
From src/data/transactionsRepo.ts:
- insertManyTransactions(rows) returns { inserted, skipped }

From src/data/categoriesRepo.ts:
- getCategoryIdBySlug(slug) returns number|null
- listCategories() returns CategoryRow[]

From src/data/accountsRepo.ts:
- ensureDefaultAccount(source) returns number

From src/lib/synthetic/mcc.ts:
- categoryForMcc(mcc, fallback='misc') returns CategorySlug

Monobank Personal API (https://api.monobank.ua/docs/):
- GET /personal/client — returns clientId, name, webHookUrl, permissions, accounts (array of { id, sendId, currencyCode, cashbackType, balance, creditLimit, maskedPan, type, iban }); X-Token header required; rate-limited to 1 req per 60s per token (account-level: per-statement).
- GET /personal/statement/{account}/{from}/{to} — returns Array of items { id, time, description, mcc, originalMcc, amount, operationAmount, currencyCode, commissionRate, cashbackAmount, balance, hold, receiptId? }. Amount is in minor units, signed (negative = expense). currencyCode is ISO 4217 numeric (980 = UAH, 978 = EUR). Time is unix seconds. Max range 31 days 1 sec; rate-limit 1/min/account.
- Auth: header 'X-Token: <token>'. Token shape: 44 url-safe base64 chars (uXXX...). Permissions field: 'sp' for statements.

From expo-document-picker:
- getDocumentAsync({ type, copyToCacheDirectory, multiple }) returns either { canceled: false, assets: [{ uri, name, size, mimeType }] } or { canceled: true }

From expo-file-system (not currently in package.json; for Phase 1 CSV import we'll use the `await fetch(uri).then(r => r.text())` trick on iOS file:// URIs — works for document-picker uris on iOS; cap size before fetch via the asset.size field).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build pure adapters — monobank client + mapper + CSV parser + mappers + Node unit tests</name>
  <files>
    apps/mobile/src/api/monobank.ts,
    apps/mobile/src/lib/monobank/mapper.ts,
    apps/mobile/src/lib/monobank/mcc-table.ts,
    apps/mobile/src/lib/csv/parser.ts,
    apps/mobile/src/lib/csv/mappers.ts,
    apps/mobile/tests/csv-parser.test.ts,
    apps/mobile/tests/monobank-mapper.test.ts
  </files>
  <read_first>
    - /home/iskan/projects/soldi/CLAUDE.md
    - /home/iskan/projects/soldi/.planning/phases/01-onboarding-data-ingest/01-SKELETON.md
    - /home/iskan/projects/soldi/apps/mobile/src/lib/http.ts
    - /home/iskan/projects/soldi/apps/mobile/src/lib/synthetic/mcc.ts
    - /home/iskan/projects/soldi/apps/mobile/src/lib/money.ts
  </read_first>
  <action>
    Create apps/mobile/src/api/monobank.ts:
    - export type MonobankCurrencyCode = number (ISO 4217 numeric)
    - export type MonobankAccount = object with fields: id (string), sendId (string), currencyCode (MonobankCurrencyCode), cashbackType (string), balance (number), creditLimit (number), maskedPan (string[]), type (string), iban (string)
    - export type MonobankClient = object with: clientId (string), name (string), webHookUrl? (string), permissions (string), accounts (MonobankAccount[])
    - export type MonobankStatementItem = object with: id, time, description, mcc, originalMcc, amount, operationAmount, currencyCode, commissionRate, cashbackAmount, balance, hold, receiptId? — types match the API doc above.
    - export async function getMonobankClient(token: string): Promise<MonobankClient> — calls httpJson against 'https://api.monobank.ua/personal/client' with headers { 'X-Token': token }, method 'GET', timeoutMs 10000. On 401 rethrows Error 'Invalid monobank token'. On 403 'Permission denied (need sp scope)'. On 429 'Rate limited; try again in 60s'.
    - export async function getMonobankStatement(token: string, accountId: string, fromSeconds: number, toSeconds: number): Promise<MonobankStatementItem[]> — same pattern. Note 31-day window cap; caller chunks. Returns items array (API returns the array directly).
    - Implementation requirement: token MUST appear ONLY in the headers object. Putting the token into the URL via template literal interpolation is forbidden — if the executor reaches for that they have implemented it wrong.

    Create apps/mobile/src/lib/monobank/mcc-table.ts:
    - import categoryForMcc from '@lib/synthetic/mcc'
    - export function resolveMcc(mcc: number, originalMcc?: number) returns an object { slug: string; mcc: number } — try mcc first; if categoryForMcc returns 'misc' AND originalMcc is defined and differs, retry with originalMcc; return whichever non-misc, else { slug:'misc', mcc }.

    Create apps/mobile/src/lib/monobank/mapper.ts:
    - import MonobankStatementItem from '@api/monobank'
    - import resolveMcc from './mcc-table'
    - export type MappedRow: object with amount_cents (number), currency (string: 'UAH'|'EUR'|'USD' or fallback number-string), merchant_name (string), merchant_id (string|null), mcc_code (number|null), categorySlug (string — always defined; never undefined), description (null), date (number), source ('monobank'), external_id (string), created_at (number)
    - export function mapMonobankItems(items: MonobankStatementItem[], nowSec: number): MappedRow[]:
      - Convert currencyCode: 980 → 'UAH', 978 → 'EUR', 840 → 'USD', other → String(currencyCode)
      - amount_cents = item.amount (already minor units, signed; monobank uses negative for expense — matches our convention)
      - merchant_name = item.description (trimmed; never include the literal X-Token; truncate at 120 chars; replace runs of whitespace with single space)
      - merchant_id = item.id
      - mcc_code = item.mcc
      - categorySlug = resolveMcc(item.mcc, item.originalMcc).slug
      - date = item.time
      - external_id = "monobank-" + item.id (UNIQUE constraint dedupes re-syncs)
      - description = null (Phase 1 rule: never store free-text descriptions in DB for AI safety — only merchant name + mcc + amount; Phase 3 will revisit but already future-proofed)
      - created_at = nowSec
    - export function isPotentiallyOversizedBatch(items: MonobankStatementItem[]): boolean — true if items.length > 1000 (monobank statement limit per 31-day window; flag for UI to inform the user).

    Create apps/mobile/src/lib/csv/parser.ts:
    - export class CsvParseError extends Error with public field `code` of type 'too-large' | 'malformed' | 'too-many-rows', passed via constructor and saved on the instance.
    - export function parseCsv(text: string, opts?: { maxBytes?: number; maxRows?: number; delimiter?: ',' | ';' | '\t' }): string[][]:
      - Reject when Buffer.byteLength(text,'utf8') > opts.maxBytes (default 5_000_000) — throw CsvParseError with code 'too-large'
      - Auto-detect delimiter if not provided: count occurrences of ',' ';' '\t' on the first non-empty line; pick the highest
      - Walk the string char-by-char with a small state machine: handles quoted fields (RFC 4180), escaped double-quotes (""), CRLF and LF line endings, and a trailing newline.
      - Stop and throw 'too-many-rows' if rows exceed opts.maxRows (default 200_000).
      - Throw 'malformed' on unbalanced quotes at EOF.
      - Return string[][] including the header row at index 0.

    Create apps/mobile/src/lib/csv/mappers.ts:
    - import { toCents, parseAmount } from '@lib/money'
    - import { categoryForMcc } from '@lib/synthetic/mcc'
    - export type ColumnMap = object with: dateCol (number), amountCol (number), merchantCol (number), currencyCol (number|null), descriptionCol (number|null), mccCol (number|null)
    - export function detectColumns(header: string[]): ColumnMap | null:
      - Lowercase + trim each cell.
      - dateCol matches one of: 'date','time','datetime','transaction date','операція','дата'
      - amountCol matches: 'amount','sum','total','debit','credit','amount (eur)','operation amount','сума','сумма'
      - merchantCol matches: 'merchant','description','memo','name','payee','контрагент','опис'
      - currencyCol optional: 'currency','валюта'
      - mccCol optional: 'mcc','mcc code'
      - descriptionCol optional: 'note','notes'
      - Return null if any required is unmatched. Phase 1 returns null gracefully — the screen surfaces "couldn't find columns" per SKELETON Out-of-Scope.
    - export type MappedCsvRow = object with the SAME shape as monobank's MappedRow EXCEPT source is the literal 'csv'. The categorySlug field is REQUIRED and ALWAYS defined (never undefined). (rev 1: BLOCKER B2 — contract closure.)
    - export function djb2(s: string): string — local djb2 hash returning lowercase hex.
    - export function csvRowsToTransactions(rows: string[][], cmap: ColumnMap, accountId: number, defaultCurrency: 'EUR'|'UAH'|'USD'): MappedCsvRow[]:
      - Skip header (rows.slice(1))
      - For each: parse amount via parseAmount; reject rows where amount is null or merchant empty.
      - Date: try parsing ISO 8601 first; then 'dd/mm/yyyy', 'mm/dd/yyyy', 'yyyy-mm-dd hh:mm'. If unparseable → skip row.
      - currency = cmap.currencyCol != null ? row[cmap.currencyCol] : defaultCurrency. Coerce to uppercase + slice(0,3).
      - mcc = cmap.mccCol != null ? parseInt(row[cmap.mccCol], 10) || null : null
      - amount_cents = toCents(amountFloat); sign convention: if value starts with '+' or is positive number, store positive (income); negative or starts with '-' → negative.
      - merchant_name trimmed to 120 chars
      - **categorySlug = categoryForMcc(mcc ?? 0, 'misc')** — ALWAYS a string. If mcc is null OR doesn't map, categorySlug equals 'misc'. (rev 1: BLOCKER B2 — guarantees the field is never undefined.)
      - external_id = "csv-" + djb2(row[cmap.dateCol] + '|' + row[cmap.amountCol] + '|' + row[cmap.merchantCol])
      - created_at = current unix seconds at call time
      - return MappedCsvRow shape ready for insertManyTransactions

    Create apps/mobile/tests/monobank-mapper.test.ts using node:test:
    - Input: fixture array of 5 MonobankStatementItem objects with varying mcc codes (5411, 5814, 5541, 4814, 99999); time within last 7 days; amounts mixed positive/negative; currencyCode 980/978
    - Assertions:
      - mapMonobankItems returns 5 MappedRow
      - All currency strings are 'UAH' | 'EUR' | 'USD' | number-string
      - mcc 5411 maps to categorySlug 'groceries' (not 'misc')
      - mcc 99999 maps to categorySlug 'misc'
      - external_id values are unique and start with 'monobank-'
      - description field is null for every row
      - amount_cents preserves sign
      - merchant_name is truncated to 120 chars max (use a test fixture with a 200-char description)
      - merchant_name with embedded newlines collapses whitespace

    Create apps/mobile/tests/csv-parser.test.ts:
    - Test 1: simple comma CSV with header → returns expected rows
    - Test 2: semicolon-delimited (auto-detected) → same row count as comma version
    - Test 3: quoted field containing comma → not split
    - Test 4: doubled double-quote inside quoted field → unescaped correctly
    - Test 5: CRLF line endings → same result as LF
    - Test 6: input > 5MB synth string → throws CsvParseError with code 'too-large'
    - Test 7: unbalanced quote → throws code 'malformed'
    - Test 8: detectColumns finds date/amount/merchant in headers ['Date','Amount','Description']
    - Test 9: detectColumns returns null when amount column is missing
    - Test 10: csvRowsToTransactions skips rows with unparseable amount; valid rows produce external_id starting with 'csv-'; sign convention upheld (parseAmount('-12.34') → negative cents).
    - **Test 11 (rev 1: BLOCKER B2 contract test): csvRowsToTransactions over a fixture WITHOUT an MCC column (cmap.mccCol === null) yields every output row with categorySlug === 'misc' (never undefined). Assert that every row's typeof categorySlug equals 'string' AND categorySlug equals 'misc'.**

    Add @types/node devDependency check: if not present in package.json, add @types/node ^22 to devDependencies. tsc needs it for the test file's node:test import resolution. If addition causes tsc trouble, revert and skip the test from CI for this plan (executor decision; record in SUMMARY).
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
    <automated>cd apps/mobile && npx tsx tests/csv-parser.test.ts</automated>
    <automated>cd apps/mobile && npx tsx tests/monobank-mapper.test.ts</automated>
    <automated>cd apps/mobile && grep -E "console\.(log|info|warn|error)" src/api/monobank.ts src/lib/monobank/ 2>/dev/null | grep -v "^#" | grep -v "// " | (read line && { echo "logging in monobank path forbidden"; exit 1; } || exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/src/api/monobank.ts exports getMonobankClient and getMonobankStatement (grep returns both)
    - apps/mobile/src/api/monobank.ts contains the literal string 'X-Token' exactly inside the headers object (grep "'X-Token'"), and grep "personal/client" returns 1+, and grep "personal/statement" returns 1+
    - apps/mobile/src/api/monobank.ts does NOT include the token inside any URL template literal — verified by `grep -E '\\$\\{token\\}.*api\\.monobank' apps/mobile/src/api/monobank.ts` returning no matches
    - apps/mobile/src/lib/csv/parser.ts exports parseCsv and CsvParseError
    - apps/mobile/src/lib/csv/mappers.ts exports detectColumns, csvRowsToTransactions, djb2, and MappedCsvRow
    - apps/mobile/src/lib/csv/mappers.ts source contains the literal "categorySlug: categoryForMcc(" (B2 contract — slug is computed, never left undefined)
    - apps/mobile/src/lib/monobank/mapper.ts exports mapMonobankItems
    - cd apps/mobile && npx tsx tests/csv-parser.test.ts exits 0 AND the test file source contains a test name or assert message mentioning the word "categorySlug" (B2 contract test present)
    - cd apps/mobile && npx tsx tests/monobank-mapper.test.ts exits 0
    - cd apps/mobile && npx tsc --noEmit exits 0
    - No console.log/info/warn/error calls in src/api/monobank.ts or src/lib/monobank/
  </acceptance_criteria>
  <done>Pure adapters + parser + mappers + tests pass. monobank path has no logging, no token-in-URL. MappedCsvRow.categorySlug is always defined.</done>
</task>

<task type="auto">
  <name>Task 2: Build TextField + manual entry form + merchant overrides repo</name>
  <files>
    apps/mobile/src/components/TextField.tsx,
    apps/mobile/src/data/merchantOverridesRepo.ts,
    apps/mobile/app/onboarding/manual.tsx,
    apps/mobile/src/lib/i18n/en.json,
    apps/mobile/src/lib/i18n/uk.json
  </files>
  <read_first>
    - /home/iskan/projects/soldi/CLAUDE.md
    - /home/iskan/projects/soldi/.planning/phases/01-onboarding-data-ingest/01-SKELETON.md
    - /home/iskan/projects/soldi/apps/mobile/src/design/tokens.ts
    - /home/iskan/projects/soldi/apps/mobile/src/design/typography.ts
    - /home/iskan/projects/soldi/apps/mobile/src/data/transactionsRepo.ts
    - /home/iskan/projects/soldi/apps/mobile/src/data/categoriesRepo.ts
    - /home/iskan/projects/soldi/apps/mobile/src/data/accountsRepo.ts
  </read_first>
  <action>
    Create apps/mobile/src/components/TextField.tsx:
    - Props: label (string), value (string), onChangeText ((s:string) => void), placeholder? (string), secureTextEntry? (boolean), multiline? (boolean), keyboardType? ('default'|'decimal-pad'|'email-address'|'numeric'), autoCapitalize? ('none'|'sentences'|'words'|'characters'), accessibilityLabel? (string), testID? (string), error? (string | null)
    - Layout: View column. Text label TYPE.uiLabel color COLORS.textSecondary marginBottom SPACING.xs. TextInput backgroundColor COLORS.surface RADIUS.md padding SPACING.md minHeight 44, multiline → minHeight 88 + textAlignVertical 'top'. Error: Text TYPE.uiMeta color COLORS.error below input when error truthy.
    - accessibilityLabel defaults to label. Use returnKeyType 'done'.
    - Use StyleSheet.create. No inline hex.

    Create apps/mobile/src/data/merchantOverridesRepo.ts:
    - export type MerchantOverrideRow: object with id, merchant_pattern, category_id, confidence, created_by_user (0|1), created_at
    - export function addMerchantOverride(pattern: string, categoryId: number, createdByUser: boolean): number — INSERT and return insertId. Lower-case the pattern + trim. Use parameterized SQL.
    - export function listMerchantOverrides(): MerchantOverrideRow[]
    - export function findOverrideForMerchant(merchantName: string): MerchantOverrideRow | null — SELECT row FROM merchant_overrides WHERE LOWER(?) LIKE '%' || merchant_pattern || '%' ORDER BY confidence DESC LIMIT 1; param = merchantName.

    Replace apps/mobile/app/onboarding/manual.tsx with the real form:
    - Form fields: amount (decimal-pad), merchant (default), date (defaults to today; show as YYYY-MM-DD plain text via two-tap quick-pick row of [Today | Yesterday | 2 days ago]; full picker deferred), category (Pressable opening a modal list of categories from listCategories()), and a hidden 'manual' source + auto account.
    - State: local useState for each field. categoryId stored as number|null.
    - Validation: amount must parse via parseAmount and not be 0; merchant non-empty after trim; categoryId non-null. Disable submit button until valid.
    - Submit: compute sign — manual entries default to expense (negative), unless amount string starts with '+' which signals income. Compose one row matching the insertManyTransactions shape with fields amount_cents, currency 'EUR', merchant_name (trimmed merchant), merchant_id null, mcc_code null, category_id, account_id from ensureDefaultAccount('manual'), description null, date set to chosen-day startOfDay seconds plus 12 hours, source 'manual', external_id formed as "manual-" + Date.now() + "-" + djb2(merchant+amount), created_at nowSeconds(). After insertion: store.setDataSource('manual'); store.setCompleted(true); router.replace('/(tabs)').
    - Add i18n keys to en.json + uk.json: onboarding.manual_amount ("Amount"/"Сума"), onboarding.manual_merchant ("Where"/"Де"), onboarding.manual_date ("Date"/"Дата"), onboarding.manual_category ("Category"/"Категорія"), onboarding.manual_today ("Today"/"Сьогодні"), onboarding.manual_yesterday ("Yesterday"/"Вчора"), onboarding.manual_2_days_ago ("2 days ago"/"2 дні тому"), onboarding.manual_submit ("Add transaction"/"Додати транзакцію"), common.invalid_amount ("Enter a valid amount"/"Введи коректну суму"), common.required ("Required"/"Обов'язково").
    - On successful insert, also persist a merchant override automatically: addMerchantOverride(merchant.toLowerCase(), categoryId, true) — so future ingest paths can suggest the same category. Wrap in try/catch; failure to add the override is silent (not blocking).

    Reuse PressableButton from 01-01 for the submit button. SHADOWS.card on inner form card. All Pressables a11y-labelled.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
    <automated>cd apps/mobile && npx expo lint</automated>
    <automated>cd apps/mobile && grep -nE "#[0-9A-Fa-f]{3,6}" src/components/TextField.tsx app/onboarding/manual.tsx | (read line && { echo "Inline hex"; exit 1; } || exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/src/components/TextField.tsx contains "accessibilityRole" or "accessibilityLabel" and "minHeight: 44" and is at least 45 lines long
    - apps/mobile/src/data/merchantOverridesRepo.ts contains "addMerchantOverride" "listMerchantOverrides" "findOverrideForMerchant"
    - apps/mobile/app/onboarding/manual.tsx contains "insertManyTransactions", "parseAmount", "ensureDefaultAccount('manual')", "addMerchantOverride"
    - apps/mobile/app/onboarding/manual.tsx grep "setCompleted(true)" returns 1+
    - apps/mobile/src/lib/i18n/en.json contains key "onboarding.manual_submit" with value "Add transaction"
    - apps/mobile/src/lib/i18n/uk.json contains key "onboarding.manual_submit" with value "Додати транзакцію"
    - cd apps/mobile && npx tsc --noEmit exits 0
    - cd apps/mobile && npx expo lint exits 0
  </acceptance_criteria>
  <done>Manual entry form fully functional, persists row + override, navigates to dashboard.</done>
</task>

<task type="auto">
  <name>Task 3: Build monobank token paste screen (primary account only) + CSV import screen (with progress, errors, retry, zero-row defensive check)</name>
  <files>
    apps/mobile/app/onboarding/monobank.tsx,
    apps/mobile/app/onboarding/csv.tsx,
    apps/mobile/src/lib/i18n/en.json,
    apps/mobile/src/lib/i18n/uk.json
  </files>
  <read_first>
    - /home/iskan/projects/soldi/CLAUDE.md
    - /home/iskan/projects/soldi/.planning/phases/01-onboarding-data-ingest/01-SKELETON.md
    - /home/iskan/projects/soldi/apps/mobile/src/api/monobank.ts
    - /home/iskan/projects/soldi/apps/mobile/src/lib/csv/parser.ts
    - /home/iskan/projects/soldi/apps/mobile/src/lib/csv/mappers.ts
    - /home/iskan/projects/soldi/apps/mobile/src/lib/secure.ts
    - /home/iskan/projects/soldi/apps/mobile/src/data/transactionsRepo.ts
    - /home/iskan/projects/soldi/apps/mobile/src/data/categoriesRepo.ts
  </read_first>
  <action>
    Replace apps/mobile/app/onboarding/monobank.tsx with the real flow:
    - SafeAreaView, ScrollView, COLORS.background.
    - State machine: 'paste' | 'validating' | 'fetching-client' | 'fetching-statement' | 'inserting' | 'done' | 'error'.
    - 'paste' UI: explainer Text (TYPE.editorialBody) — "Generate a personal token at https://api.monobank.ua/. We never store the token in plaintext or send it anywhere except monobank's API." Add i18n key onboarding.monobank_explainer with EN + UK copy.
    - One TextField labelled t('onboarding.monobank_token_label'), multiline true (tokens are 44 chars; multiline reduces clipping), autoCapitalize 'none', secureTextEntry true.
    - PressableButton "Connect" enabled when value.trim().length equals 44.
    - On submit:
      1. setPhase('validating'). Trim token. Reject early if length differs from 44 — show t('onboarding.monobank_invalid_token').
      2. setPhase('fetching-client'). Call getMonobankClient(token). On HttpError 401 → setError(t('onboarding.monobank_token_rejected')) and setPhase('error'). On 429 → setError(t('onboarding.monobank_rate_limited')). On network error → setError generic.
      3. **Pick client.accounts[0] (primary account). Multi-account picker deferred per 01-SKELETON.md Out of Scope.** (rev 1: BLOCKER B3 — explicit sanctioned scope reduction.) currency for that account: 980 → 'UAH', 978 → 'EUR'; pass through.
      4. setPhase('fetching-statement'). Compute fromSeconds = nowSeconds() - 31*86400 + 60, toSeconds = nowSeconds(). Call getMonobankStatement(token, accountId, fromSeconds, toSeconds). On 429 → setError rate limit message.
      5. setPhase('inserting'). dbAccount = ensureDefaultAccount('monobank'); mapped = mapMonobankItems(items, nowSeconds()); rows = mapped.map and set category_id = getCategoryIdBySlug(r.categorySlug) and account_id = dbAccount; filter rows where category_id is not null; call insertManyTransactions.
      6. await secureSet('monobank_token', token); useOnboardingStore.getState().setDataSource('monobank'); setCompleted(true); router.replace('/(tabs)').
    - Critical: NEVER call setToken('') on success path before secureSet; clear local state AFTER persistence to secure-store. Never log the token. Never include it in error message text rendered to UI (HttpError already strips).
    - Error UI: show message + PressableButton "Try again" → setPhase('paste').
    - Progress UI: a single Text TYPE.editorialBody showing phase-specific copy via i18n keys; add accessibilityLiveRegion 'polite'.
    - Add i18n keys to en.json + uk.json: onboarding.monobank_token_label ("Personal token"/"Особистий токен"), onboarding.monobank_explainer (multi-sentence above), onboarding.monobank_invalid_token ("That doesn't look like a 44-character monobank token."/"Це не схоже на 44-символьний токен monobank."), onboarding.monobank_token_rejected ("monobank rejected the token. Generate a new one."/"monobank відхилив токен. Створи новий."), onboarding.monobank_rate_limited ("monobank is rate-limiting us. Try again in a minute."/"monobank лімітує запити. Спробуй за хвилину."), onboarding.monobank_phase_validating ("Checking the token..."/"Перевіряємо токен..."), onboarding.monobank_phase_fetching_client ("Reading your accounts..."/"Завантажуємо рахунки..."), onboarding.monobank_phase_fetching_statement ("Loading 31 days of transactions..."/"Завантажуємо 31 день транзакцій..."), onboarding.monobank_phase_inserting ("Saving locally..."/"Зберігаємо локально..."), onboarding.monobank_phase_done ("All set."/"Готово.").

    Replace apps/mobile/app/onboarding/csv.tsx with the real flow:
    - State: 'idle' | 'picking' | 'previewing' | 'mapping-failed' | 'inserting' | 'done' | 'error'.
    - 'idle' UI: explainer Text + PressableButton t('onboarding.csv_pick'). Add i18n keys onboarding.csv_pick "Pick CSV file" / "Обрати CSV", onboarding.csv_explainer "Export your transactions from your bank's website as CSV. Max 5MB." / "Експортуй транзакції з сайту банку у форматі CSV. Максимум 5 МБ.", onboarding.csv_preview "{{rows}} rows detected. {{usable}} will be imported." / "{{rows}} рядків знайдено. {{usable}} буде імпортовано.", onboarding.csv_import "Import" / "Імпортувати", onboarding.csv_too_large "File is over 5MB. Try a smaller export." / "Файл більше 5 МБ. Спробуй менший експорт.", onboarding.csv_unparseable "Couldn't read this file. Please check the format." / "Не вдалося прочитати файл. Перевір формат.", onboarding.csv_mapping_failed "Couldn't find the date/amount/merchant columns. Manual mapping comes in a future update." / "Не знайшли колонки дати/суми/одержувача. Ручне співставлення з'явиться згодом.", **onboarding.csv_no_rows_imported "No transactions imported. Categories may be missing." / "Жодної транзакції не імпортовано. Можливо, бракує категорій."** (rev 1: BLOCKER B4 — required key).
    - On pick: import the named getDocumentAsync from 'expo-document-picker'. Call getDocumentAsync with the option object: type set to ['text/csv','text/comma-separated-values','application/vnd.ms-excel','text/plain'], copyToCacheDirectory true, multiple false. If canceled → setPhase('idle'). Else assert asset.size ≤ 5_000_000 → 'too-large'. Use globalThis.fetch on the asset.uri and read .text() for the file contents.
    - parseCsv(text, options object with maxBytes 5_000_000 and maxRows 200_000) — wrap in try/catch; CsvParseError code 'too-large' → t('onboarding.csv_too_large'), 'malformed' → t('onboarding.csv_unparseable'), 'too-many-rows' → t('onboarding.csv_unparseable').
    - detectColumns(rows[0]) — if null → setPhase('mapping-failed') with t('onboarding.csv_mapping_failed') + Retry button → 'idle'.
    - 'previewing' UI shows total rows + how many parse cleanly (call csvRowsToTransactions then count). PressableButton 'Import'.
    - On Import:
      1. setPhase('inserting')
      2. accountId = ensureDefaultAccount('csv')
      3. finalRows = csvRowsToTransactions(rows, columnMap, accountId, 'EUR').map and resolve category_id = getCategoryIdBySlug(r.categorySlug) — since 01-04 Task 1's B2 contract guarantees categorySlug is always defined (default 'misc'), the slug lookup will only fail if the categories table is missing the slug. Filter rows where category_id is null.
      4. result = await insertManyTransactions(finalRows)
      5. **Defensive zero-row check (rev 1: BLOCKER B4)**: if result.inserted equals 0 AND finalRows.length > 0, setPhase('error'); setErrMsg(t('onboarding.csv_no_rows_imported')); return (do NOT navigate to /(tabs)). Otherwise: store.setDataSource('csv'); setCompleted(true); router.replace('/(tabs)').
    - All errors render via Text + Retry; no raw stack to user.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
    <automated>cd apps/mobile && npx expo lint</automated>
    <automated>cd apps/mobile && grep -nE "console\\.(log|info|warn|error)" app/onboarding/monobank.tsx | (read line && { echo "logging in monobank screen forbidden"; exit 1; } || exit 0)</automated>
    <automated>cd apps/mobile && grep -nE "X-Token.*\\\$\\{|console.*token" app/onboarding/monobank.tsx | (read line && { echo "token leak risk"; exit 1; } || exit 0)</automated>
    <automated>cd apps/mobile && grep -n "csv_no_rows_imported" app/onboarding/csv.tsx</automated>
    <automated>cd apps/mobile && grep -nE "client\\.accounts\\[0\\]" app/onboarding/monobank.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/app/onboarding/monobank.tsx contains "getMonobankClient" and "getMonobankStatement" and "secureSet('monobank_token'"
    - apps/mobile/app/onboarding/monobank.tsx contains the literal "client.accounts[0]" (B3 primary-account-only assertion)
    - apps/mobile/app/onboarding/monobank.tsx contains "accessibilityLiveRegion"
    - apps/mobile/app/onboarding/monobank.tsx does NOT contain any console.log/info/warn/error
    - apps/mobile/app/onboarding/csv.tsx contains "getDocumentAsync" and "parseCsv" and "csvRowsToTransactions"
    - apps/mobile/app/onboarding/csv.tsx contains the literal "result.inserted === 0" or "result.inserted == 0" (B4 zero-row guard)
    - apps/mobile/app/onboarding/csv.tsx contains the i18n key "onboarding.csv_no_rows_imported" (B4 error key)
    - Both screens contain "router.replace('/(tabs)')" and "setCompleted(true)"
    - apps/mobile/src/lib/i18n/en.json contains all 13 new monobank + csv keys (12 originally planned + csv_no_rows_imported)
    - apps/mobile/src/lib/i18n/uk.json contains the same 13 keys
    - cd apps/mobile && npx tsc --noEmit exits 0
    - cd apps/mobile && npx expo lint exits 0
  </acceptance_criteria>
  <done>monobank + CSV flows complete end-to-end; primary-account-only confirmed in code; token stays in secure-store; CSV size + format errors degrade gracefully; zero-import case surfaces explicit error instead of silently navigating to an empty dashboard.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: End-to-end Phase 1 verification on a physical iPhone</name>
  <what-built>
    All four onboarding paths now run end-to-end:
    - synthetic: ~270-540 rows + salaries
    - monobank: 31 days from the real API (primary account only)
    - csv: user-supplied file
    - manual: single row
    Each lands on the Phase 1 dashboard showing count + 30-day expense total.
  </what-built>
  <how-to-verify>
    1. cd /home/iskan/projects/soldi/apps/mobile && npx expo start --tunnel (or --lan if on same wifi).
    2. Open Expo Go on the iPhone, scan QR.
    3. Reset onboarding for a clean test: delete and reinstall Expo Go OR call SecureStore.deleteItemAsync('soldi-onboarding') via a dev hook. Easier: clear Expo Go's app data.
    4. From welcome screen, start a stopwatch. Pick a language. Pick "Try with sample data". Confirm dashboard appears within 90 seconds. Record actual elapsed seconds.
    5. Force-quit, relaunch. Confirm app goes straight to dashboard (skips onboarding).
    6. Reset onboarding. Repeat with "Add manually": enter Amount 12.50, Where "Tesco", Date Today, Category Groceries, submit. Confirm row appears.
    7. Reset onboarding. Repeat with "Import CSV". Use a sample CSV (committed to apps/mobile/tests/fixtures/sample.csv? — if not present, paste a tiny inline CSV using the iOS Files app: Date,Amount,Description / 2026-05-10,-12.34,Tesco / 2026-05-11,-3.50,Aroma Kava). Confirm rows imported.
    8. Reset onboarding. Repeat with "monobank": paste a real personal token. (User-only step; if user doesn't have one, skip to step 9.) Confirm 31 days of real transactions appear from the primary account only.
    9. With dashboard populated from any path, confirm: count displayed > 0, 30-day expense total > 0, formatted as €X,XXX.XX or ₴.
    10. Run VoiceOver (Settings → Accessibility → VoiceOver) on the welcome screen — confirm the EN/UK tiles are reachable and labelled.

    Record results in SUMMARY:
    - synthetic timing (seconds from welcome to dashboard)
    - manual timing
    - csv timing (using a 50-row sample)
    - monobank timing (or "skipped — no token available")
    - VoiceOver result: pass/fail
    - Any errors observed (paste exact message)
  </how-to-verify>
  <resume-signal>
    Type "approved" with the four timing numbers OR describe issues so revision is created.
  </resume-signal>
  <read_first>
    - /home/iskan/projects/soldi/.planning/phases/01-onboarding-data-ingest/01-SKELETON.md
  </read_first>
  <acceptance_criteria>
    - User reports synthetic, manual, csv (and ideally monobank) all completing under 90 seconds end-to-end on a warm device
    - User reports the dashboard reflects DB state (count + expense total) for each path
    - User reports relaunching the app skips onboarding
    - User reports VoiceOver reaches and labels the language tiles
    - SUMMARY records timings + any defects
  </acceptance_criteria>
  <done>Human approves with timings recorded, or defects logged for follow-up plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → token paste box | untrusted string; treat as opaque secret |
| device → api.monobank.ua | TLS 1.3 (iOS ATS default); header-based auth |
| user CSV → parser | untrusted bytes; size + row capped; quoted-field safe |
| parsed rows → DB | parameterized inserts; UNIQUE de-dupes |
| description text → DB | Phase 1 stores merchant_name only (truncated, whitespace-collapsed); raw description discarded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Information Disclosure | monobank token logged | mitigate | http.ts strips X-Token; verify gate greps for console.* in monobank.ts; no token in URL |
| T-01-04-02 | Information Disclosure | monobank token in error UI | mitigate | HttpError.bodyText not rendered to user; only translated message keys shown |
| T-01-04-03 | Tampering | token persisted in plaintext | mitigate | expo-secure-store with WHEN_UNLOCKED_THIS_DEVICE_ONLY keychain access |
| T-01-04-04 | Spoofing | non-monobank HTTPS endpoint | accept | URL is hardcoded literal in monobank.ts; not derived from input |
| T-01-04-05 | Denial of Service | huge CSV crashes parser | mitigate | maxBytes 5_000_000 throws CsvParseError 'too-large' before parsing; maxRows 200_000 |
| T-01-04-06 | Tampering | CSV cell with formula injection | mitigate | parser returns plain strings; nothing is eval'd; categoryslug resolution uses prepared SQL params |
| T-01-04-07 | Information Disclosure | description text crossing to AI in Phase 3 | mitigate | description column intentionally NULL for monobank + CSV; Phase 3 has no raw description to send |
| T-01-04-08 | Repudiation | failed insert silently swallowed | mitigate | insertManyTransactions returns { inserted, skipped }; CSV screen surfaces zero-import as explicit error per B4 (no silent success) |
| T-01-04-09 | Spoofing | manual entry accepts arbitrary merchant_name | accept | This is user-typed and goes only to local DB; no impact outside the user's device |
| T-01-04-10 | Denial of Service | monobank rate limit (429) blocks app | mitigate | Error path shows rate-limit message; retry button does not auto-loop; no spam |
</threat_model>

<verification>
1. cd apps/mobile && npx tsc --noEmit exits 0
2. cd apps/mobile && npx expo lint exits 0
3. cd apps/mobile && npx tsx tests/csv-parser.test.ts exits 0 (includes B2 contract test for categorySlug)
4. cd apps/mobile && npx tsx tests/monobank-mapper.test.ts exits 0
5. grep -nE "console\\.(log|info|warn|error)" apps/mobile/src/api/monobank.ts apps/mobile/src/lib/monobank/ apps/mobile/app/onboarding/monobank.tsx returns no hits
6. grep -E '\\$\\{token\\}.*api\\.monobank|api\\.monobank.*\\$\\{token\\}' across the codebase returns no hits (token-in-URL guard)
7. grep "client.accounts[0]" apps/mobile/app/onboarding/monobank.tsx returns a hit (B3 primary-only)
8. grep "csv_no_rows_imported" apps/mobile/app/onboarding/csv.tsx returns a hit (B4 zero-row guard)
9. Human verification (Task 4) confirms all four flows under 90s end-to-end on a physical iPhone.
</verification>

<success_criteria>
- DATA-01 satisfied: CSV import via document picker → op-sqlite rows, malformed input handled, zero-import case surfaces explicit error.
- DATA-02 satisfied: monobank token → /personal/client + /personal/statement → op-sqlite rows; token in secure-store only; primary account only.
- DATA-03 satisfied: manual entry form persists single transaction + merchant override.
- ONBD-03 satisfied: timed end-to-end < 90 seconds for all four paths on warm device, recorded in SUMMARY.
- All design / a11y / security constraints from CLAUDE.md upheld: design tokens only, accessibilityLabel + Role on every interactive element, expo-secure-store for token, no AsyncStorage anywhere, no inline hex, no logging of token, no token in URL.
- B2 contract closed: MappedCsvRow.categorySlug is always a defined string; test 11 in csv-parser.test.ts proves it.
- B3 sanctioned: monobank primary-account-only is reflected in SKELETON Out-of-Scope and in code.
- B4 closed: csv.tsx checks result.inserted vs finalRows.length and surfaces an explicit error instead of silently navigating to an empty dashboard.
</success_criteria>

<output>
After completion, create `.planning/phases/01-onboarding-data-ingest/01-04-SUMMARY.md` per the SUMMARY template, including the four timing measurements from the device test.
</output>
