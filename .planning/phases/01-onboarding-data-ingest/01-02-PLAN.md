---
phase: 01-onboarding-data-ingest
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - apps/mobile/app/onboarding/data-source.tsx
  - apps/mobile/src/components/SourceTile.tsx
autonomous: true
requirements:
  - ONBD-02
user_setup: []
must_haves:
  truths:
    - "After picking a language, user sees four source tiles labelled synthetic / manual / monobank / CSV"
    - "Each tile shows a localized title + localized one-line body"
    - "Tapping a tile sets dataSource in the onboarding store and pushes to the source-specific route"
    - "Tile press visually responds (Pressable feedback) and is reachable via VoiceOver"
    - "User can navigate back from data-source to welcome without state loss"
  artifacts:
    - path: "apps/mobile/app/onboarding/data-source.tsx"
      provides: "Vertical-slice data-source picker with 4 SourceTile entries"
      contains: "useOnboardingStore"
      min_lines: 60
    - path: "apps/mobile/src/components/SourceTile.tsx"
      provides: "Reusable tile component with icon ref, title, body, Pressable feedback, a11y"
      exports: ["SourceTile"]
      min_lines: 60
  key_links:
    - from: "app/onboarding/data-source.tsx"
      to: "src/stores/onboarding.ts::setDataSource"
      via: "Tile onPress calls store.setDataSource then router.push"
      pattern: "setDataSource\\("
    - from: "app/onboarding/data-source.tsx"
      to: "expo-router routes /onboarding/{synthetic|manual|monobank|csv}"
      via: "router.push with literal path matching store value"
      pattern: "/onboarding/(synthetic|manual|monobank|csv)"
---

<objective>
Replace the data-source stub from 01-01 with the real 4-way picker. Vertical slice: UI → store → route. After this plan, the user can pick a source and land on the source-specific screen (which 01-03 + 01-04 will populate).

Purpose: Closes the data-source pick UX gap. Source-specific screens are stubbed minimally here so navigation is observable end-to-end; full ingest logic lands in 01-03 and 01-04.
Output: A working 4-tile picker, each tile routing forward and writing dataSource to the persisted store.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-onboarding-data-ingest/01-SKELETON.md
@.planning/phases/01-onboarding-data-ingest/01-01-PLAN.md
@CLAUDE.md
@apps/mobile/src/design/tokens.ts
@apps/mobile/src/design/typography.ts

<interfaces>
<!-- Established by 01-01 -->

From src/stores/onboarding.ts:
- useOnboardingStore: hook with { language, dataSource, completed, setLanguage, setDataSource, setCompleted, reset }
- dataSource type: 'synthetic' | 'manual' | 'monobank' | 'csv' | null

From src/lib/i18n/en.json + uk.json (already populated by 01-01):
- onboarding.pick_source_title, onboarding.pick_source_subtitle
- onboarding.source_synthetic_title, onboarding.source_synthetic_body
- onboarding.source_manual_title, onboarding.source_manual_body
- onboarding.source_monobank_title, onboarding.source_monobank_body
- onboarding.source_csv_title, onboarding.source_csv_body

From src/components/PressableButton.tsx (from 01-01):
- A primary button pattern to mirror for SourceTile (Pressable wrapping, a11y, 44pt min)

Reanimated v4:
- useSharedValue, withTiming, useAnimatedStyle, Layout (for entering animations)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the reusable SourceTile component</name>
  <files>apps/mobile/src/components/SourceTile.tsx</files>
  <read_first>
    - /home/iskan/projects/soldi/CLAUDE.md
    - /home/iskan/projects/soldi/.planning/phases/01-onboarding-data-ingest/01-SKELETON.md
    - /home/iskan/projects/soldi/apps/mobile/src/design/tokens.ts
    - /home/iskan/projects/soldi/apps/mobile/src/design/typography.ts
    - /home/iskan/projects/soldi/apps/mobile/src/components/PressableButton.tsx
  </read_first>
  <action>
    Create apps/mobile/src/components/SourceTile.tsx exporting `SourceTile`:

    Props type:
    - title: string
    - body: string
    - iconName: string (purely a string ref for later Phase 2 SVG wiring; render no SVG yet — leave a 32×32 View placeholder using COLORS.accentSoft with RADIUS.md)
    - onPress: () => void
    - accessibilityLabel?: string (defaults to title)
    - testID?: string
    - delayMs?: number (for staggered entrance; defaults to 0)

    Layout:
    - Outer = Animated.View (Reanimated) running an entrance fade + translateY (16 → 0) with withTiming(1, {duration: 400}). Use the delayMs via withDelay.
    - Pressable inside: backgroundColor COLORS.surface, RADIUS.lg, padding SPACING.lg, minHeight 88, flexDirection 'row', alignItems 'center', gap SPACING.md, SHADOWS.card.
    - Pressed state: scale 0.98 + opacity 0.92 via Pressable's style callback OR via Reanimated useSharedValue toggled on onPressIn/Out. Choose Pressable style callback for simplicity.
    - Left placeholder: View 32×32 RADIUS.md backgroundColor COLORS.accentSoft (no SVG yet; testID="source-tile-icon-placeholder")
    - Right column: Text title with TYPE.uiBody color COLORS.textPrimary, marginBottom SPACING.xs. Text body with TYPE.uiLabel color COLORS.textSecondary. Both numberOfLines=2.
    - accessibilityRole='button'; accessibilityLabel falls back to title.
    - Use StyleSheet.create. No inline hex anywhere.

    Export:
    - export function SourceTile(props: SourceTileProps): JSX.Element
    - export type SourceTileProps as above

    No console.* in this file.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
    <automated>cd apps/mobile && grep -nE "#[0-9A-Fa-f]{3,6}" src/components/SourceTile.tsx | (read line && { echo "Inline hex found"; exit 1; } || exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/src/components/SourceTile.tsx exports a function named SourceTile (grep "export function SourceTile" returns a hit) and is at least 60 lines long
    - The file contains "accessibilityRole" and "accessibilityLabel"
    - The file contains "minHeight: 88" or "minHeight:88"
    - The file contains "useSharedValue" or "withTiming" (Reanimated entrance)
    - No inline hex color literals (grep returns 0 lines)
    - cd apps/mobile && npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Reusable SourceTile component with Reanimated entrance, Pressable feedback, design-token-only styling, and full a11y.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the data-source picker screen + 4 stub destination screens</name>
  <files>
    apps/mobile/app/onboarding/data-source.tsx,
    apps/mobile/app/onboarding/synthetic.tsx,
    apps/mobile/app/onboarding/manual.tsx,
    apps/mobile/app/onboarding/monobank.tsx,
    apps/mobile/app/onboarding/csv.tsx
  </files>
  <read_first>
    - /home/iskan/projects/soldi/CLAUDE.md
    - /home/iskan/projects/soldi/.planning/phases/01-onboarding-data-ingest/01-SKELETON.md
    - /home/iskan/projects/soldi/apps/mobile/app/onboarding/welcome.tsx
    - /home/iskan/projects/soldi/apps/mobile/src/stores/onboarding.ts
    - /home/iskan/projects/soldi/apps/mobile/src/lib/i18n/en.json
  </read_first>
  <action>
    Replace apps/mobile/app/onboarding/data-source.tsx with the real picker:
    - Imports: View, Text, ScrollView, SafeAreaView from react-native, useRouter from expo-router, useTranslation from react-i18next, useOnboardingStore from @stores/onboarding, SourceTile from @components/SourceTile, COLORS, SPACING, TYPE from @design.
    - Layout: SafeAreaView with backgroundColor COLORS.background, ScrollView contentContainerStyle paddingHorizontal SPACING.lg paddingTop SPACING.xl paddingBottom SPACING.xxl.
    - Header: Text TYPE.displayM color COLORS.textPrimary with key 'onboarding.pick_source_title' marginBottom SPACING.sm. Subtitle Text TYPE.editorialBody color COLORS.textSecondary with key 'onboarding.pick_source_subtitle' marginBottom SPACING.xl.
    - Four SourceTile instances in this order: synthetic, manual, monobank, csv. Stagger delayMs: 0, 60, 120, 180.
    - Each tile's onPress calls a local helper choose(source) that does useOnboardingStore.getState().setDataSource(source); router.push the literal path '/onboarding/' + source typed as const. Use typed routes (expo-router experiments.typedRoutes is enabled).
    - testID for each tile: "source-tile-synthetic" etc.
    - default export the screen component.

    Create 4 stub destination screens at apps/mobile/app/onboarding/{synthetic,manual,monobank,csv}.tsx, each identical shape:
    - SafeAreaView + ScrollView wrapper, COLORS.background
    - Text TYPE.displayM with the route's localized title key (synthetic → 'onboarding.source_synthetic_title' etc.)
    - Text TYPE.editorialBody color COLORS.textSecondary with body key matching the title
    - One PressableButton labelled 'common.continue' that calls useOnboardingStore.getState().setCompleted(true) then router.replace('/(tabs)') — purely to make the route reachable to the dashboard at this stage; 01-03 and 01-04 replace this stub with real ingest flow.
    - Each screen named default export `SyntheticScreen`, `ManualScreen`, `MonobankScreen`, `CsvScreen`.
    - accessibilityRole + Label on the PressableButton.

    Do NOT touch anything outside the listed files. Do NOT add libraries.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
    <automated>cd apps/mobile && npx expo lint</automated>
    <automated>cd apps/mobile && grep -c "SourceTile" app/onboarding/data-source.tsx | awk '$1 < 5 { exit 1 }'</automated>
    <automated>cd apps/mobile && grep -nE "#[0-9A-Fa-f]{3,6}" app/onboarding/ 2>/dev/null | grep -v "//" | (read line && { echo "Inline hex"; exit 1; } || exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/app/onboarding/data-source.tsx contains exactly 4 SourceTile usages (grep -c "<SourceTile" returns 4) and is at least 60 lines long
    - apps/mobile/app/onboarding/data-source.tsx contains the four route literals: '/onboarding/synthetic', '/onboarding/manual', '/onboarding/monobank', '/onboarding/csv'
    - All four destination files exist: apps/mobile/app/onboarding/synthetic.tsx, manual.tsx, monobank.tsx, csv.tsx
    - Each destination file contains "setCompleted(true)" and "router.replace('/(tabs)')"
    - cd apps/mobile && npx tsc --noEmit exits 0
    - cd apps/mobile && npx expo lint exits 0
    - No inline hex colors in any file under app/onboarding/
  </acceptance_criteria>
  <done>4-way picker functional, 4 destination stubs reachable, store records dataSource, navigation lands user at (tabs) dashboard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → router | source choice is a literal from a fixed set, not free text |
| store → secure-store | dataSource value persisted via 01-01's secure-store-backed Zustand persist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | route literal string concatenation | mitigate | Source value is a TypeScript literal union 'synthetic'\|'manual'\|'monobank'\|'csv'; router.push receives a typed Href from expo-router's typedRoutes; not user-typed |
| T-01-02-02 | Information Disclosure | tile labels include "monobank" | accept | brand reference only; no PII; no tokens until 01-04 |
| T-01-02-03 | Repudiation | no log of pick | accept | Phase 5 adds PostHog tracking with opt-in; Phase 1 stays silent |
</threat_model>

<verification>
1. cd apps/mobile && npx tsc --noEmit exits 0
2. cd apps/mobile && npx expo lint exits 0
3. data-source.tsx grep count of `<SourceTile` = 4
4. Each destination route file present and exports default component
5. Manual: Welcome → pick lang → land on data-source → tap each tile → land on stub → Continue → /(tabs); back nav at each stage preserves dataSource in store
</verification>

<success_criteria>
- Requirement ONBD-02 satisfied: user picks one of four data sources via a 4-tile screen, choice persists in the onboarding store, and navigation flows forward to a source-specific route.
- No new dependencies added; no inline hex; design tokens used throughout.
</success_criteria>

<output>
After completion, create `.planning/phases/01-onboarding-data-ingest/01-02-SUMMARY.md` per the SUMMARY template.
</output>
