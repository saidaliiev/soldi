---
phase: 04-jars-i18n-accessibility
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/mobile/src/features/jars/roundUp.ts
  - apps/mobile/src/features/jars/roundUp.test.ts
  - apps/mobile/src/features/jars/jarRingGeometry.ts
  - apps/mobile/src/features/jars/jarRingGeometry.test.ts
  - apps/mobile/src/features/jars/JarRing.tsx
  - apps/mobile/src/features/jars/sweepRepo.ts
  - apps/mobile/src/features/jars/sweepRepo.test.ts
  - apps/mobile/src/features/jars/JarDetailScreen.tsx
  - apps/mobile/src/features/jars/JarRow.tsx
  - apps/mobile/src/i18n/locales/en/jars.json
  - apps/mobile/src/i18n/locales/uk/jars.json
autonomous: true
requirements: [JAR-02, JAR-03]
must_haves:
  truths:
    - "User taps 'Sweep to jar' on jar detail and pending EUR-only round-ups since last sweep are contributed; sweep is manual (no auto-hook/scheduler), UAH/income excluded (D-02, D-03)"
    - "Round-up unit is configurable per jar (€1/€5/€10) and defaults to €1 when none is chosen (D-01)"
    - "The Skia progress ring animates from old balance fraction to new balance fraction over 300ms on contribution"
    - "Ring clamps display at 100%; balance over target shows an over-funded label, ring stays full"
  artifacts:
    - path: "apps/mobile/src/features/jars/roundUp.ts"
      provides: "pure round-up + pending-since-last-sweep calculation"
      exports: ["roundUpCents", "pendingContributionCents"]
    - path: "apps/mobile/src/features/jars/jarRingGeometry.ts"
      provides: "pure single-arc 0→pct geometry (donutArcs analog)"
      exports: ["jarRingArcPath"]
    - path: "apps/mobile/src/features/jars/JarRing.tsx"
      provides: "animated Skia ring (canvas-opacity crossfade + withTiming 300ms)"
      min_lines: 40
    - path: "apps/mobile/src/features/jars/sweepRepo.ts"
      provides: "sweepToJar transactional contribution writer"
      exports: ["sweepToJar", "lastSweepAt"]
  key_links:
    - from: "apps/mobile/src/features/jars/JarDetailScreen.tsx"
      to: "apps/mobile/src/features/jars/sweepRepo.ts"
      via: "sweepToJar() on Sweep tap"
      pattern: "sweepToJar\\("
    - from: "apps/mobile/src/features/jars/sweepRepo.ts"
      to: "apps/mobile/src/features/jars/roundUp.ts"
      via: "pendingContributionCents() over EUR expenses"
      pattern: "pendingContributionCents\\("
    - from: "apps/mobile/src/features/jars/JarRing.tsx"
      to: "apps/mobile/src/features/jars/jarRingGeometry.ts"
      via: "jarRingArcPath() for the progress arc"
      pattern: "jarRingArcPath\\("
---

<objective>
Second vertical slice (JAR-02 + JAR-03 round-up only): a user opens a jar, taps "Sweep to jar", the app sums pending €1/€5/€10 round-ups from EUR expenses since the last sweep and contributes them, and the Skia progress ring animates from the old fraction to the new fraction.

Purpose: Delivers the signature "monobank-style jars feel delightful" moment (ROADMAP goal) — the animated ring + manual deterministic sweep. Builds directly on 04-01's jarsRepo + JarDetailScreen.
Output: Pure roundUp + jarRingGeometry modules (+ colocated tests), animated JarRing Skia component, sweepRepo transactional writer, JarDetailScreen Sweep wiring, JarRow ring thumbnail.

JAR-03 NOTE: requirement text lists three top-up methods (round-up, fixed-weekly, %-of-income). Per 04-CONTEXT Deferred Ideas this is a deliberate scope narrowing — v1 ships configurable round-up unit ONLY. JAR-03 coverage is PARTIAL by design (frontmatter requirements includes JAR-03 with this note); fixed-weekly + %-income are deferred post-v1, NOT a miss.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-jars-i18n-accessibility/04-CONTEXT.md
@./CLAUDE.md
@.planning/phases/04-jars-i18n-accessibility/04-01-PLAN.md

<interfaces>
<!-- Extracted from codebase + 04-01 contracts. Executor uses these directly. -->

From plan 04-01 (jarsRepo.ts — already shipped in wave 1):
- `createJar(input): number`, `listJars(): readonly Jar[]`, `getJar(id): Jar | null`
- `jarBalanceCents(jarId: number): number` — COALESCE(SUM(amount_cents),0) over jar_contributions
- `insertContribution(c: { jarId; amountCents; source:'roundup'|'manual'; txId:number|null; createdAt }): number`
- types.ts: `Jar` (id,name,targetCents,icon,ruleJson,createdAt), `JarContribution`, `JarRule = { kind:'roundup'; unitCents: 100|500|1000 }` (rule_json shape; default 100 = €1 per D-01)
- DB schema v5: `jars`, `jar_contributions(id,jar_id,amount_cents,source,tx_id,created_at)`.

Pure-geometry analog — apps/mobile/src/features/dashboard/donutArcs.ts:
- `export type DonutArc = { path: string; color: string; categoryId: number|'other' }`
- `export function computeSliceAngles(slices, other, gapDeg): readonly SliceAngle[]` — 0° = 12 o'clock, clockwise.
- `export function buildDonutArcs(slices, other, radius, _strokeWidth, gapDeg): readonly DonutArc[]` — returns SVG arc path strings (M/A commands) consumable by `Skia.Path.MakeFromSVGString`.
- Colocated `donutArcs.test.ts` uses node `assert` (no jest). Mirror this for jarRingGeometry.test.ts + roundUp.test.ts + sweepRepo.test.ts.
Second analog — apps/mobile/src/features/chat/chatChartGeometry.ts: `donutArcs(slices, radius=40, strokeWidth=8)` single-arc-per-slice idiom; arc start at -π/2 (12 o'clock), GAP_DEG handling.

Skia + reanimated v4 animation idiom — apps/mobile/src/features/dashboard/DonutChart.tsx:
- `import { Canvas, Path, Skia } from '@shopify/react-native-skia';`
- `import Animated, { Easing, useAnimatedStyle, useSharedValue, withTiming } from 'react-native-reanimated';`
- D-05 locked pattern: drive ONE shared progress value with `withTiming(target, { duration: 300, easing: Easing.out(Easing.cubic) })`; on data change crossfade canvas opacity over the same 300ms window (per-path topology interpolation deferred). Anim on JS thread (D-26). usePathInterpolation is NOT used in the existing code — only adopt it if a context7 check confirms the installed react-native-skia + reanimated v4 API supports it cleanly (D-04); default to the proven opacity-crossfade.

Money — apps/mobile/src/lib/money.ts: `Money={amountCents,currency}`, `formatMoney(m, locale='en-IE')`, `toCents`.

EUR-expense source — apps/mobile/src/features/transactions/filterCompose.ts:
- expense predicate is `amount_cents < 0`; `transactions.currency TEXT NOT NULL DEFAULT 'EUR'`.
- D-03: round-up source = EUR expenses ONLY (negative amount_cents AND currency='EUR'); UAH/monobank excluded.
- DB exec: `getDB().executeSync(sql)` → `{ rows }`; row col access `result.rows[i]['col']`.

Typography — Oswald for the ring center hero number (TYPE.* preset; same family as MonthlyTotalHero.tsx). GRADIENTS.sage / COLORS.sage = savings/success semantic for the ring fill (D-04). Tokens only, no BANNED_COLORS.
</interfaces>

# Source-of-truth analogs the executor MUST read before writing
@apps/mobile/src/features/dashboard/donutArcs.ts
@apps/mobile/src/features/dashboard/donutArcs.test.ts
@apps/mobile/src/features/chat/chatChartGeometry.ts
@apps/mobile/src/features/dashboard/DonutChart.tsx
@apps/mobile/src/features/jars/jarsRepo.ts
@apps/mobile/src/features/jars/types.ts
@apps/mobile/src/features/jars/JarDetailScreen.tsx
@apps/mobile/src/features/jars/JarRow.tsx
@apps/mobile/src/features/transactions/filterCompose.ts
@apps/mobile/src/lib/db/index.ts
@apps/mobile/src/lib/money.ts
@apps/mobile/src/design/tokens.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure round-up engine + jar ring geometry (+ colocated tests)</name>
  <files>apps/mobile/src/features/jars/roundUp.ts, apps/mobile/src/features/jars/roundUp.test.ts, apps/mobile/src/features/jars/jarRingGeometry.ts, apps/mobile/src/features/jars/jarRingGeometry.test.ts</files>
  <read_first>
    apps/mobile/src/features/dashboard/donutArcs.ts + donutArcs.test.ts (pure-geometry + node-assert test idiom — exact template to mirror),
    apps/mobile/src/features/chat/chatChartGeometry.ts (single-arc SVG path, -π/2 start, GAP_DEG),
    apps/mobile/src/features/jars/types.ts (JarRule = { kind:'roundup'; unitCents: 100|500|1000 }),
    apps/mobile/src/features/transactions/filterCompose.ts (amount_cents < 0 expense predicate, currency column)
  </read_first>
  <behavior>
    - roundUp.test.ts: `roundUpCents(amountCents, unitCents)` — for an expense of -437 cents with unit 100 returns 63 (next €1 boundary on absolute value); -437 with unit 500 returns 63→ actually distance to next €5 = 500-(437%500)=63; -500 with unit 100 returns 0 (already on boundary); 0 returns 0; a positive (income) amount returns 0 (income never rounds up).
    - roundUp.test.ts: `pendingContributionCents(expenses, unitCents)` sums roundUpCents over a list of EUR expense rows; ignores any row with currency !== 'EUR'; ignores positive amounts; empty list → 0.
    - jarRingGeometry.test.ts: `jarRingArcPath(fraction, radius, strokeWidth)` — fraction 0 returns an empty/zero-length path (or a documented degenerate value); fraction 1 returns a full 360°−ε arc; fraction 0.5 sweeps 180°; fraction > 1 is clamped to 1 (over-funded → full ring); start at 12 o'clock matching the donut idiom; returned string is Skia-MakeFromSVGString-compatible (M/A commands).
  </behavior>
  <action>
    Create apps/mobile/src/features/jars/roundUp.ts exporting:
    `roundUpCents(amountCents: number, unitCents: 100 | 500 | 1000): number` — if amountCents >= 0 return 0 (income/transfer never contributes, D-03); else let abs = Math.abs(amountCents); remainder = abs % unitCents; return remainder === 0 ? 0 : unitCents - remainder.
    `pendingContributionCents(expenses: readonly { amountCents: number; currency: string }[], unitCents: 100|500|1000): number` — sum roundUpCents over rows where currency === 'EUR' && amountCents < 0 (D-03 EUR-only; UAH excluded).
    Create apps/mobile/src/features/jars/jarRingGeometry.ts exporting `jarRingArcPath(fraction: number, radius: number, strokeWidth: number): string` — clamp fraction to [0,1] (over-funded clamps to 1 per D-04); compute a single arc from -90° (12 o'clock, matching donutArcs/chatChartGeometry convention) sweeping `clampedFraction * 360` degrees clockwise; emit an SVG `M`/`A` path string consumable by `Skia.Path.MakeFromSVGString` (reuse the arc-path helper math idiom from chatChartGeometry.ts / donutArcs.ts — do not invent a new convention). Document the fraction=0 degenerate return in a JSDoc line.
    Create roundUp.test.ts and jarRingGeometry.test.ts colocated, using the project node `assert` test idiom (mirror donutArcs.test.ts exactly), covering every behavior above. Author even though jest is absent (STATE.md / [[jest-harness-missing]]) — must typecheck + lint.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/mobile && npx tsc --noEmit` exits 0; `cd apps/mobile && npx expo lint` exits 0.
    - roundUp.ts exports `roundUpCents` + `pendingContributionCents`; jarRingGeometry.ts exports `jarRingArcPath` (grep `export function`).
    - roundUp.ts contains an explicit `amountCents >= 0` early-return and a `currency === 'EUR'` filter (grep) — D-03 honored.
    - jarRingGeometry.ts clamps fraction (grep for `Math.min(1` or equivalent clamp) — D-04 over-funded → full ring.
    - jarRingGeometry start angle uses the -90°/-π/2 12-o'clock convention (grep, consistent with donutArcs/chatChartGeometry).
    - roundUp.test.ts + jarRingGeometry.test.ts exist with assertions for each documented behavior (grep for `roundUpCents`, `pendingContributionCents`, `jarRingArcPath`).
    - No BANNED_COLORS literal in these pure modules (none expected).
  </acceptance_criteria>
  <done>Pure round-up + ring geometry modules + colocated tests exist; tsc + lint exit 0; D-01/D-03/D-04 invariants encoded.</done>
</task>

<task type="auto">
  <name>Task 2: sweepToJar transactional writer (manual sweep, pending-since-last-sweep)</name>
  <files>apps/mobile/src/features/jars/sweepRepo.ts, apps/mobile/src/features/jars/sweepRepo.test.ts</files>
  <read_first>
    apps/mobile/src/lib/db/index.ts (getDB().executeSync, transaction/BEGIN-COMMIT idiom),
    apps/mobile/src/lib/db/schema.sql.ts (jar_contributions columns: jar_id, amount_cents, source, tx_id, created_at; SCHEMA_005),
    apps/mobile/src/features/jars/jarsRepo.ts (insertContribution, jarBalanceCents, getJar — 04-01),
    apps/mobile/src/features/jars/roundUp.ts (pendingContributionCents — Task 1),
    apps/mobile/src/features/transactions/filterCompose.ts (amount_cents < 0 AND currency = 'EUR' predicate),
    apps/mobile/src/features/jars/types.ts (JarRule.unitCents)
  </read_first>
  <action>
    Create apps/mobile/src/features/jars/sweepRepo.ts exporting:
    `lastSweepAt(jarId: number): number` — the MAX(created_at) over jar_contributions WHERE jar_id=? AND source='roundup' (0 if none → first sweep considers all history).
    `sweepToJar(jarId: number, now?: number): { contributedCents: number; newBalanceCents: number }` — read the jar via getJar to parse `rule_json` → unitCents (D-01); query EUR expense transactions with `amount_cents < 0 AND currency = 'EUR' AND created_at > ?` (the lastSweepAt cutoff) using getDB().executeSync with bound params (D-02 manual, D-03 EUR-only — never read or log the merchant/description column, CLAUDE.md AI/PII rule still applies even though no AI here); compute `pendingContributionCents(rows, unitCents)`; if > 0 insert ONE `jar_contributions` row via insertContribution (source='roundup', tx_id=null since it aggregates many tx, created_at=now ?? Date.now()); wrap the read+insert so a zero-pending sweep is a no-op (no empty contribution row). Return contributed + recomputed jarBalanceCents.
    Accept an optional injected DB defaulting to getDB() so the colocated sweepRepo.test.ts can drive an openTestDB seeded with jars + transactions (mirror jarsRepo test injection from 04-01).
    Create sweepRepo.test.ts (node assert idiom): seed v5 DB with one jar (rule_json unit 100) + EUR expenses + one UAH expense + one income row; assert sweepToJar contributes the EUR-expense-only round-up sum, that a second immediate sweep contributes 0 (cutoff advanced), and that UAH/income rows are excluded. Author even though jest absent.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/mobile && npx tsc --noEmit` exits 0; `cd apps/mobile && npx expo lint` exits 0.
    - sweepRepo.ts exports `sweepToJar` + `lastSweepAt` (grep export function).
    - sweepRepo.ts query string contains `amount_cents < 0` AND `currency = 'EUR'` AND a `created_at > ?` cutoff bind (grep) — D-02/D-03.
    - sweepToJar parses rule_json for unitCents and calls `pendingContributionCents(` and `insertContribution(` with `source` of `'roundup'` (grep).
    - sweepRepo.ts does NOT select or reference any merchant/description column (grep must NOT match `merchant_name` or `description` in the sweep query) — PII contract.
    - Zero-pending sweep inserts no row (test asserts contribution count unchanged when pending=0).
    - sweepRepo.test.ts asserts UAH + income exclusion and second-sweep-returns-0.
  </acceptance_criteria>
  <done>sweepToJar deterministically contributes pending EUR round-ups since last sweep; idempotent on empty; tsc + lint exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: Animated Skia JarRing + wire Sweep into JarDetailScreen + JarRow thumbnail</name>
  <files>apps/mobile/src/features/jars/JarRing.tsx, apps/mobile/src/features/jars/JarDetailScreen.tsx, apps/mobile/src/features/jars/JarRow.tsx, apps/mobile/src/i18n/locales/en/jars.json, apps/mobile/src/i18n/locales/uk/jars.json</files>
  <read_first>
    apps/mobile/src/features/dashboard/DonutChart.tsx (Canvas/Path/Skia imports, useSharedValue/withTiming 300ms Easing.out(Easing.cubic), canvas-opacity crossfade on data change, JS-thread anim, __DEV__ console.time guard),
    apps/mobile/src/features/jars/jarRingGeometry.ts (jarRingArcPath — Task 1),
    apps/mobile/src/features/jars/sweepRepo.ts (sweepToJar — Task 2),
    apps/mobile/src/features/jars/jarsRepo.ts (getJar, jarBalanceCents),
    apps/mobile/src/features/jars/JarDetailScreen.tsx + JarRow.tsx (04-01 — the jar-ring-slot placeholder + row layout to upgrade),
    apps/mobile/src/lib/money.ts (formatMoney), apps/mobile/src/design/tokens.ts (GRADIENTS.sage / COLORS.sage), @design/typography (Oswald hero preset)
  </read_first>
  <action>
    CONTEXT7 DOC-CHECK (mandatory per CLAUDE.md, do this first): resolve `@shopify/react-native-skia` and `react-native-reanimated` and confirm (a) the installed Skia API for rendering an SVG-path arc (`Skia.Path.MakeFromSVGString` + `<Path>`), and (b) whether the installed reanimated v4 + skia expose `usePathInterpolation` cleanly. Record the finding in the SUMMARY. Per D-04: ONLY use usePathInterpolation if context7 confirms a clean installed API; otherwise use the proven D-05 canvas-opacity crossfade + single shared progress value. Default to the crossfade.
    Create apps/mobile/src/features/jars/JarRing.tsx: a Skia `<Canvas>` rendering a background track ring + a progress arc from `jarRingArcPath(fraction, radius, strokeWidth)` where fraction = balanceCents / targetCents (clamped in the geometry module). Props: `{ balanceCents: number; targetCents: number; size?: number }`. Animate old→new fraction with a single `useSharedValue` driven by `withTiming(targetFraction, { duration: 300, easing: Easing.out(Easing.cubic) })` and the canvas-opacity crossfade on change (mirror DonutChart.tsx exactly; anim on JS thread per D-26). Center label: Oswald hero number (TYPE.* preset, same family as MonthlyTotalHero.tsx) showing formatMoney of the balance; when balanceCents > targetCents render an `over_funded` label from the jars i18n namespace and keep the ring full (D-04). Ring fill uses COLORS.sage / GRADIENTS.sage (savings/success semantic) — tokens only, no BANNED_COLORS. The Canvas gets a SINGLE summarizing accessibilityLabel (e.g. t('jars.ring_a11y', { balance, target, pct })) with child paths decorative/hidden (this satisfies the Skia-canvas a11y contract; the global focus sweep is plan 04-04, but the ring's own summarizing label is authored here so 04-04 only standardizes, not retrofits).
    Add jars i18n keys `over_funded`, `ring_a11y`, `sweep_cta`, `sweep_done`, `sweep_nothing` to en/jars.json + uk/jars.json (real Ukrainian first-pass; key parity maintained — 04-03 owns the full audited pass).
    Edit apps/mobile/src/features/jars/JarDetailScreen.tsx: replace the `jar-ring-slot` placeholder with `<JarRing balanceCents={...} targetCents={...} />`; add a "Sweep to jar" Pressable (accessibilityLabel + accessibilityRole="button", 44pt) that calls `sweepToJar(jarId)`, then refreshes balance state so the ring animates old→new; show a brief result message using sweep_done / sweep_nothing keys (no raw tx data). Edit JarRow.tsx to render a small JarRing thumbnail (or a compact arc) so the list reflects progress.
    No console.log of amounts in production (wrap any timing log in `__DEV__` like DonutChart). Tokens + correct typeface throughout.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/mobile && npx tsc --noEmit` exits 0; `cd apps/mobile && npx expo lint` exits 0.
    - JarRing.tsx imports `{ Canvas, Path, Skia }` from '@shopify/react-native-skia' and `useSharedValue` + `withTiming` + `Easing` from 'react-native-reanimated' (grep) and calls `jarRingArcPath(`.
    - JarRing.tsx uses `withTiming(`...`duration: 300` and `Easing.out(Easing.cubic)` (grep) — D-05 locked animation spec.
    - JarRing.tsx Canvas has exactly one summarizing accessibilityLabel and child Path elements are not individually focusable (grep: accessibilityLabel present on Canvas/wrapper; no per-Path accessibilityLabel).
    - JarDetailScreen.tsx no longer contains `testID="jar-ring-slot"` and now renders `<JarRing` and calls `sweepToJar(`.
    - en/uk jars.json still have identical key sets and include over_funded/ring_a11y/sweep_cta (`python3` set-equality check exits 0); uk values not byte-identical to en.
    - SUMMARY records the context7 finding on usePathInterpolation availability and which animation path (crossfade vs path-interp) was chosen and why (D-04 traceability).
    - No BANNED_COLORS hex literal; ring uses COLORS.sage/GRADIENTS.sage token (grep token name, not raw hex).
  </acceptance_criteria>
  <done>Ring animates old→new on sweep; over-funded clamps full + label; Sweep wired end-to-end; context7-verified animation choice recorded; tsc + lint exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local SQLite → sweep aggregation | sweepToJar reads EUR expense rows and writes an aggregate contribution row, all local |
| no network / no AI boundary | sweep + ring are 100% local; no Edge Function; PII contract still applies (no merchant/description read or log) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | sweepRepo SQL | mitigate | Parameter-bound queries only (`?` placeholders), no string-concatenated input; mirrors transactions repo idiom |
| T-04-02-02 | Information disclosure | sweep query reading tx rows | mitigate | Sweep query selects only amount_cents + currency + created_at — never merchant_name/description; no console.log of amounts in production (`__DEV__`-guarded only, mirrors DonutChart) |
| T-04-02-03 | Repudiation | contribution ledger | mitigate | Every sweep writes an auditable jar_contributions row (source, created_at) — supports Phase-5 milestone notifications + undo/audit (D-05 rationale) |
| T-04-02-04 | Denial of service | double-sweep / rapid taps | accept | lastSweepAt cutoff advances after each sweep so a repeated tap contributes 0 (idempotent within a window); UI disables button during the synchronous write |
| T-04-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (uses installed skia + reanimated + op-sqlite). Any new install requires a blocking-human legitimacy checkpoint first. |
</threat_model>

<verification>
- `cd apps/mobile && npx tsc --noEmit` exits 0
- `cd apps/mobile && npx expo lint` exits 0
- roundUp / jarRingGeometry / sweepRepo pure + repo modules + colocated tests exist
- D-01 (configurable unit, default €1), D-02 (manual sweep), D-03 (EUR-only), D-04 (clamp + over-funded + context7-gated interpolation), D-05 (300ms Easing.out(Easing.cubic) crossfade) all grep-verifiable
- JAR-03 partial coverage documented (round-up only; fixed-weekly/%-income deferred — not a miss)
- No BANNED_COLORS; sweep query has no PII columns
</verification>

<success_criteria>
- User taps "Sweep to jar" and pending EUR round-ups (configurable €1/€5/€10 unit, default €1) since last sweep are contributed
- Skia ring animates old→new fraction over 300ms Easing.out(Easing.cubic); clamps at 100%; over-funded shows label, ring full
- JAR-02 satisfied; JAR-03 partially satisfied by design (round-up method only — fixed-weekly + %-income deferred per 04-CONTEXT)
</success_criteria>

<output>
Create `.planning/phases/04-jars-i18n-accessibility/04-02-SUMMARY.md` when done. Record the context7 usePathInterpolation finding + chosen animation path.
</output>
