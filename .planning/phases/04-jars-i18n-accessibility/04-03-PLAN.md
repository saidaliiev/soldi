---
phase: 04-jars-i18n-accessibility
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/mobile/app/settings.tsx
  - apps/mobile/src/features/settings/SettingsScreen.tsx
  - apps/mobile/src/features/settings/LanguageToggle.tsx
  - apps/mobile/app/_layout.tsx
  - apps/mobile/app/(tabs)/index.tsx
  - apps/mobile/src/lib/i18n/index.ts
  - apps/mobile/src/stores/onboarding.ts
  - apps/mobile/src/i18n/locales/uk/dashboard.json
  - apps/mobile/src/i18n/locales/uk/transactions.json
  - apps/mobile/src/i18n/locales/uk/categories.json
  - apps/mobile/src/i18n/locales/uk/chat.json
  - apps/mobile/src/i18n/locales/uk/ai.json
  - apps/mobile/src/i18n/locales/uk/settings.json
  - apps/mobile/src/i18n/locales/en/settings.json
  - apps/mobile/scripts/check-i18n-parity.mjs
  - apps/mobile/package.json
  - .planning/STATE.md
  - .planning/phases/03-ai-categorization-chat/03-VERIFICATION.md
autonomous: true
requirements: [SET-02]
must_haves:
  truths:
    - "User opens Settings via a gear control in the dashboard header (a new app/settings.tsx stack route, not a 5th tab) (D-06)"
    - "User toggles language EN↔UK in Settings (setLanguage + onboarding store + root key-bump remount) and every screen — including date headers and sticky list headers — retranslates without a manual restart (D-07)"
    - "Every en namespace key has a uk counterpart and vice-versa (UK first-pass all namespaces), enforced by a key-parity script that gates the tsc/lint verification (D-08)"
  artifacts:
    - path: "apps/mobile/app/settings.tsx"
      provides: "Settings stack route"
    - path: "apps/mobile/src/features/settings/LanguageToggle.tsx"
      provides: "EN↔UK toggle that drives setLanguage + store + remount"
      min_lines: 25
    - path: "apps/mobile/scripts/check-i18n-parity.mjs"
      provides: "en/uk key-parity gate"
    - path: "apps/mobile/src/i18n/locales/uk/transactions.json"
      provides: "completed native-first-pass UK translation"
  key_links:
    - from: "apps/mobile/app/(tabs)/index.tsx"
      to: "apps/mobile/app/settings.tsx"
      via: "gear header control → router.push('/settings')"
      pattern: "settings"
    - from: "apps/mobile/src/features/settings/LanguageToggle.tsx"
      to: "apps/mobile/src/lib/i18n/index.ts"
      via: "setLanguage(lng)"
      pattern: "setLanguage\\("
    - from: "apps/mobile/src/features/settings/LanguageToggle.tsx"
      to: "apps/mobile/app/_layout.tsx"
      via: "language → root key-bump remount"
      pattern: "key="
---

<objective>
Third vertical slice (SET-02): a new Settings stack route reached via a gear control in the dashboard header, containing an EN↔UK language toggle that retranslates the entire app at runtime (no restart), backed by a complete native-first-pass Ukrainian translation across all namespaces and a key-parity script that gates verification.

Purpose: SET-02 is the runtime-language capability. Independent of jars (touches settings/i18n/root-layout, zero overlap with 04-02's jar files) so it runs parallel to 04-02 in wave 2. Also performs the orphaned-deferral doc correction (merchant_overrides repoint).
Output: app/settings.tsx route, SettingsScreen + LanguageToggle, gear entry in dashboard header, onboarding-store language write, root key-bump remount, completed UK translations for all 5 existing namespaces + settings ns, en/settings.json, check-i18n-parity script wired into lint, STATE.md + 03-VERIFICATION.md deferral repoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-jars-i18n-accessibility/04-CONTEXT.md
@./CLAUDE.md

<interfaces>
<!-- Extracted from codebase. Executor uses these directly. -->

i18n — apps/mobile/src/lib/i18n/index.ts:
- `export const i18n = createInstance();`
- `export async function initI18n(forcedLng?: 'en'|'uk'): Promise<void>` — detects device locale, falls back to 'en'.
- `export async function setLanguage(lng: 'en'|'uk'): Promise<void>` — wraps `i18n.changeLanguage(lng)`.
- Resources are a deep-merged single `translation` ns: enBundle/ukBundle compose namespaced JSON from src/i18n/locales/{en,uk}/{dashboard,categories,transactions,chat,ai}.json (+ jars from 04-01). New `settings` ns must be imported + merged into BOTH bundles the same way.
- KNOWN STALE COMMENT at index.ts ~:28 ("uk bundle currently mirrors en TODO Phase 4") — measured reality (this plan must re-measure, not trust the comment): uk/dashboard.json = 15/15 keys byte-identical to en (UNTRANSLATED), uk/transactions.json = only 4/35 translated, uk/chat.json + uk/ai.json = en stubs, uk/categories.json = measure during the task. Treat ALL uk namespaces as needing a real native-first-pass.

onboarding store — apps/mobile/src/stores/onboarding.ts:
- `useOnboardingStore = create<OnboardingState>()(persist((set)=>({ language: null, setLanguage:(lng)=>set({language:lng}), ... }), { name:'soldi-onboarding', storage: createJSONStorage(<expo-secure-store adapter>) }))`
- `language: 'en'|'uk'|null` persisted via expo-secure-store (NOT AsyncStorage — CLAUDE.md security rule). `setLanguage(lng)` already exists on the store; the language toggle must call BOTH this AND the i18n module setLanguage (D-07).

root layout — apps/mobile/app/_layout.tsx:
- `export default function RootLayout()`: loads fonts, `useEffect(() => { await initI18n(); ... runMigrations(getDB()); })`, gates render on `fontsLoaded && i18nReady && dbReady`, then returns `<GestureHandlerRootView><QueryClientProvider><I18nextProvider i18n={i18n}><Stack screenOptions={{contentStyle:{backgroundColor:COLORS.background}}}> <Stack.Screen name="index"/> name="onboarding" name="(tabs)" name="modal" name="transactions/[id]" name="transactions/search" </Stack> <RecategorizeBottomSheet/><PropagationToast/><ChatBottomSheet/><StatusBar/></I18nextProvider>...`
- There is NO `key` prop anywhere today. D-07: bump a `key` on a wrapper that subtrees the whole app (e.g. `<I18nextProvider key={lang}>` or the `<Stack key={lang}>`) sourced from the persisted language so a language change forces a full remount — i18next reactivity alone is proven insufficient for module-scope `t()`, `Intl.DateTimeFormat` date headers, and FlashList sticky headers (D-07).

dashboard header — apps/mobile/app/(tabs)/index.tsx: `export default function DashboardScreen()`, structure MonthSwiper → MonthlyTotalHero → DonutChart + CategoryRows → EmptyState; add a gear Pressable in the header region (top of the screen, near MonthSwiper) that does `router.push('/settings')` via expo-router `useRouter()`. Custom SVG gear icon (no emoji/lucide) — mirror src/design/icons/tabs.tsx idiom.

Existing script idiom — apps/mobile/scripts/ contains only reset-project.js (plain node script, no ts). package.json scripts: { start, reset-project, android, ios, web, lint:"expo lint" }. No jest, no `test` script (jest harness absent — STATE.md / [[jest-harness-missing]]). Add the parity script as a node `.mjs` and wire it so the verification gate runs it (e.g. a `lint:i18n` script + invoke it in the acceptance commands; do NOT add a `test` script).

locale shape — every src/i18n/locales/{en,uk}/<ns>.json is a flat `{ "key": "value with {{interpolation}}" }` object.
</interfaces>

# Source-of-truth analogs the executor MUST read before writing
@apps/mobile/src/lib/i18n/index.ts
@apps/mobile/src/stores/onboarding.ts
@apps/mobile/app/_layout.tsx
@apps/mobile/app/(tabs)/index.tsx
@apps/mobile/app/(tabs)/_layout.tsx
@apps/mobile/src/design/icons/tabs.tsx
@apps/mobile/scripts/reset-project.js
@apps/mobile/src/i18n/locales/en/transactions.json
@apps/mobile/src/i18n/locales/uk/transactions.json
@apps/mobile/src/design/tokens.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings route + gear entry + LanguageToggle + runtime key-bump remount</name>
  <files>apps/mobile/app/settings.tsx, apps/mobile/src/features/settings/SettingsScreen.tsx, apps/mobile/src/features/settings/LanguageToggle.tsx, apps/mobile/app/_layout.tsx, apps/mobile/app/(tabs)/index.tsx</files>
  <read_first>
    apps/mobile/app/_layout.tsx (Stack tree, render gate, I18nextProvider wrapper, NO existing key prop — where the key-bump attaches per D-07),
    apps/mobile/src/lib/i18n/index.ts (setLanguage, initI18n, single translation ns merge),
    apps/mobile/src/stores/onboarding.ts (useOnboardingStore.language + setLanguage, expo-secure-store persistence),
    apps/mobile/app/(tabs)/index.tsx (dashboard header region to add gear control),
    apps/mobile/app/(tabs)/_layout.tsx (confirm Settings is a stack route NOT a 5th tab — D-06),
    apps/mobile/src/design/icons/tabs.tsx (SVG icon idiom for the gear),
    apps/mobile/src/design/tokens.ts + @design/typography (tokens + TYPE.* presets)
  </read_first>
  <action>
    CONTEXT7 DOC-CHECK (mandatory, first): resolve `i18next` / `react-i18next` / `expo-localization` and confirm the runtime `changeLanguage` propagation behavior and that a React remount via a changed `key` is the supported way to force module-scope `t()` / `Intl.DateTimeFormat` re-evaluation. Record the finding in SUMMARY (D-07 traceability).
    Create apps/mobile/app/settings.tsx as a stack route that renders `SettingsScreen` (D-06: NEW stack route — do NOT add a tab to app/(tabs)/_layout.tsx; preserve the locked Phase-2 four-tab editorial shell). Register it in app/_layout.tsx `<Stack>` as `<Stack.Screen name="settings" options={{ title: ... , presentation:'card' }} />` (screen-local title may instead come from a `<Stack.Screen/>` inside SettingsScreen — prefer the root registration here since this plan owns app/_layout.tsx).
    Create apps/mobile/src/features/settings/SettingsScreen.tsx: a scrollable settings screen (RN primitives, StyleSheet.create, tokens, TYPE.* — EB Garamond/Manrope per role) with a "Language" section hosting `LanguageToggle`. Section scaffold should accommodate future settings (D-06) but only Language is implemented now (biometric/export are Phase 5 — do NOT add them; not in scope).
    Create apps/mobile/src/features/settings/LanguageToggle.tsx: a segmented EN | Українська control. On select it MUST (D-07): (1) call `setLanguage(lng)` from @lib/i18n, (2) call `useOnboardingStore.getState().setLanguage(lng)` to persist to expo-secure-store, (3) cause the root key-bump. Implement the key-bump by reading the persisted `language` in app/_layout.tsx and applying it as `key={language ?? 'en'}` on the app-wide wrapper (the `<I18nextProvider>` or the `<Stack>` — whichever subtrees the entire navigable app incl. tabs, modals, sticky list headers). A brief flash on this explicit user action is the accepted trade-off (04-CONTEXT specifics).
    Edit apps/mobile/app/(tabs)/index.tsx: add a gear Pressable in the dashboard header region using `useRouter().push('/settings')`, a custom SVG gear icon (new icon following src/design/icons/tabs.tsx idiom, no emoji), accessibilityLabel (i18n settings.open_a11y) + accessibilityRole="button", 44pt min tap target.
    All new UI: tokens only (no BANNED_COLORS hex), correct typeface, every interactive element accessibilityLabel + accessibilityRole.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/mobile && npx tsc --noEmit` exits 0; `cd apps/mobile && npx expo lint` exits 0.
    - app/settings.tsx exists and is registered as a `Stack.Screen name="settings"` in app/_layout.tsx; app/(tabs)/_layout.tsx still has exactly the original tab count (jars tab from 04-01 + original 3 = 4; NO settings tab) — `grep -c "Tabs.Screen" app/(tabs)/_layout.tsx` unchanged from 04-01.
    - LanguageToggle.tsx calls `setLanguage(` (from @lib/i18n) AND `useOnboardingStore` setLanguage (grep both).
    - app/_layout.tsx contains a `key=` bound to the persisted language on an app-wide wrapper (grep `key=` near I18nextProvider/Stack) — D-07 remount.
    - app/(tabs)/index.tsx contains a gear Pressable doing `push('/settings')` with accessibilityRole="button" + accessibilityLabel (grep).
    - SUMMARY records the context7 i18next/key-remount finding (D-07 traceability).
    - No BANNED_COLORS literal in new files; gear icon is SVG (no emoji).
  </acceptance_criteria>
  <done>Settings route reachable via dashboard gear; language toggle wired to setLanguage + store + root remount; tsc + lint exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Complete native-first-pass UK translation for all namespaces + settings ns + i18n wiring</name>
  <files>apps/mobile/src/i18n/locales/uk/dashboard.json, apps/mobile/src/i18n/locales/uk/transactions.json, apps/mobile/src/i18n/locales/uk/categories.json, apps/mobile/src/i18n/locales/uk/chat.json, apps/mobile/src/i18n/locales/uk/ai.json, apps/mobile/src/i18n/locales/uk/jars.json, apps/mobile/src/i18n/locales/en/settings.json, apps/mobile/src/i18n/locales/uk/settings.json, apps/mobile/src/lib/i18n/index.ts</files>
  <read_first>
    apps/mobile/src/lib/i18n/index.ts (deep-merge wiring; the STALE :28 comment — measure, do not trust),
    apps/mobile/src/i18n/locales/en/dashboard.json + uk/dashboard.json (15/15 untranslated — baseline),
    apps/mobile/src/i18n/locales/en/transactions.json + uk/transactions.json (35 keys, only 4 translated),
    apps/mobile/src/i18n/locales/en/categories.json + uk/categories.json,
    apps/mobile/src/i18n/locales/en/chat.json + uk/chat.json (en stubs),
    apps/mobile/src/i18n/locales/en/ai.json + uk/ai.json (en stubs),
    apps/mobile/src/i18n/locales/en/jars.json + uk/jars.json (04-01/04-02 first-pass — bring to audited quality + parity)
  </read_first>
  <action>
    First MEASURE every uk namespace against its en counterpart (the index.ts :28 "uk mirrors en" comment is stale — re-derive actual translated/untranslated counts; record the measured baseline in SUMMARY).
    Produce a Claude first-pass native-quality Ukrainian translation for EVERY key in ALL namespaces where uk currently equals en or is missing: uk/dashboard.json, uk/transactions.json, uk/categories.json, uk/chat.json, uk/ai.json, and bring uk/jars.json (from 04-01/04-02) to the same audited quality. Preserve every `{{interpolation}}` placeholder and ICU plural key structure exactly (e.g. transactions count plurals, propagation_one/propagation_many) — placeholders must not be translated or reordered in a way that breaks i18next. Financial/UI terminology must be natural Ukrainian (not transliterated English); keep technical tokens (currency formatting handled by Intl, do not translate currency symbols).
    Create en/settings.json + uk/settings.json with the keys this phase needs (title, language_section, language_en, language_uk, open_a11y, plus placeholders the SettingsScreen renders). Wire the `settings` ns into src/lib/i18n/index.ts: add `settingsEn`/`settingsUk` imports and `settings: { ...(en as {settings?:Record<string,string>}).settings, ...settingsEn }` into enBundle + the uk equivalent into ukBundle, exactly mirroring the existing chat/ai entries. Also correct/remove the stale ":28" comment block so it no longer falsely claims uk mirrors en.
    This is the Claude first-pass artifact; the paid Upwork native-Ukrainian review is a downstream human step (D-08) — the reviewer's committed edits are the approval artifact (mirrors Phase-3 review-artifact pattern). Add a SUMMARY note flagging that the Upwork native review is the outstanding human approval gate for ROADMAP success-criterion #2 (does NOT block tsc/lint or this plan's completion; AI chat response language is model-side, out of i18next scope, does not block SET-02 — D-08).
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/mobile && npx tsc --noEmit` exits 0; `cd apps/mobile && npx expo lint` exits 0.
    - For every namespace, en and uk JSON have identical key sets: `python3 -c "import json,glob; [exit(1) for ns in ['dashboard','transactions','categories','chat','ai','jars','settings'] if set(json.load(open(f'apps/mobile/src/i18n/locales/en/{ns}.json')))!=set(json.load(open(f'apps/mobile/src/i18n/locales/uk/{ns}.json')))]"` exits 0.
    - For dashboard/transactions/categories/chat/ai, fewer than 5% of uk values are byte-identical to en (i.e. genuinely translated, not stubs) — measurable via a python diff count per ns.
    - All `{{...}}` placeholders present in en values are present in the corresponding uk values (placeholder-preservation check — script in Task 3 enforces this; here at least manual+grep evidence).
    - src/lib/i18n/index.ts has `settingsEn`/`settingsUk` merged into both bundles and the stale :28 "uk mirrors en" comment is removed/corrected (grep must NOT find "uk bundle currently mirrors en").
    - SUMMARY records measured per-namespace pre-translation baseline + flags Upwork native review as the outstanding human gate for ROADMAP SC#2.
  </acceptance_criteria>
  <done>All namespaces have a complete native-first-pass UK translation with preserved placeholders; settings ns wired; stale comment fixed; tsc + lint exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: en/uk key-parity + placeholder-parity gate script + deferral-repoint doc correction</name>
  <files>apps/mobile/scripts/check-i18n-parity.mjs, apps/mobile/package.json, .planning/STATE.md, .planning/phases/03-ai-categorization-chat/03-VERIFICATION.md</files>
  <read_first>
    apps/mobile/scripts/reset-project.js (existing scripts/ idiom — plain node, no ts),
    apps/mobile/package.json (scripts block; no jest/test — add a lint:i18n script, NOT a test script),
    apps/mobile/src/i18n/locales/en + uk (all namespace JSON to validate),
    .planning/STATE.md ("Phase 3 deferred → UAT / later phases" section, the merchant_overrides line targeting "Phase 4"),
    .planning/phases/03-ai-categorization-chat/03-VERIFICATION.md (the deferred-target entry pointing at Phase 4)
  </read_first>
  <action>
    Create apps/mobile/scripts/check-i18n-parity.mjs (node ESM, mirror the plain-script idiom of reset-project.js): for every namespace file present under src/i18n/locales/en/, assert (1) the same file exists under uk/, (2) en key set === uk key set (report missing keys in either direction with the namespace + key, non-zero exit on any mismatch), and (3) every `{{placeholder}}` token appearing in an en value also appears in the matching uk value and vice-versa (regex extract `\{\{[^}]+\}\}`, set-compare; non-zero exit on mismatch). Print a concise pass/fail summary. No external deps (use node:fs).
    Wire it: add a `"lint:i18n": "node ./scripts/check-i18n-parity.mjs"` script to apps/mobile/package.json (do NOT add or rely on a `test` script — jest harness is absent; this is the i18n parity gate that complements the tsc+lint verification gate per D-08). The phase verification commands (this plan's acceptance + the phase <verification> block) MUST invoke `npm run -s lint:i18n` so parity gates verification consistently with tsc/lint.
    DEFERRAL REPOINT (doc-correction only, per 04-CONTEXT Deferred Ideas — merchant_overrides remote DDL is OUT of Phase 4 scope and was orphaned): edit .planning/STATE.md — in the "Phase 3 deferred → UAT / later phases" section change the line `Remote supabase/migrations/0001_merchant_overrides.sql DDL+RLS → Phase 4` so its target reads `→ Phase 5/6 (re-deferred 2026-05-15; Phase 4 ROADMAP never listed it — orphaned deferral repointed)`. Edit .planning/phases/03-ai-categorization-chat/03-VERIFICATION.md similarly: any deferred-target entry that says the merchant_overrides remote DDL / resolveTier1 wiring lands in "Phase 4" must be changed to "Phase 5/6" with the same brief rationale. Do NOT implement the DDL; resolveTier1 stays an intentional no-op. This is the ONLY change to those two .planning files.
  </action>
  <verify>
    <automated>cd apps/mobile && npm run -s lint:i18n && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/mobile && npm run -s lint:i18n` exits 0 (all namespaces key-parity + placeholder-parity pass).
    - Negative check: temporarily removing a uk key (then restoring) makes `lint:i18n` exit non-zero — script actually gates (executor demonstrates in SUMMARY, restores file).
    - `cd apps/mobile && npx tsc --noEmit` exits 0; `cd apps/mobile && npx expo lint` exits 0.
    - package.json has a `lint:i18n` script and NO `test` script was added (grep package.json scripts).
    - check-i18n-parity.mjs uses only node builtins (no new dependency; grep no bare non-node import).
    - .planning/STATE.md merchant_overrides line now targets "Phase 5/6" (grep must NOT find `merchant_overrides.sql DDL+RLS → Phase 4`); 03-VERIFICATION.md updated likewise; no other .planning content changed by this plan (git diff scoped to STATE.md + 03-VERIFICATION.md for .planning).
    - No implementation of merchant_overrides DDL added anywhere (grep: no new supabase/migrations file, resolveTier1 unchanged).
  </acceptance_criteria>
  <done>Parity+placeholder script gates verification via npm run lint:i18n; orphaned merchant_overrides deferral repointed to Phase 5/6 (doc-only); tsc + lint exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → language preference store | language selection persisted via expo-secure-store (NOT AsyncStorage — CLAUDE.md) |
| no network boundary | i18n is fully local bundled JSON; no remote translation fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | translation JSON placeholders | mitigate | check-i18n-parity.mjs enforces `{{placeholder}}` parity en↔uk so a bad translation cannot silently break interpolation/crash a screen |
| T-04-03-02 | Information disclosure | language pref persistence | mitigate | persisted via expo-secure-store store adapter (existing onboarding store), never AsyncStorage, never logged |
| T-04-03-03 | Denial of service | runtime remount on language switch | accept | full remount on explicit user action only; brief flash accepted (04-CONTEXT specifics); no data loss (state persisted) |
| T-04-03-SC | Tampering | npm/pip/cargo installs | mitigate | check-i18n-parity.mjs uses node builtins only — zero new dependencies; any new install requires a blocking-human legitimacy checkpoint first |
</threat_model>

<verification>
- `cd apps/mobile && npx tsc --noEmit` exits 0
- `cd apps/mobile && npx expo lint` exits 0
- `cd apps/mobile && npm run -s lint:i18n` exits 0 (key + placeholder parity across all namespaces)
- Settings is a stack route (D-06), language toggle drives setLanguage + store + root key-bump (D-07)
- All uk namespaces are real native-first-pass (not en stubs); Upwork native review flagged as outstanding human gate (D-08)
- merchant_overrides orphaned deferral repointed to Phase 5/6 (doc-only); no DDL implemented
- No BANNED_COLORS; expo-secure-store (no AsyncStorage)
</verification>

<success_criteria>
- User opens Settings via a dashboard-header gear (new stack route, four-tab shell preserved)
- User switches EN↔UK at runtime; every screen incl. date headers + sticky list headers retranslates without restart (SET-02)
- en/uk key + placeholder parity gates verification; all namespaces natively translated (Claude first-pass; Upwork review outstanding per D-08)
- Orphaned merchant_overrides deferral repointed to Phase 5/6 (doc correction only)
</success_criteria>

<output>
Create `.planning/phases/04-jars-i18n-accessibility/04-03-SUMMARY.md` when done. Record the context7 i18next/remount finding, measured UK-translation baseline, and the outstanding Upwork-native-review human gate.
</output>
