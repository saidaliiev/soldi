---
phase: 04-jars-i18n-accessibility
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - apps/mobile/src/components/BottomSheet/BottomSheetPrimitive.tsx
  - apps/mobile/src/components/BottomSheet/ConfirmModal.tsx
  - apps/mobile/src/features/dashboard/DonutChart.tsx
  - apps/mobile/src/features/dashboard/Sparkline.tsx
  - apps/mobile/src/features/jars/JarRing.tsx
  - apps/mobile/src/design/tokens.ts
  - apps/mobile/src/design/contrast.ts
  - apps/mobile/src/design/contrast.test.ts
  - apps/mobile/src/features/dashboard/MonthlyTotalHero.tsx
  - apps/mobile/app/(tabs)/_layout.tsx
  - apps/mobile/src/features/transactions/TransactionRow.tsx
  - apps/mobile/src/features/transactions/RecategorizeBottomSheet.tsx
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-03, QUAL-04]
must_haves:
  truths:
    - "Opening any bottom sheet / modal moves VoiceOver focus into it (BottomSheetPrimitive/ConfirmModal); closing returns focus to the invoking control — no dead ends; applies to existing + new jar/settings UI (D-09)"
    - "Skia canvases announce a single summarizing label; their child paths are not individually focusable"
    - "Every foreground/background token pair meets WCAG AA (4.5:1 body, 3:1 large); remediated globally in tokens.ts without violating BANNED_COLORS"
    - "Oswald hero number, tab bar, and jar-ring label clamp at AccessibilityXXXL and reflow without truncation/overlap"
    - "Swipe-to-recategorize has a VoiceOver-accessible alternative action"
  artifacts:
    - path: "apps/mobile/src/components/BottomSheet/BottomSheetPrimitive.tsx"
      provides: "standardized focus-on-open + return-focus-on-close contract"
      contains: "AccessibilityInfo"
    - path: "apps/mobile/src/design/contrast.ts"
      provides: "pure WCAG contrast-ratio util + token-pair audit table"
      exports: ["contrastRatio", "auditTokenPairs"]
    - path: "apps/mobile/src/design/contrast.test.ts"
      provides: "colocated assertions that every audited pair passes AA"
  key_links:
    - from: "apps/mobile/src/components/BottomSheet/BottomSheetPrimitive.tsx"
      to: "react-native AccessibilityInfo"
      via: "setAccessibilityFocus on open / return on close"
      pattern: "AccessibilityInfo"
    - from: "apps/mobile/src/design/contrast.test.ts"
      to: "apps/mobile/src/design/tokens.ts"
      via: "auditTokenPairs over COLORS pairs"
      pattern: "auditTokenPairs\\("
    - from: "apps/mobile/src/features/dashboard/MonthlyTotalHero.tsx"
      to: "maxFontSizeMultiplier clamp"
      via: "Text maxFontSizeMultiplier + reflow"
      pattern: "maxFontSizeMultiplier"
---

<objective>
Fourth slice — the full accessibility contract (QUAL-01..04) applied to the ENTIRE app (existing screens + the new jar/settings UI from 04-01..04-03). Phase-2's a11y-primitive claims are treated as unverified, not done (04-CONTEXT D-09). Runs in wave 3 because it standardizes focus + clamps + contrast across UI that 04-01/04-02/04-03 must already exist for.

Purpose: ROADMAP success criteria 4 (VoiceOver no dead ends) + 5 (zero AA contrast failures), plus dynamic-type (QUAL-04) and the QUAL-01 accessibilityLabel/role sweep.
Output: standardized focus-on-open/return-on-close in BottomSheetPrimitive + ConfirmModal; single summarizing label on all Skia canvases; pure WCAG contrast util + colocated audit test + global tokens.ts remediation (BANNED_COLORS re-verified); maxFontSizeMultiplier clamps with reflow on Oswald hero / tab bar / jar-ring label; VoiceOver alternative for swipe-recategorize.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-jars-i18n-accessibility/04-CONTEXT.md
@./CLAUDE.md

<interfaces>
<!-- Extracted from codebase. Executor uses these directly. -->

A11y baseline (measured): zero `setAccessibilityFocus` / `AccessibilityInfo` / `maxFontSizeMultiplier` usage anywhere in src/ or app/ today (focus-management + dynamic-type clamp are GREENFIELD); ~40 components already have accessibilityLabel/Role; only ONE accessibilityViewIsModal in the codebase.

apps/mobile/src/components/BottomSheet/BottomSheetPrimitive.tsx + ConfirmModal.tsx — the single shared sheet/modal primitives. Add the focus contract here ONCE: on open call `AccessibilityInfo.setAccessibilityFocus(reactTag)` on the sheet's first focusable / a header element ref (`findNodeHandle`); on close return focus to the invoking control (the primitive should accept an optional `returnFocusRef` or expose an onClose hook the caller passes its trigger ref to). Globally-mounted sheets driven by Zustand stores: RecategorizeBottomSheet (recategorizeStore open/close), JarCreateBottomSheet (jarStore — 04-01), ChatBottomSheet — they all funnel through this primitive, so fixing the primitive fixes all of them.

Skia canvases needing a single summarizing label + decorative-hidden children:
- apps/mobile/src/features/dashboard/DonutChart.tsx (Canvas + per-slice Path)
- apps/mobile/src/features/dashboard/Sparkline.tsx
- apps/mobile/src/features/jars/JarRing.tsx (04-02 — already authored its own ring_a11y summarizing label; here only standardize/verify, do not regress)

Tokens — apps/mobile/src/design/tokens.ts: COLORS.background #F7F1E8 (warm cream shell), COLORS.textSecondary #7A5C52, COLORS.textMuted #B8968A (KNOWN AA SUSPECTS on cream — D-09), COLORS.accent, COLORS.sage #9DA88C, COLORS.success #7A876A; BANNED_COLORS is an exported array (re-verify any remediated value is NOT in it: AI-slop blue #667EEA / purple #8B7AB8 / lavender #E8E0FF / bright green #10B981 / fintech blue #1A73E8 #2563EB). Editing tokens.ts ripples app-wide — regression-sensitive.

Dynamic type — Oswald hero number: apps/mobile/src/features/dashboard/MonthlyTotalHero.tsx (uses an Oswald TYPE.* preset). Tab bar labels: app/(tabs)/_layout.tsx TabLabel uses `<Text allowFontScaling style={[styles.label,...]}>`. Jar-ring center label: apps/mobile/src/features/jars/JarRing.tsx (04-02). These three need `maxFontSizeMultiplier` clamps + verified reflow at AccessibilityXXXL.

Swipe-recategorize: apps/mobile/src/features/transactions/TransactionRow.tsx (swipe gesture) → RecategorizeBottomSheet (recategorizeStore.openFor). VoiceOver alternative: add an `accessibilityActions` entry (e.g. {name:'recategorize'}) + `onAccessibilityAction` that calls the same recategorizeStore.openFor — so VoiceOver users get the action without the swipe.

Pure-util + colocated-test idiom: apps/mobile/src/features/dashboard/donutArcs.ts + donutArcs.test.ts (node `assert`, no jest — STATE.md / [[jest-harness-missing]]). Mirror for src/design/contrast.ts + contrast.test.ts.

Verification gate: tsc --noEmit + expo lint + npm run -s lint:i18n (from 04-03) + grep/source assertions. NO jest, NO `npm test`.
</interfaces>

# Source-of-truth analogs the executor MUST read before writing
@apps/mobile/src/components/BottomSheet/BottomSheetPrimitive.tsx
@apps/mobile/src/components/BottomSheet/ConfirmModal.tsx
@apps/mobile/src/features/transactions/RecategorizeBottomSheet.tsx
@apps/mobile/src/features/transactions/recategorizeStore.ts
@apps/mobile/src/features/transactions/TransactionRow.tsx
@apps/mobile/src/features/dashboard/DonutChart.tsx
@apps/mobile/src/features/dashboard/Sparkline.tsx
@apps/mobile/src/features/jars/JarRing.tsx
@apps/mobile/src/features/dashboard/MonthlyTotalHero.tsx
@apps/mobile/app/(tabs)/_layout.tsx
@apps/mobile/src/design/tokens.ts
@apps/mobile/src/features/dashboard/donutArcs.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardized focus-on-open / return-on-close + Skia summarizing labels + swipe VoiceOver alternative</name>
  <files>apps/mobile/src/components/BottomSheet/BottomSheetPrimitive.tsx, apps/mobile/src/components/BottomSheet/ConfirmModal.tsx, apps/mobile/src/features/dashboard/DonutChart.tsx, apps/mobile/src/features/dashboard/Sparkline.tsx, apps/mobile/src/features/jars/JarRing.tsx, apps/mobile/src/features/transactions/TransactionRow.tsx, apps/mobile/src/features/transactions/RecategorizeBottomSheet.tsx</files>
  <read_first>
    apps/mobile/src/components/BottomSheet/BottomSheetPrimitive.tsx (current props, open/close lifecycle, ref surface),
    apps/mobile/src/components/BottomSheet/ConfirmModal.tsx (visible/onClose/onConfirm props),
    apps/mobile/src/features/transactions/RecategorizeBottomSheet.tsx + recategorizeStore.ts (globally-mounted store-driven sheet that funnels through the primitive),
    apps/mobile/src/features/transactions/TransactionRow.tsx (swipe gesture → openFor),
    apps/mobile/src/features/dashboard/DonutChart.tsx + Sparkline.tsx (Canvas + child Path — need single summarizing label),
    apps/mobile/src/features/jars/JarRing.tsx (04-02 ring_a11y label — verify, don't regress)
  </read_first>
  <action>
    CONTEXT7 DOC-CHECK (mandatory, first): resolve `react-native` AccessibilityInfo + `expo`/RN findNodeHandle to confirm the installed API for `AccessibilityInfo.setAccessibilityFocus(reactTag)` and the supported way to obtain a reactTag from a ref (findNodeHandle vs ref.current handle) on the project's RN 0.81. Record finding in SUMMARY.
    In BottomSheetPrimitive.tsx implement the focus contract ONCE (D-09): on the sheet becoming visible, move VoiceOver focus to the sheet (a header/title element ref → `AccessibilityInfo.setAccessibilityFocus(findNodeHandle(ref))`), set `accessibilityViewIsModal` on the sheet container so background content is not reachable; on close, call `AccessibilityInfo.setAccessibilityFocus` back to a caller-supplied `returnFocusRef` (add an optional prop; if absent, no-op gracefully). Apply the same contract to ConfirmModal.tsx. Because RecategorizeBottomSheet / JarCreateBottomSheet / ChatBottomSheet all render through this primitive, pass their invoking control's ref as returnFocusRef where the trigger is known (at minimum wire RecategorizeBottomSheet via recategorizeStore — store the trigger ref or refocus the originating TransactionRow; if a ref cannot be threaded cleanly, document the limitation in SUMMARY and ensure focus at least lands somewhere valid — never a dead end).
    Skia canvases: ensure DonutChart.tsx, Sparkline.tsx, JarRing.tsx each expose ONE summarizing accessibilityLabel on the Canvas/wrapper that conveys the data narratively (use existing i18n keys; DonutChart/Sparkline get new dashboard ns keys if needed — maintain en/uk parity so 04-03's lint:i18n still passes), and child `<Path>` elements are `accessibilityElementsHidden`/`importantForAccessibility="no-hide-descendants"` (decorative). JarRing already has ring_a11y from 04-02 — verify present, do not duplicate.
    Swipe-recategorize VoiceOver alternative: on TransactionRow.tsx add `accessibilityActions={[{ name:'recategorize', label: t(...) }]}` + `onAccessibilityAction` handler that calls the same `recategorizeStore.getState().openFor(txId)` the swipe triggers, so VoiceOver users get the recategorize action without performing a swipe gesture (D-09).
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit && npm run -s lint:i18n</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/mobile && npx tsc --noEmit` exits 0; `cd apps/mobile && npx expo lint` exits 0; `cd apps/mobile && npm run -s lint:i18n` exits 0 (any new i18n keys keep parity).
    - BottomSheetPrimitive.tsx + ConfirmModal.tsx import `AccessibilityInfo` and call `setAccessibilityFocus` on open and on close-return (grep both files).
    - BottomSheetPrimitive sheet container sets `accessibilityViewIsModal` (grep) — background not reachable.
    - DonutChart.tsx, Sparkline.tsx, JarRing.tsx each have exactly one summarizing accessibilityLabel on the canvas/wrapper and child Path elements are accessibility-hidden (grep: a hidden/no-hide-descendants prop present near `<Path`; no per-Path accessibilityLabel).
    - TransactionRow.tsx has `accessibilityActions` + `onAccessibilityAction` calling `openFor(` (grep) — swipe VoiceOver alternative.
    - SUMMARY records the context7 AccessibilityInfo/findNodeHandle finding and any returnFocusRef threading limitation (no dead-end focus).
  </acceptance_criteria>
  <done>Focus contract standardized in the shared primitives; Skia canvases single-label + decorative children; swipe has VoiceOver alternative; tsc + lint + lint:i18n exit 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure WCAG contrast util + colocated audit test + global tokens.ts remediation</name>
  <files>apps/mobile/src/design/contrast.ts, apps/mobile/src/design/contrast.test.ts, apps/mobile/src/design/tokens.ts</files>
  <read_first>
    apps/mobile/src/design/tokens.ts (COLORS keys, the textMuted #B8968A / textSecondary #7A5C52 on background #F7F1E8 suspects, BANNED_COLORS array),
    apps/mobile/src/features/dashboard/donutArcs.test.ts (node `assert` colocated-test idiom to mirror),
    ./CLAUDE.md (BANNED_COLORS list + design token rules)
  </read_first>
  <behavior>
    - contrast.test.ts (node assert, colocated): `contrastRatio('#000000','#FFFFFF')` ≈ 21; `contrastRatio('#FFFFFF','#FFFFFF')` === 1; ratio is symmetric.
    - `auditTokenPairs()` returns, for each foreground/background pair that actually occurs in the app (text* on background/surface, accent on background, etc.), `{ fg, bg, ratio, requiredAA, passes }`. After remediation EVERY entry's `passes` is true (4.5:1 for body text, 3:1 for large/hero text).
    - The test asserts no audited pair fails AA AND that no remediated COLORS value appears in BANNED_COLORS.
  </behavior>
  <action>
    Create apps/mobile/src/design/contrast.ts: pure functions `contrastRatio(hexA: string, hexB: string): number` (WCAG 2.1 relative-luminance formula, sRGB linearization, (L1+0.05)/(L2+0.05)) and `auditTokenPairs(): readonly { fg: string; bg: string; fgToken: string; bgToken: string; ratio: number; requiredAA: number; passes: boolean }[]` enumerating the real fg/bg COLORS pairs the app renders (at minimum: textPrimary/textSecondary/textMuted/accent each over background and over surface; tab active/inactive over surface; sage/success over background). requiredAA = 3.0 for tokens used only as large/hero text (document which), else 4.5.
    Create apps/mobile/src/design/contrast.test.ts mirroring donutArcs.test.ts node-assert idiom, asserting the behaviors above. Author even though jest absent.
    Run `auditTokenPairs()` reasoning during implementation; for every failing pair, REMEDIATE in tokens.ts globally (D-09): darken textMuted / textSecondary (and any other failing fg) just enough to clear AA on the cream background, choosing values that stay within the warm-earth palette and are NOT in BANNED_COLORS and do not introduce AI-slop blue/purple/lavender/bright-green/fintech-blue. Adjust the smallest set of tokens; document each old→new hex + the resulting ratio in SUMMARY (regression-sensitive — these ripple app-wide). Do NOT hardcode any color outside tokens.ts; do not add new BANNED entries.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit && npx expo lint</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/mobile && npx tsc --noEmit` exits 0; `cd apps/mobile && npx expo lint` exits 0; `cd apps/mobile && npm run -s lint:i18n` exits 0.
    - contrast.ts exports `contrastRatio` + `auditTokenPairs` (grep export function); contrast.test.ts exists with the documented assertions.
    - Static assertion that no audited pair fails: a node one-liner running `auditTokenPairs()` and checking every `.passes === true` exits 0 — e.g. `node --input-type=module -e "import('./apps/mobile/src/design/contrast.ts').then(...)"` OR (if TS import at node is impractical) the colocated contrast.test.ts contains an explicit `assert(pair.passes)` loop AND a SUMMARY table of every pair's measured ratio ≥ requiredAA.
    - No remediated tokens.ts value is present in `BANNED_COLORS` (grep each changed hex against the BANNED_COLORS array — must not match), and none of #667EEA/#8B7AB8/#E8E0FF/#10B981/#1A73E8/#2563EB introduced.
    - SUMMARY documents every old→new token hex change with before/after contrast ratio (regression traceability).
    - No color hardcoded outside tokens.ts by this task (grep new hex literals only in tokens.ts / contrast.ts test fixtures).
  </acceptance_criteria>
  <done>Pure contrast util + audit test exist; every real token pair clears WCAG AA after global tokens.ts remediation; BANNED_COLORS re-verified clean; tsc + lint exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: Dynamic-type maxFontSizeMultiplier clamps + reflow on Oswald hero, tab bar, jar-ring label</name>
  <files>apps/mobile/src/features/dashboard/MonthlyTotalHero.tsx, apps/mobile/app/(tabs)/_layout.tsx, apps/mobile/src/features/jars/JarRing.tsx</files>
  <read_first>
    apps/mobile/src/features/dashboard/MonthlyTotalHero.tsx (Oswald hero number Text + TYPE.* preset),
    apps/mobile/app/(tabs)/_layout.tsx (TabLabel <Text allowFontScaling> + styles.label),
    apps/mobile/src/features/jars/JarRing.tsx (04-02 Oswald center label),
    @design/typography (TYPE.* presets — confirm which is the Oswald hero preset),
    apps/mobile/src/design/tokens.ts (SPACING for reflow gaps)
  </read_first>
  <action>
    CONTEXT7 DOC-CHECK (mandatory, first): resolve `react-native` Text to confirm the installed `maxFontSizeMultiplier` semantics + interaction with `allowFontScaling` and `adjustsFontSizeToFit`/`numberOfLines` on RN 0.81. Record finding in SUMMARY.
    Apply a `maxFontSizeMultiplier` clamp to the three large/fixed-layout text surfaces (D-09 / QUAL-04) so AccessibilityXXXL does not break layout while still scaling meaningfully:
    - MonthlyTotalHero.tsx Oswald hero number: add a sensible `maxFontSizeMultiplier` (e.g. ~1.6–2.0, choose so XXXL still fits the hero band) and ensure the container reflows (wrap/min-height/allow the number to shrink-to-fit with adjustsFontSizeToFit + numberOfLines if needed) rather than clipping.
    - app/(tabs)/_layout.tsx TabLabel Text: clamp `maxFontSizeMultiplier` low enough that the tab bar stays single-line and the icon+label fit (tab bars cannot grow unbounded); keep `allowFontScaling`.
    - JarRing.tsx center label (Oswald): clamp so the largest dynamic type still fits inside the ring; reflow (shrink-to-fit) rather than overflow the canvas.
    Verify reflow by reasoning about the XXXL case and documenting expected behavior; add a brief comment at each clamp citing QUAL-04 + the chosen multiplier rationale. Tokens/typeface unchanged (still Oswald via TYPE.*); no BANNED_COLORS; no inline hex.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit && npx expo lint</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/mobile && npx tsc --noEmit` exits 0; `cd apps/mobile && npx expo lint` exits 0; `cd apps/mobile && npm run -s lint:i18n` exits 0.
    - `grep -rn "maxFontSizeMultiplier" apps/mobile/src/features/dashboard/MonthlyTotalHero.tsx apps/mobile/app/(tabs)/_layout.tsx apps/mobile/src/features/jars/JarRing.tsx` returns a clamp in all three files.
    - Each clamp site has an inline comment citing QUAL-04 + the multiplier rationale (grep `QUAL-04` near each).
    - Reflow strategy present at the hero + ring (adjustsFontSizeToFit/numberOfLines or documented wrap) — not a hard clip (grep evidence + SUMMARY rationale).
    - SUMMARY records the context7 maxFontSizeMultiplier finding and the chosen multiplier per surface with the XXXL-fit rationale.
    - Typeface unchanged (still Oswald TYPE.* preset — grep no typeface swap); no BANNED_COLORS / inline hex introduced.
  </acceptance_criteria>
  <done>Dynamic-type clamps + reflow on Oswald hero / tab bar / jar-ring label; XXXL does not break layout; tsc + lint + lint:i18n exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| no external boundary | a11y/contrast/dynamic-type changes are pure UI/styling + accessibility metadata; no data or network |
| tokens.ts global ripple | a contrast remediation in tokens.ts changes color app-wide — internal regression boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | global tokens.ts remediation | mitigate | contrast.test.ts colocated audit + BANNED_COLORS re-verification gates the change; old→new hex + ratio documented in SUMMARY for regression traceability |
| T-04-04-02 | Denial of service | unbounded dynamic type breaking tab bar / hero | mitigate | maxFontSizeMultiplier clamps + reflow at the three fixed-layout surfaces so AccessibilityXXXL cannot make the UI unusable |
| T-04-04-03 | Repudiation | a11y "done" claim without device test | accept | tsc/lint/grep cannot prove VoiceOver UX; SUMMARY explicitly flags on-device VoiceOver pass + axe-style contrast audit (ROADMAP SC#4/#5) as an outstanding human/device verification gate (consistent with project verification-gate honesty rule) |
| T-04-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (AccessibilityInfo/findNodeHandle are react-native builtins); any new install requires a blocking-human legitimacy checkpoint first |
</threat_model>

<verification>
- `cd apps/mobile && npx tsc --noEmit` exits 0
- `cd apps/mobile && npx expo lint` exits 0
- `cd apps/mobile && npm run -s lint:i18n` exits 0 (any new a11y i18n keys keep parity)
- Focus contract in BottomSheetPrimitive + ConfirmModal (AccessibilityInfo.setAccessibilityFocus open + return; accessibilityViewIsModal)
- Skia canvases single summarizing label + decorative-hidden children
- contrast.ts + colocated audit test: every real token pair ≥ AA; BANNED_COLORS clean
- maxFontSizeMultiplier clamp + reflow on Oswald hero / tab bar / jar-ring label
- swipe-recategorize has accessibilityActions VoiceOver alternative
- SUMMARY flags on-device VoiceOver + axe contrast audit as outstanding human/device gate (ROADMAP SC#4/#5)
</verification>

<success_criteria>
- VoiceOver enters every sheet/modal on open and returns to the trigger on close — no dead ends (QUAL-03)
- Every interactive element has accessibilityLabel + accessibilityRole; Skia canvases summarize, don't trap (QUAL-01/03)
- Every real token fg/bg pair meets WCAG AA after global tokens.ts remediation; BANNED_COLORS not violated (QUAL-02)
- Oswald hero, tab bar, jar-ring label clamp + reflow at AccessibilityXXXL without layout break (QUAL-04)
- On-device VoiceOver pass + axe-style contrast audit (ROADMAP SC#4/#5) flagged as outstanding human/device verification
</success_criteria>

<output>
Create `.planning/phases/04-jars-i18n-accessibility/04-04-SUMMARY.md` when done. Record context7 findings (AccessibilityInfo, maxFontSizeMultiplier), every old→new token hex + contrast ratio, and the outstanding on-device VoiceOver/axe human gate.
</output>
